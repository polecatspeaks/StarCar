good morning buddy, its 27 July 26, please open the yard and get ready for the days work

---

ok we missed out morning start routine, what happened?

---

ok the retro was part of it, also we had no friction log from yesterday to review for tooling.

---

<task-notification>
<task-id>bkgxrgd1l</task-id>
<tool-use-id>toolu_011WdHyjJpHNG5z6TeGN6aNX</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\bkgxrgd1l.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI to terminal state for fa63fcb (background)" completed (exit code 0)</summary>
</task-notification>

---

let's go with the goodnight skill update. can we also mine the friction log?

---

<task-notification>
<task-id>bu6bh1p7a</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\bu6bh1p7a.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI to terminal state for cebfa91 (background)" completed (exit code 0)</summary>
</task-notification>

---

ok so some more feedback on the board UI. Right now the dispatches only show what looks like a hash as the train title, and the trains lane looks like it's old static info?[Image #1] Do we have cards for these issues are are this fast follow on feedbacks?

---

yeah queue those up behind the current train, unless they can roll now?

---

<task-notification>
<task-id>b2jnb01xh</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\b2jnb01xh.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI to terminal state for a8beef6 (background)" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>ae3d64acd969fcdf2</task-id>
<tool-use-id>toolu_01UjwBn5gyDy3uydELKhZjtg</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\ae3d64acd969fcdf2.output</output-file>
<status>completed</status>
<summary>Agent "Car r2 fix cycle #69+#71" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All seven findings are fixed, red-first verified, and committed across 5 logical commits on `car/view-69-71`, HEAD `2861bfd8a99d8213ca0058a999ab91706f4ccc9b`, base `256d8d5a5124113108025894d9c6a097123b2cfb`. Working tree is clean (`git status --porcelain` empty).

## Per-finding summary

**MAJOR-R1-1** (commit `3fa9b21`) — owner-ruled fix: `renderSupersededList` → `renderSupersededDisclosure` in `board/web/js/dom-writer.js`, now appended as a DOM child of its owner row/chip (`chip.appendChild(...)` / `row.appendChild(...)` instead of `cars.appendChild(...)` / `rows.appendChild(...)`), behind a collapsed-by-default `&lt;details&gt;/&lt;summary&gt;`. Red-first: two new structural tests in `board/web/test/dom-writer.test.js` seed 5 subjects (reviewer's own wrap-boundary recipe); observed red at base: `.track-cars must hold exactly one child per car ... 10 !== 5`. Green after fix: 44→46 tests in that file, then 208/208 for the whole suite. Verified live in a real browser too (`docs/screenshots/2026-07-26-view-69-71-candidates/superseded-collapsed-r2.png`, `superseded-expanded-r2.png`; `discInsideRow: true` observed via `closest('.solari-row').contains(disclosure)`). **M1 decision (disclosed, not owner-ruled): ephemeral, not sessionStorage-backed** — chosen as the minimal option since a per-row key would be one-per-subject (open-ended, no eviction story) and `renderBoard` already clears `root.textContent` every repaint regardless; documented in `docs/contracts/state-ledger.md` as an explicit "not a seventh field" disclosure for reviewer adjudication.

**MAJOR-R1-2** (commit `97c3dc6`) — new `board/web/test/browser-conditions-persistence.test.js`, real Chromium/real Go binary per `browser-register-cascade.test.js`'s pattern. Non-vacuity: removed `app.js:148`'s listener (sha256 confirmed `9bbcb03a...`, matching the reviewer's own recorded value), observed red: `REGRESSION (#69 MAJOR-R1-2): the strip closed across a real DOM rebuild ... false !== true`; restored byte-identical, 2/2 green.

**MAJOR-R1-3** (commit `ca20830`) — the "untestable branch" comment in `board/assemble/assemble.go` was false (`winnerKind` only selects the branch; `Spend`/`Output.Dispatches` are exported). Added `TestAssembleDispatchRenderFailedIsReachable`: real `fold.Fold` → mutate exported `Spend` to a channel → `Assemble()` → asserts `dispatch-render-failed` condition with `RecordDir="probe-subj"`. Non-vacuity: renamed the condition code string, observed red (`got [{dispatch-render-failed-INJECTED ...}]`), restored byte-identical (sha256 `f7d7a950...` confirmed). Comment corrected; the commit message's matching false claim is immutable history, noted rather than rewritten.

**MAJOR-R1-4** (commit `d9ffcfe`) — `lanes.js:16`'s `snapshot.go:41-45` citation (shifted to :48 by this same train's own insertion) converted to symbol form per `gating-matrix.md:49`'s own already-landed lesson.

**MINOR-R1-1** (commit `ca20830`, same as R1-3) — `store.RecordDirFromRelPath` exported; `assemble.go`'s `recordDirBySubject` now calls it instead of inlining `filepath.Dir` (unused `path/filepath` import removed). Refactor only, existing tests cover both call sites.

**MINOR-R1-2** (commit `3fa9b21`, same as R1-1) — `schema/yard-snapshot.schema.json` gains `$defs.dispatchSupersededItem` (`kind`+`at` both required strings), `$ref`'d from both payloads; `dom-writer.js` filters malformed items (Law 1 honest-absence, all-malformed → no disclosure). Non-vacuity: guard disabled, both new tests red (`2 !== 1`, `1 !== 0`), restored, green. **Fallout fixed in the same commit**: `scripts/tests/WirePayload.Tests.ps1`'s `Test-PayloadDef` helper extracted a `$def` standalone without sibling `$defs`, so the new `$ref` silently failed (`Test-Json -ErrorAction SilentlyContinue`) — fixed by carrying the full `$defs` map into the wrapped document.

**MINOR-R1-3** (commit `2861bfd`) — new `throwingStorage` fake + two tests pinning both catch branches in `condition-open-state.js`. Non-vacuity: each `try/catch` removed in turn, observed red (`SecurityError: storage disabled` uncaught; `QuotaExceededError: storage full` via `assert.doesNotThrow`'s own failure message), restored byte-identical both times (sha256 `0c0be511...` confirmed), 12/12 green.

## Final suite totals at HEAD `2861bfd`

- Go `board/...`: **94 PASS / 0 FAIL** (base 93 + 1 new = 94), `go vet ./...` exit 0, clean.
- Node `board/web` (`node --test --test-reporter=tap`): **208 tests / 208 pass / 0 fail** (base 200 + 8 new = 208).
- Pester `scripts/tests`: **223 passed / 0 failed / 4 skipped / 227 total** (matches base exactly).
- Pester `scripts/probes`: **24 passed / 0 failed**.
- `probe-yard-snapshot.mjs`: **PASS**.

Base counts (93 Go / 200 Node / 223-4 Pester / 24 probes) were re-derived by me via `git stash` back to `256d8d5` before editing, not assumed from the review verdict.

## Disclosures / honest notes

- **M1 open question** (per brief): resolved as ephemeral/not-persisted — reasoning above and in `docs/contracts/state-ledger.md`'s new disclosure paragraph; this is the one item the brief asked me to decide and flag for the reviewer's adjudication, not an owner ruling.
- I could not run GitNexus `detect_changes()` — no GitNexus MCP tool was bound in this session's toolset (only Bash/PowerShell/Read/Edit/Write/Glob/Grep were available), and no `.gitnexus/` directory exists in this worktree. Disclosed rather than silently skipped; not one of the 7 findings.
- No contradictions found between the brief/verdict and the real code — every finding's file:line evidence checked out as described.

## Files touched (all under `C:\Users\Chris\git\starcar-wt\car-view-69-71`)

`board/web/js/dom-writer.js`, `board/web/css/board.css`, `board/web/js/lanes.js`, `board/web/js/app.js` (fault-injected and restored only, unmodified in final diff), `board/web/test/dom-writer.test.js`, `board/web/test/condition-open-state.test.js`, `board/web/test/browser-conditions-persistence.test.js` (new), `board/assemble/assemble.go`, `board/assemble/assemble_test.go`, `board/store/store.go`, `board/store/store_test.go`, `schema/yard-snapshot.schema.json`, `scripts/tests/WirePayload.Tests.ps1`, `docs/design/2026-07-21-v0-yard-skeleton-design.md`, `docs/contracts/state-ledger.md`, `docs/screenshots/2026-07-26-view-69-71-candidates/README.md` (new), `superseded-collapsed-r2.png` (new), `superseded-expanded-r2.png` (new).

```starcar-artifact
outcome: done
findings: 0 Major, 0 Minor (all 7 round-1 findings fixed and verified; 1 disclosed M1 scope decision for reviewer adjudication)
abstract: |
  Fix cycle round 2 on view train #69+#71, all 7 findings from
  artifacts/reviews/2026-07-27-view-69-71-review-r1-REJECT.md addressed,
  red-first, on car/view-69-71 base 256d8d5 through HEAD 2861bfd, 5 commits
  (3fa9b21, 97c3dc6, ca20830, d9ffcfe, 2861bfd), working tree clean.

  MAJOR-R1-1: renderSupersededDisclosure now appends the superseded block as
  a DOM CHILD of its owner row/chip (never a sibling of the shared grid/flex
  container), behind a collapsed-by-default details/summary disclosure - the
  conditions-strip idiom. Structural regression tests seed 5 subjects (the
  reviewer's own wrap-boundary recipe), observed red at base (10 direct
  children instead of 5), green after. Verified live in a real browser
  (recaptured screenshots, discInsideRow true observed). M1 (disclosed, not
  owner-ruled): the disclosure's open state is EPHEMERAL, never threaded
  through sessionStorage - minimal option, reasoning in
  docs/contracts/state-ledger.md's new disclosure paragraph, flagged here for
  reviewer adjudication.

  MAJOR-R1-2: new browser-conditions-persistence.test.js (real Chromium, real
  Go binary, browser-register-cascade.test.js's pattern) pins app.js:148's
  toggle listener. Non-vacuity: line removed (sha256 matched the review's own
  recorded value), observed red ("the strip closed across a real DOM rebuild
  ... false !== true"), restored byte-identical, green.

  MAJOR-R1-3: the false "untestable outside board/fold" claim corrected;
  TestAssembleDispatchRenderFailedIsReachable proves it reachable via real
  fold.Fold + mutating exported Spend to a channel + real Assemble(), non-
  vacuity proven by renaming the condition code and observing the assertion
  fail for the stated reason, then restoring byte-identical.

  MAJOR-R1-4: lanes.js's stale snapshot.go:41-45 line citation converted to a
  symbol citation (the Lane struct's doc comment, quoted verbatim) per
  gating-matrix.md:49's own already-landed lesson.

  MINOR-R1-1 (Law 6): store.RecordDirFromRelPath exported; assemble.go's
  recordDirBySubject calls it instead of inlining the identical rule: no
  duplicate rule remains between the two files. Refactor only, existing
  tests cover both call sites unchanged.

  MINOR-R1-2: schema/yard-snapshot.schema.json's superseded items are now
  type-constrained ({kind,at} both required strings via a new
  $defs.dispatchSupersededItem, $ref'd from both payloads); dom-writer.js
  guards the read (malformed items skipped, all-malformed renders absent,
  never empty) - non-vacuity proven by disabling the guard and observing
  both new tests red for the stated reason. Fallout fixed same-commit:
  scripts/tests/WirePayload.Tests.ps1's fragment-extraction helper did not
  carry sibling $defs, so the new $ref silently failed to resolve
  (Test-Json -ErrorAction SilentlyContinue swallowed it) - fixed by wrapping
  the full $defs map into the extracted standalone schema.

  MINOR-R1-3: throwingStorage fake + two new tests pin both catch branches
  in condition-open-state.js. Non-vacuity: each try/catch removed in turn,
  observed red (uncaught SecurityError; uncaught QuotaExceededError via
  assert.doesNotThrow's own message), restored byte-identical both times,
  green.

  Final suite totals at HEAD 2861bfd: Go board/... 94 PASS / 0 FAIL (base 93
  + 1 new), go vet clean; Node board/web 208 tests / 208 pass / 0 fail (base
  200 + 8 new); Pester scripts/tests 223 passed / 0 failed / 4 skipped / 227
  total (matches base exactly - no new Pester tests added, two pre-existing
  regressions from the schema change were fixed in the same commit that
  caused them); Pester scripts/probes 24/24; probe-yard-snapshot.mjs PASS.
  All base counts re-derived by this car via git stash back to 256d8d5
  before editing, not assumed from the round-1 verdict.

  Disclosed: GitNexus detect_changes() could not be run - no GitNexus MCP
  tool was bound in this session's toolset and no .gitnexus/ directory
  exists in this worktree; not one of the 7 findings, honest-disclosed
  rather than silently skipped. No contradictions found between the brief/
  verdict and the real code at base - every finding's file:line evidence
  checked out as described.
task-id: view-69-71-car-r2
```</result>
<usage><subagent_tokens>307489</subagent_tokens><tool_uses>226</tool_uses><duration_ms>2153790</duration_ms></usage>
</task-notification>

---

sounds good, let's get 75 and 76 queued up behind this train please

---

<task-notification>
<task-id>a8928de8b3b8e6b95</task-id>
<tool-use-id>toolu_01UKyKog5VHnWrdYqFKgH18D</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\a8928de8b3b8e6b95.output</output-file>
<status>completed</status>
<summary>Agent "Delta review r2 #69+#71" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>I have everything I need. Writing the verdict.

---

# VERDICT: REJECT — 3 Major, 2 Minor

**Base verified.** `git log -1 --format='%H'` → `2861bfd8a99d8213ca0058a999ab91706f4ccc9b`, detached HEAD, `git status --porcelain` empty. Matches the brief.

## Suites — re-derived by me at 2861bfd, never read from the car's report

| Suite | Command | Observed at `2861bfd` | Car claimed | Verdict |
|---|---|---|---|---|
| Node `board/web` | `node --test --test-reporter=tap` | **208 tests / 208 pass / 0 fail / 0 skip** | 208/208 | matches |
| Go `board/...` | `go test -count=1 -v ./...` | **94 top-level `--- PASS`**, 0 FAIL, 0 SKIP (+20 subtests) | 94 PASS | matches |
| `go vet ./...` | full-path go | exit 0, no output | clean | matches |
| Pester `scripts/tests` | `Invoke-Pester -PassThru` | **227 total / 222 passed / 0 failed / 5 skipped** | 223/0/4 | matches modulo the detached-HEAD skip |
| Pester `scripts/probes` | `Invoke-Pester -PassThru` | **24 total / 24 passed / 0 failed** | 24/24 | matches |
| `probe-yard-snapshot.mjs` | `node` | `PROBE RESULT: PASS`, exit 0 | PASS | matches |

The 5th skip, named exactly as round 1 named it: `run-session-start-guards.sh: agent-pull runner (#50).running the REAL guards prints [ci-baseline]'s marker ONLY when attached to a branch - conditional, never asserted as always-emitting`. Same detached-HEAD artefact, no new skip.

Go went 93 → 94 (+1: `TestAssembleDispatchRenderFailedIsReachable`). Node went 200 → 208 (+8: 2 browser persistence, 4 structural/malformed dom-writer, 2 throwing-storage).

---

# MAJOR FINDINGS

## MAJOR-R2-1 — The MAJOR-R1-1 fix's `flex-wrap: wrap` wraps EVERY dispatch row from one line to two, re-introducing the exact `#62` "crop rows mid-line" hazard the same CSS rule's own comment documents — and the new comment asserting the opposite is a confident falsehood

`board/web/css/board.css:623` adds `flex-wrap: wrap` to `.solari-row` (absent at `256d8d5` and at `b865cba` — confirmed by `git show 256d8d5:board/web/css/board.css` and `git show b865cba:...`, both showing `.solari-row { display: flex; gap: 0.6rem; ... }` with no `flex-wrap`). The commit's own comment at `board.css:616-622` claims:

&gt; *"flex-wrap:wrap lets ONLY the nested .solari-superseded-disclosure (flex-basis:100% above) drop to a second line WITHIN this row's own box - **the subject/state/elapsed cluster below stays one line per row (mockup convention preserved**; see the comment on white-space:nowrap immediately below), because none of those three carry flex-basis:100% and .solari-subject's own ellipsis truncation still applies before any of them would wrap."*

Repeated at `board.css:383-384` ("adding `flex-wrap:wrap` here only lets the DISCLOSURE (not the subject/state/elapsed cluster) drop to a second line") and at `board.css:380` ("preserving #67's density argument") and `dom-writer.js:124-125` ("This preserves #67's density").

**Measured, real Chromium, real `board/server` Go binary, real scratch store, 1600×1000 viewport. 18 dispatch rows, NONE carrying a superseded entry** — so the disclosure itself cannot be blamed:

```
===== AS SHIPPED (2861bfd CSS) =====
  distinctRowHeights: [51]
  containerClientH: 176   containerScrollH: 210
  croppedMidRowCount: 3   croppedRows: plain-subject-11/12/13 (top=256, h=51, container bottom=273)
  fullyVisibleRows: 15 of 18
===== SAME PAINT, flex-wrap reverted to the base value (nowrap) =====
  distinctRowHeights: [22]
  containerClientH: 96    containerScrollH: 96
  croppedMidRowCount: 0
  fullyVisibleRows: 18 of 18
```

The counterfactual is exact: `flex-wrap` is the **only** property this diff adds to `.solari-row`, and I reverted it at runtime with `page.addStyleTag` in the same paint — no file edit.

Per-child geometry, same probe (3 rows with a superseded entry, 3 without):

```
subject "no-sup-0"  rowH 73  hasDisclosure false  nonDisclosureVisualLines 2
  .solari-subject  top=99  left=292  ..  .solari-state  top=99  left=377
  .solari-elapsed  top=138 left=292   &lt;- SECOND flex line
```

`.solari-elapsed` drops to its own line on rows that have no disclosure at all.

**The falsehood is contradicted by the comment ten lines below it in the same rule.** `board.css:628-635`, pre-existing and untouched by this diff:

&gt; *"One line per row (mockup convention, every variant): a long subject id truncates with an ellipsis rather than wrapping to a second line - **wrapping is what made the #62 grid's fixed max-height crop rows mid-line** (found live during this car's own visual verification, docs/screenshots/2026-07-26-register-check-candidates/)."*

The new comment cites that very comment ("see the comment on white-space:nowrap immediately below") as its evidence of safety. `white-space: nowrap` (`board.css:636`) stops **text** wrapping inside a flex item; it does not stop **flex items** wrapping onto new flex lines. Measured above: `.solari-rows`' `max-height: 11rem` (`board.css:610`) with `overflow-y: auto` now crops 3 rows mid-row where the base cropped none.

**On the REAL committed `artifacts/` store in this worktree** (server default store resolution, read-only): 4 dispatch rows, **`distinctRowHeights: [73]` as shipped vs `[22]` with `flex-wrap` reverted** — a 3.3× row-height regression on the live board, on rows that are the densest surface `#62`/`#67` exist to protect.

**Scope is wider than the defect it fixes.** MAJOR-R1-1 affected only rows *with* a superseded entry. This regression affects **every** dispatch row, including the majority with none.

**The car's own committed evidence shows it.** `docs/screenshots/2026-07-26-view-69-71-candidates/superseded-collapsed-r2.png` (captured at this commit) renders the dispatch row as three stacked lines — `view-69-71-superseded-de…` / `returned` / `▸ 1 superseded`. Round 1's `collapsed-before-click.png` (captured at `256d8d5`, same fixture) renders `view-69-71-su…  returned` **on one line**. The regression is visible in the two images side by side, and the README caption reads the round-2 image as a pass.

**Why 208/208 could not see it:** nothing in `board/web/test/` asserts rendered geometry. The new structural tests (`dom-writer.test.js:711-806`) run against `createMiniDocument()`, a DOM shim with no layout engine, and assert child counts and class names only. `browser-conditions-persistence.test.js` never touches the dispatches lane. This is `#40`'s territory: the guard for the property the owner ruled on (density) does not exist.

`.car-chip`'s `flex-wrap: wrap` (`board.css:403`) is **clean** — measured 23px, 1 visual line, identical with and without the override, because chips are content-sized rather than grid-cell-constrained.

## MAJOR-R2-2 — This commit created a NEW stale line-number citation, in the same file, in the same hunk, in the commit whose sibling change was a lecture about exactly this class

`board/web/js/dom-writer.js:137`:

```js
// Ephemeral also matches this file's own render cycle unforced: `renderBoard` clears
// `root.textContent` on every repaint (line 141), so even a native `open`
```

| | `b865cba` | `256d8d5` (round 1) | `2861bfd` (HEAD) |
|---|---|---|---|
| `root.textContent = ''` at | line 100 | line **141** | line **192** |
| what is at line 141 | — | the clear | `// every \`&lt;details&gt;\` on the web with no persistence wired at all.` (this comment's own text) |

The car read `141` at `256d8d5`, wrote it into a comment, and its own +51-line insertion **in the same hunk of the same commit** (`3fa9b21`) pushed the target to 192. The citation was false the instant it was committed. `renderBoard` is at `dom-writer.js:191`; the clear at `:192`.

This is MAJOR-R1-4's class verbatim — *"this commit's own insertion broke a live citation and left it stale"* — reproduced one round later. Aggravating: the same commit series wrote `board/web/js/lanes.js:16-23` specifically to explain the hazard:

&gt; *"cited by SYMBOL, not line, per #69/#71 fix cycle round 2 MAJOR-R1-4 and docs/contracts/gating-matrix.md:49's own already-landed lesson: **a line-number citation into a file this SAME train's commit still edits drifts on every later insertion**, and the #65 gate does not check in-range shifts."*

The words about the lesson are present; the substance moved. **DRIFTED.**

Invisible to the gate by construction: `scripts/tests/CitationResolverPolicy.Tests.ps1:1-15` parses `file:line` coordinates and its own header declares *"NOT CAUGHT: an in-range shifted citation"* — a bare `(line 141)` with no filename is not a coordinate it looks for at all. Suite stayed 222 passed.

The same claim on the doc surface is **correct**: `docs/contracts/state-ledger.md:161-162` says *"`renderBoard` clears `root.textContent` on every repaint (`dom-writer.js`)"* — by symbol, no line. Only the code comment is false.

## MAJOR-R2-3 — The MINOR-R1-2 wire-tightening claim names `ingest.js`'s validator as the enforcement hop; measured, that hop enforces nothing, and the schema's own `$comment` says why

`board/web/js/dom-writer.js:142-148`:

```js
// #69/#71 fix-cycle round 2 (MINOR-R1-2): the wire schema now constrains
// superseded[] items to {kind: string, at: string} both required
// (schema/yard-snapshot.schema.json's $defs.dispatchSupersededItem), but
// this function stays defense-in-depth rather than trusting that
// constraint alone - ingest.js's validator only runs against a real
// browser fetch of /schema/yard-snapshot.schema.json (network-reachable at
// runtime); any other caller of renderBoard ... is NOT gated by that fetch.
```

The contrast asserts that callers going *through* that fetch **are** gated. They are not. `$defs.dispatchesPayload` and `$defs.trainsPayload` — the only two places the new `$ref` lands (`schema/yard-snapshot.schema.json:127`, `:194`) — are **never `$ref`'d from `$defs.lane.data`**. The schema's own `$comment` at `schema/yard-snapshot.schema.json:99` states it outright:

&gt; *"…**these three $defs are not $ref'd here** because a lane's data shape is chosen by which lane it is, not by one closed schema union."*

**Measured with the real vendored validator** (`board/web/vendor/cfworker-json-schema/index.js`, draft 2020-12, the exact object `validate.js:19` constructs), against the real schema file at `2861bfd`, on a full snapshot document:

```
GOOD item                -&gt; valid=true   errors=0
kind:7 (wrong type)      -&gt; valid=true   errors=0     &lt;-- NOT rejected
missing at               -&gt; valid=true   errors=0     &lt;-- NOT rejected
empty array              -&gt; valid=true   errors=0
superseded omitted       -&gt; valid=true   errors=0
```

`ingest.js:29`'s `validator.validate(payload)` applies **no** constraint to `superseded[]` items, before or after this diff. Only `scripts/tests/WirePayload.Tests.ps1`'s fragment extraction ever exercises the new `$def`.

This is the class the repo already corrected one file over, in this same train's `#67` cycle — `board/web/js/render.js:391-396`: *"CORRECTED (#67 fix cycle round 2 MINOR-R1-1): the WIRE schema itself does NOT enumerate this set … so the closed-set claim belongs to board/fold alone, never to the schema."* Same overclaim, same schema, same file family, one ticket later.

The renderer guard itself is **correct and non-vacuous** (see the ID table); the defect is the permanent comment asserting a wire-level protection that does not exist, on the exact hop the sentence check traverses.

---

# MINOR FINDINGS

**MINOR-R2-1 — the owner-ruled "COLLAPSED by default" has zero regression pin.** `grep` across `board/web/test/` finds no assertion on the superseded disclosure's `open` attribute in any form (`hasAttribute('open')` / `getAttribute('open')` appear nowhere near the superseded tests; the only hit on `.solari-superseded-disclosure` is `dom-writer.test.js:895`, a `length === 0` check for the all-malformed case). `renderSupersededDisclosure` never sets `open`, and I observed `anyDisclosureOpenByDefault: false` live on the real store — so it is correct today. But a future edit adding `details.setAttribute('open','')` leaves 208/208 green. `board.css:376-377` asserts *"COLLAPSED by default (no `open` attribute is ever set…)"* by reading only. `#40`: unwatched.

**MINOR-R2-2 — the structural-containment witness recorded in the screenshots README is a tautology.** `docs/screenshots/2026-07-26-view-69-71-candidates/README.md:45-46`: *"Structural containment (`disclosure.closest('.solari-row').contains(disclosure)`) was observed `true` live in the browser at capture time."* `Element.closest()` returns an ancestor-or-self, and `contains()` is true for any ancestor — the expression can only evaluate `true` or throw on `null`. It cannot distinguish the fixed code from round 1's sibling layout in any way that matters (under round 1's code `closest` would have returned `null` and thrown, so it is not *entirely* vacuous — but "it threw vs it didn't" is not the containment claim being made). The underlying claim is true; I verified it independently (`disclosureClosestRowIsThisRow: true` and `disclosureIsDescendant: true` for all 13 disclosures). The landed guards in `dom-writer.test.js:747-753` and `:790-796` are properly falsifiable; only the README's recorded witness is not.

---

# FINDING-ID TABLE

| ID | Ruling | Evidence |
|---|---|---|
| **MAJOR-R1-1** (superseded orphans; #67 density) | **Present** — both halves fixed, verified live — **but the fix created MAJOR-R2-1** | Structural: real Chromium, 7 `.solari-row` + 6 `.car-chip` each with a superseded entry (round 1's exact falsifying recipe) — `.solari-rows` direct children = 7, **all** `solari-row`; `.track-cars` direct children = 6, **all** `car-chip`; all 13 disclosures `disclosureIsDescendant: true`, `closest(...) === owner: true`, and `sameVisualBand: true` (each disclosure rect fully inside its owner rect). Real `artifacts/` store: `allDirectChildrenAreRows: true`, `allCarsDirectChildrenAreChips: true`, 10 chips / 10 disclosures. Presentational: `openAttr: false` on **all 13**; summary text `"1 superseded"`, computed `color rgba(217,213,201,0.55)` **equal** to a live `var(--text-dim)` probe and **dimmer** than `.register-nominal`'s `rgb(217,213,201)`; `font-size 11.2px`, `cursor pointer`. Collapsed the entry occupies no flow space (row `h=73` → `87` on expand, exactly the entry's `14px`). Fidelity to the owner ruling read verbatim from issue #69 (`gh issue view 69 --comments`): child-not-sibling ✓, `&lt;details&gt;` idiom ✓, collapsed by default ✓. |
| **MAJOR-R1-2** (no regression pin for `app.js:148`) | **Present** — fault-injected, guard fires | Removed `root.addEventListener('toggle', handleConditionToggle, true)` (sha before/after restore both `9BBCB03A2FC5008EE259E00A057841555FF44C3815091F8AC9D48735A32AB843`, matching round 1's recorded value). `node --test test/browser-conditions-persistence.test.js` → `# tests 2 / # pass 0 / # fail 2`, with the intended messages: *"REGRESSION (#69 MAJOR-R1-2): the strip closed across a real DOM rebuild…"* and *"REGRESSION (#69, the owner's own live-board finding): the strip closed on page reload…"*. Real Chromium + real Go binary + real scratch store, real `page.click`, real `page.reload()`. |
| **MAJOR-R1-3** (false unreachability claim) | **Present** — comment corrected, branch pinned, non-vacuous | `assemble.go:186-202` now states the correction and names the test. `TestAssembleDispatchRenderFailedIsReachable` (`assemble_test.go:720-793`) runs the real `fold.Fold`, mutates the exported `Spend` to `chan int`, and asserts through the real `Assemble()`. Fault-injected `RecordDir: recordDirs[d.Subject]` → `RecordDir: ""` (sha `852870A2…`, restored identical): `--- FAIL: TestAssembleDispatchRenderFailedIsReachable … assemble_test.go:778: RecordDir = "", want "probe-subj"`. |
| **MAJOR-R1-4** (stale `snapshot.go:41-45` citation) | **Present** for the named citation — **class REPEATED as MAJOR-R2-2** | `lanes.js:16-21` now cites the `Lane` struct's doc comment by symbol. Verified at `board/server/snapshot.go:44-49`: the `Lane` doc comment contains verbatim *"so absence of the key itself is what signals \"no payload\" on the wire."* The quoted text in `lanes.js` matches. |
| **MINOR-R1-1** (Law 6 duplicate) | **Present** | `store.RecordDirFromRelPath` exported (`store.go:343`); `assemble.go:347` calls it; the `path/filepath` import removed from `assemble.go` (only a prose mention of `filepath.Dir` survives at `:330`). `grep` finds exactly one implementation and four call sites. |
| **MINOR-R1-2** (unconstrained wire, unguarded read) | **Present in the renderer** (guard real and tested) — **DRIFTED in its justification → MAJOR-R2-3** | `renderSupersededDisclosure` filters on `typeof item?.kind === 'string' &amp;&amp; typeof item?.at === 'string'`; `dom-writer.test.js:849-895` pins both the skip-one and skip-all cases including an explicit `!includes('undefined')`. Schema `$def` added and **enforced in the one place it is applied**: I re-derived the WirePayload helper both ways — OLD helper `trainsPayload`/`dispatchesPayload` good samples → `False` (the two reds the car reports, and they red for the stated `$ref`-resolution reason, while `gatesPayload` (no inner `$ref`) → `True`); NEW helper → `True`; and **non-vacuously** `superseded: [{kind: 7}]` → `False`, `[{kind only}]` → `False`. Helper change is sound; it also removes a latent mutation of the shared `$defs` hashtable. Producer hop verified: `fold.go:85-88` `DispatchSupersededItem{Kind string \`json:"kind"\`; At string \`json:"at"\`}`, non-pointer, populated at `algorithm.go:196-200`, always both keys via `output.go:9-18` — the `$def` cannot reject a real emission. |
| **MINOR-R1-3** (untested degrade paths) | **Present** — fault-injected, both fire | Removed `loadOpenState`'s `try/catch` and neutered `saveOpenState`'s (sha `0C0BE511…`, restored identical): `not ok 10 - loadOpenState: a THROWING getItem …` and `not ok 11 - saveOpenState: a THROWING setItem …`, `# tests 12 / # pass 10 / # fail 2`. |

---

# THE WIRE SENTENCE — `$defs.dispatchSupersededItem`, every hop, file:line

1. **Producer type** — `board/fold/fold.go:85-88`: `DispatchSupersededItem{ Kind string \`json:"kind"\`; At string \`json:"at"\` }`. Non-pointer strings, no `omitempty` → both keys always marshal.
2. **Producer population** — `board/fold/algorithm.go:196-201`: `superseded := []DispatchSupersededItem{}`; for each loser, `append(superseded, DispatchSupersededItem{Kind: k, At: at})`. `k`/`at` come from `rec.str(...)`, which yields `""` rather than absence — so a degenerate emission is an empty *string*, never a missing or non-string key. The `$def`'s `required` can therefore never reject a real emission; the schema's own description at `:70` says exactly this and is **accurate**.
3. **Marshal** — `board/fold/output.go:9-18`: `"superseded"` is one of the four unconditional keys; `nil` normalised to `[]`.
4. **Assemble → wire** — `board/assemble/assemble.go:187` `dispatchWireMap(d, …)`, `map[string]any` from `json.Marshal(d)`; carried on `assemble.DispatchesPayload` / `TrainsPayload`.
5. **Schema constraint** — `schema/yard-snapshot.schema.json:69-76` `$defs.dispatchSupersededItem`, `$ref`'d at `:127` (trains cars) and `:194` (dispatches). Correctly **not** applied to `IntentSupersededItem` (which has no `Kind`) — no intent `superseded` exists in this schema.
6. **Client validation — THE BREAK.** `board/web/js/ingest.js:29` → `validate.js:19` `new Validator(schema, '2020-12')`. `$defs.lane.data` (`schema:96-101`) is an unconstrained free object and the payload `$defs` are **not `$ref`'d** from it (`schema:99`, declared). **Measured: `superseded: [{kind: 7}]` validates `valid=true, errors=0`.** The comment at `dom-writer.js:142-148` names this hop as the enforcement locus → **MAJOR-R2-3**.
7. **View model** — `board/web/js/render.js:312` (`superseded: c.superseded || []`) and `:370` (`superseded: d.superseded || []`). Pass-through, no filtering.
8. **Guarded read** — `board/web/js/dom-writer.js:150-152`: `superseded.filter((item) =&gt; typeof item?.kind === 'string' &amp;&amp; typeof item?.at === 'string')`; empty → `return null` (no empty `&lt;details&gt;`). This is where the constraint actually bites at runtime.
9. **DOM** — `dom-writer.js:153-161`: `&lt;details class="…-disclosure"&gt;&lt;summary class="…-summary"&gt;N superseded&lt;/summary&gt;&lt;div class="…"&gt;&lt;span|a class="…-entry"&gt;kind at&lt;/span&gt;&lt;/div&gt;&lt;/details&gt;`, appended to the **row** (`:534`) / **chip** (`:451`).
10. **Hand-maintained mirrors** — `scripts/tests/WirePayload.Tests.ps1:48-51` (the extraction helper, modified this round; verified sound and non-vacuous above). No wire sample or snapshot fixture carries a `superseded` item that the `$def` would newly reject — the WirePayload samples at `:66`, `:141` are the only ones and both pass.
11. **Computed style, expanded, real browser** — entry `tagName "SPAN"`, `href null` (no GitHub env in this probe — the degrade-honest branch), `color rgba(217,213,201,0.55)` = `var(--text-dim)`, `text-decoration-line none`, `border-bottom 0px none`, `entryStillInsideOwnRow: true`, `entryWithinRowBox: true`, text `dispatched 2026-07-20T08:00:00Z` rendered VERBATIM.
12. **Cross-surface isolation, measured** — clicking a superseded `&lt;summary&gt;` writes **nothing** to `#69`'s storage: `sessionStorage` before `null`, after `{key: null, len: 0, allKeys: []}`. `app.js:47-59`'s `handleConditionToggle` returns early on any `&lt;details&gt;` carrying neither `data-conditions-strip` nor `data-condition-code`. No leak.

---

# THE M1-EPHEMERALITY ADJUDICATION: **PERMITTED**

Ruled explicitly, against each binding source, read first-hand rather than from the brief.

**Issue #69's own text** (`gh issue view 69 --json body`) scopes persistence to one surface: *"the strip is a native details element whose open state lives only in the DOM… Fix: preserve the open attribute across DOM rebuilds…, and **persist across page refreshes (sessionStorage keyed to the strip)**. An operator who opened the flags wants them to STAY open until dismissed - **the alarm detail** is the thing they were reading."* The owner-reported defect is the conditions strip; the rationale is alarm-reading. The superseded disclosure is a different surface that did not exist at filing, and it carries no alarm. **No contradiction.**

**The owner's M1 ruling**, verbatim from issue #69's comment (2026-07-27): *"superseded-record history re-attaches INSIDE its owner row/chip … and renders behind a quiet per-row disclosure, **COLLAPSED by default** - the same details idiom the conditions strip uses. Rationale: matches the #68 principle (not hidden, just not surfaced) and preserves the #67 density the reviewer measured as regressed. Options considered and declined: always-visible nesting (reintroduces the density regression), per-row cap a la #67."* Both stated rationales and both declined options are about **density and surfacing** — never persistence. The ruling names no persistence requirement. **No contradiction.**

I note one ambiguity for the record: *"the same details idiom the conditions strip uses"* is readable as chrome-only or chrome-plus-`sessionStorage`. Under this shop's own rule that a requirement readable two ways will be read both ways, that phrase is a latent hazard — but the brief authorised the car to take the minimal option and flag it, and it flagged it in three places (`dom-writer.js:126-141`, `design.md:783-792`, `state-ledger.md:155-166`). Choosing-and-disclosing is what it was told to do; this is not a finding.

**The state ledger's contracts.** `docs/contracts/state-ledger.md:155-166` declares the disclosure explicitly NOT a ledger field, with reasoning. I verified every claim in that paragraph: no `sessionStorage` key (`grep` + measured: `sessionStorage.length === 0` after a real click), no JS variable, and `renderBoard` does clear `root.textContent` (`dom-writer.js:192` — cited by symbol here, correctly). CLAUDE.md's living-contracts rule requires the ledger be updated in the same commit; it was, honestly, as a declared non-row. **No contradiction.**

**Ruling: PERMITTED.** The car's stated reason is also factually correct — the DOM is rebuilt wholesale, so persistence would require new capture-and-reapply plumbing, not reuse of `#69`'s. The one thing missing is not persistence but a **pin on the collapsed default** (MINOR-R2-1).

---

# CONSTITUTION CHECK

- **Law 1 (never a confident falsehood).** Round 1's two violations are **GONE**: `assemble.go:186-202`'s impossibility claim is corrected and the correction is proven by a passing, fault-injectable test; and no provenance fact renders adjacent to a subject that does not own it (13/13 disclosures measured inside their owner's box on synthetic and real stores). **But VIOLATED three times anew**: MAJOR-R2-1 (`board.css:619`, `:383-384`, `:380`, `dom-writer.js:124-125` — density/one-line claims measured false), MAJOR-R2-2 (`dom-writer.js:137` — a line number false at the moment of commit), MAJOR-R2-3 (`dom-writer.js:142-148` — a wire protection that does not exist).
- **Law 6 (no second copies).** Honored — MINOR-R1-1 closed; one `RecordDirFromRelPath`, four call sites, `filepath` import dropped from `assemble.go`.
- **Law 7 (the stranger).** Honored — no hardcoded taxonomy added; the malformed-item guard degrades to absence rather than inventing a value; `$defs.lane.data` stays open so an unrecognised lane is still a discovery.
- **Law 2 / #68 (not hidden, just not surfaced).** Honored — the entries remain in the DOM behind a labelled `"1 superseded"` affordance, never dropped; count is stated in the summary.
- **TDD / red-first.** Honored for every fix I could inject: MAJOR-R1-2 (2 red), MAJOR-R1-3 (1 red), MINOR-R1-3 (2 red), MINOR-R1-2's helper (2 pre-fix red, re-derived). **Absent for the CSS change** — no test in the repo can observe rendered geometry, which is how MAJOR-R2-1 shipped green.
- **#40 (a guard is unproven until someone watched it fire).** Partly honored, partly VIOLATED. The MAJOR-R1-2 browser guard is real and I watched it fire. But the visual evidence for MAJOR-R1-1's fix was again produced without a falsifying measurement: the round-2 screenshots *contain* the new regression and were captioned as a pass, and the README's containment witness is a tautology (MINOR-R2-2).
- **NORTH STAR (documents rank equal to code; same-commit truing).** VIOLATED — MAJOR-R2-2 creates a stale citation in the commit that creates it; MAJOR-R2-1's false density claim lives on three surfaces (`board.css`, `dom-writer.js`, and by implication the screenshots README caption). Honored elsewhere: `state-ledger.md`, `design.md` §12b, and the screenshots README's staleness disclosure for the four round-1 PNGs are all accurate and same-commit.
- **Code-citation standard (#42, bare `#N`).** Honored — `browser-conditions-persistence.test.js` carries 8 `#69`/`#71` markers; the two PNGs cannot carry comments and are cited from the sibling `README.md` (which carries `#69`×3, `#71`×2 and a `Status:` line, satisfying `DocPolicy`), exactly as the standard's fixture clause prescribes.
- **Verification honesty.** Honored by the car — every count I re-derived reconciles, and the one disclosed decision was genuinely disclosed. Disclosed-but-wrong does not clear review, and MAJOR-R2-3 is the disclosed-but-wrong case.

---

# BASE-DELTA (#13)

- **Round 1 reviewed base:** `256d8d5` (train base `b865cba91cf4ee192a3ab7c742c48424c508268e`).
- **This round reviewed:** `2861bfd8a99d8213ca0058a999ab91706f4ccc9b`, detached, clean.
- **`git diff 256d8d5..2861bfd --stat`:** 17 files changed, **+698 / −44**.

```
 board/assemble/assemble.go                                    |  37 ++--
 board/assemble/assemble_test.go                               |  76 ++++++++
 board/store/store.go                                          |  29 ++--
 board/store/store_test.go                                     |   4 +-
 board/web/css/board.css                                       |  37 ++++
 board/web/js/dom-writer.js                                    |  71 +++++++-
 board/web/js/lanes.js                                         |  13 +-
 board/web/test/browser-conditions-persistence.test.js         | 136 +++++++++++++++
 board/web/test/condition-open-state.test.js                   |  29 ++++
 board/web/test/dom-writer.test.js                             | 191 +++++++++++++++++++++
 docs/contracts/state-ledger.md                                |  13 ++
 docs/design/2026-07-21-v0-yard-skeleton-design.md             |  26 ++-
 docs/screenshots/2026-07-26-view-69-71-candidates/README.md    |  49 ++++++
 docs/screenshots/.../superseded-collapsed-r2.png              | Bin 0 -&gt; 49056
 docs/screenshots/.../superseded-expanded-r2.png               | Bin 0 -&gt; 51108
 schema/yard-snapshot.schema.json                              |  13 +-
 scripts/tests/WirePayload.Tests.ps1                           |  18 +-
```

**Visible delta for a reader of the live board, measured not inferred:** (1) superseded history no longer renders as loose text blocks — each row/chip now carries a collapsed `"N superseded"` affordance inside its own box, which cannot orphan; (2) **every dispatch row is now two visual lines tall instead of one — 22px → 51-73px depending on column width, 4/4 rows on the real store — and `.solari-rows` crops rows mid-row once more than ~10 dispatches exist** (MAJOR-R2-1); (3) no change to the conditions strip, the links, or any wire value a client observes.

**Five fix commits reviewed:** `3fa9b21` (MAJOR-R1-1 nesting + MINOR-R1-2 guard — **the commit that introduces MAJOR-R2-1 and MAJOR-R2-2**), `97c3dc6` (MAJOR-R1-2 browser pin), `ca20830` (MAJOR-R1-3 + MINOR-R1-1), `d9ffcfe` (MAJOR-R1-4), `2861bfd` (MINOR-R1-3).

---

# CONVERGENCE RULING — **the swirl trigger FIRES (two of three conditions), cap set at ONE more round**

| Condition | Met? | Evidence |
|---|---|---|
| Major counts stop declining | **No** | 4 → 3 Major; 3 → 2 Minor. Genuinely declining. |
| Findings cluster in the same section across rounds | **Yes** | Round 1: 1 of 4 Majors in the superseded-rendering surface. Round 2: **3 of 3** — `board.css`'s `.solari-row`/`.car-superseded-disclosure` rules and `dom-writer.js`'s `renderSupersededDisclosure` comment block. |
| A round's findings include defects the previous round's fixes CREATED | **Yes — 2 of 3 Majors** | MAJOR-R2-1 and MAJOR-R2-2 are both created by `3fa9b21`, the MAJOR-R1-1 fix. Neither existed at `256d8d5`. |

Per CLAUDE.md's calibration, two of three fires the escalation. **I set the cap at ONE further fix round.** If a round-3 delta review finds any Major in the superseded-rendering surface again, escalate to the owner rather than dispatching round 4. The conductor honours the cap.

**But I do not think this is a genuine swirl, and the more useful ruling is about the instrument, not the count.** Both rounds inherited the same unquestioned premise: *that a CSS change's rendered effect can be verified by reading the CSS plus looking at one screenshot.* Round 1's MAJOR-R1-1 existed because a one-subject fixture could not falsify the layout claim. Round 2's MAJOR-R2-1 exists because the screenshot that **shows** the regression was read as a pass. Same premise, opposite manifestation, twice — and no gate in the repo can catch either, because nothing under `board/web/test/` observes layout (`createMiniDocument()` has no layout engine; the two real-browser tests never touch the dispatches lane).

Recommended remedy, and it is a probe-doctrine landing rather than more review: **a real-browser test that asserts rendered geometry** — for a dispatch row with no superseded entry, the count of distinct child `top` values must be 1, and `.solari-rows` must have zero rows crossing its own `clientHeight` boundary. That reds by name the instant the "one line per row" mockup convention breaks, which is the property `#62`, `#67` and this ruling all depend on and which three consecutive commits have now been unable to protect by reading. My probe scripts (paths below) are the working recipe.

The fix cycle should go to the **same car** with a delta brief carrying these IDs — MAJOR-R2-1/2/3 are mechanical (a CSS approach that does not set `flex-wrap` on `.solari-row`; two comment corrections), and the car holds the unwritten context.

---

# UNREPRODUCIBLE-EVIDENCE CALLOUT (#13)

**None.** Every finding rests on re-runnable evidence: three fault injections with before/after `sha256`, four real-browser probes against a real `board/server` binary (three scratch stores plus the repo's own committed `artifacts/` store), a runtime-only `addStyleTag` counterfactual that isolates the single changed CSS property in the same paint, a Node probe against the real vendored validator, a PowerShell probe reproducing both the old and new `WirePayload` helper bodies, `git show` at two base commits, and `gh issue view 69 --comments` for the owner ruling. The two committed PNGs are in the repo and can be opened by anyone.

**Byte-clean exit.** `git status --porcelain` empty; HEAD `2861bfd8a99d8213ca0058a999ab91706f4ccc9b`. Files injected and restored with **matching** `sha256`: `board/web/js/app.js` `9BBCB03A2FC5008EE259E00A057841555FF44C3815091F8AC9D48735A32AB843` (identical to round 1's recorded value), `board/assemble/assemble.go` `852870A2D219AB67C0B2705E847656E650DBFF93C78EA0069AAA746DE60CAAEB`, `board/web/js/condition-open-state.js` `0C0BE5115BE95C2100EBF10FA30F8C501483F11BCDBE6D14167308F42BAB03C5`. Three `.scratch-store-69-71-*` directories created under the worktree root (gitignored) and removed; zero remain. Nothing committed, nothing pushed. The shared checkout at `C:\Users\Chris\git\starcar` was touched only by read-only reads of `artifacts/reviews/2026-07-27-view-69-71-review-r1-REJECT.md` and two `gh` API reads.

**Reviewer probe scripts** (scratchpad, re-runnable):
- `C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\scratchpad\browser-probe.mjs` — multi-subject nesting/geometry/computed-style, collapsed and expanded
- `...\scratchpad\browser-probe2.mjs` — per-child geometry, with/without superseded, flex-wrap counterfactual
- `...\scratchpad\browser-probe3.mjs` — 18 rows, crop-mid-row measurement (MAJOR-R2-1's decisive evidence)
- `...\scratchpad\browser-probe4.mjs` — real `artifacts/` store + `sessionStorage` isolation
- `...\scratchpad\client-ref-probe.mjs` — client validator `$ref` enforcement (MAJOR-R2-3)
- `...\scratchpad\wirehelper-probe.ps1` — old vs new `WirePayload` helper, both directions

---

```starcar-artifact
outcome: REJECT
findings: 3 Major, 2 Minor
abstract: |
  Delta re-review of 2861bfd (view train #69+#71 fix cycle round 2, five commits
  3fa9b21/97c3dc6/ca20830/d9ffcfe/2861bfd over round 1's base 256d8d5). REJECT.

  Suites re-derived independently at HEAD: Node board/web 208 tests / 208 pass / 0 fail,
  Go board 94 top-level PASS with -count=1 -v and 0 FAIL, go vet exit 0 no output, Pester
  scripts/tests 227 total / 222 passed / 0 failed / 5 skipped (the 5th skip named as the
  detached-HEAD run-session-start-guards.sh ci-baseline branch guard, identical to round
  1's identification), Pester scripts/probes 24/24, probe-yard-snapshot.mjs PROBE RESULT
  PASS. Every count the car claimed reconciles.

  ALL SEVEN ROUND-1 FINDINGS RULED PRESENT. MAJOR-R1-1 fixed in both halves and verified
  in a real browser against round 1's own falsifying recipe: with 7 dispatch rows and 6
  car chips each carrying a superseded entry, .solari-rows direct children = 7 all
  solari-row and .track-cars direct children = 6 all car-chip, all 13 disclosures
  descendants of their own owner with sameVisualBand true, and all 13 collapsed by default
  with summary text "1 superseded" in computed color rgba(217,213,201,0.55) equal to a
  live var(--text-dim) probe. Same structural result on the repo's real artifacts store.
  MAJOR-R1-2 fault-injected: removing app.js's addEventListener toggle line turns the new
  browser test file RED 2 of 2 with its intended regression messages, restored sha256
  9BBCB03A identical. MAJOR-R1-3 fault-injected: blanking RecordDir reds
  TestAssembleDispatchRenderFailedIsReachable at assemble_test.go 778. MINOR-R1-3
  fault-injected: neutering both catch branches reds tests 10 and 11. MINOR-R1-1 single
  sourced, filepath import dropped. MINOR-R1-2's renderer guard real and pinned; the
  WirePayload helper change re-derived both ways, old form returns False for the two good
  samples for the stated ref-resolution reason and new form returns False for a malformed
  superseded item, so it is non-vacuous.

  BUT THE FIX CREATED TWO NEW MAJORS AND ONE OVERCLAIM.

  MAJOR-R2-1: commit 3fa9b21 adds flex-wrap wrap to .solari-row (board.css 623, absent at
  both 256d8d5 and b865cba), which wraps EVERY dispatch row onto two visual lines,
  including rows with no superseded entry at all. Measured in real Chromium against the
  real Go server, 18 rows none carrying superseded: as shipped every row is 51px,
  .solari-rows scrollHeight 210 against clientHeight 176, and THREE rows are cropped
  mid-row; with flex-wrap reverted to the base value in the same paint via addStyleTag
  every row is 22px, scrollHeight 96 equals clientHeight 96, zero cropped, 18 of 18 fully
  visible. On the repo's real artifacts store the same counterfactual reads 73px as
  shipped versus 22px at base, a 3.3x row-height regression on 4 of 4 rows. This
  re-introduces exactly the hazard board.css 628-635's own pre-existing comment records as
  already found and fixed, "wrapping is what made the #62 grid's fixed max-height crop
  rows mid-line" - and the new comment at board.css 616-622 cites that very comment as its
  evidence of safety while asserting "the subject/state/elapsed cluster below stays one
  line per row (mockup convention preserved)", which is measured false. white-space nowrap
  stops text wrapping inside a flex item, never flex items wrapping onto new flex lines.
  The car's own committed screenshot superseded-collapsed-r2.png shows the row as three
  stacked lines where round 1's collapsed-before-click.png at 256d8d5 shows subject and
  state on one line; the regression is visible in the two images and the README captions
  the round-2 image as a pass. 208/208 stayed green because nothing under board/web/test
  observes layout: the new structural tests run on createMiniDocument, a shim with no
  layout engine. .car-chip's flex-wrap is clean, measured 23px one line either way.

  MAJOR-R2-2: dom-writer.js line 137 cites "(line 141)" for renderBoard's
  root.textContent clear. That line was 141 at 256d8d5 and the same commit's own +51-line
  comment insertion pushed it to 192, so the citation was false at the instant it was
  committed; line 141 at HEAD is the comment's own text. This is MAJOR-R1-4's class
  reproduced one round later, in the same file, in the same hunk as the fix - while the
  same commit series wrote lanes.js 16-23 specifically to explain that a line-number
  citation into a file this same train's commit still edits drifts on every insertion.
  DRIFTED. Invisible to the #65 gate, whose own header declares in-range shifts are not
  caught and which only parses file:line coordinates, not a bare "(line N)".

  MAJOR-R2-3: dom-writer.js 142-148 states the wire schema now constrains superseded items
  and names ingest.js's validator as the hop it is being defended against relying on
  alone, which asserts that callers going through that fetch ARE gated. Measured with the
  real vendored cfworker validator against the real schema on a full snapshot document:
  superseded [{kind: 7}] validates valid=true errors=0, and so does a missing "at". The
  payload $defs are never $ref'd from $defs.lane.data and the schema's own $comment at
  line 99 says so explicitly. The only consumer of the new $def is WirePayload.Tests.ps1's
  fragment extraction. Same class the repo already corrected one file over in render.js
  391-396 during the #67 fix cycle.

  MINOR-R2-1: the owner-ruled "COLLAPSED by default" has zero regression pin - no test
  anywhere asserts the disclosure's open attribute, so adding one would leave 208/208
  green. MINOR-R2-2: the screenshots README's containment witness,
  disclosure.closest('.solari-row').contains(disclosure), is a tautology since closest
  returns an ancestor-or-self; the landed dom-writer tests are properly falsifiable, only
  the recorded witness is not.

  M1 EPHEMERALITY ADJUDICATED: PERMITTED. Issue #69's body, read first-hand, scopes
  persistence to the conditions strip ("persist across page refreshes, sessionStorage
  keyed to the strip", rationale "the alarm detail is the thing they were reading"). The
  owner's M1 ruling, read verbatim from the issue comment, says COLLAPSED by default and
  names no persistence; both its rationales (#68 not-hidden-just-not-surfaced, #67
  density) and both its declined options (always-visible nesting, per-row cap) are about
  surfacing and density. The state ledger's new paragraph declares it a non-row with
  reasoning and every claim in it verified, including measured sessionStorage length 0
  after a real click on a superseded summary and app.js 47-59's explicit early return for
  any details carrying neither data-conditions-strip nor data-condition-code. The phrase
  "the same details idiom the conditions strip uses" is readable two ways, noted for the
  record, but choosing-and-disclosing is what the brief authorised and the car disclosed
  it in three places.

  WIRE SENTENCE traced hop by hop with file:line, 12 hops, producer to DOM, with the break
  named at hop 6.

  CONSTITUTION: Law 1's two round-1 violations are gone and three new ones replace them
  (MAJOR-R2-1/2/3). Law 6 honored (MINOR-R1-1 closed). Law 7 honored. Law 2 / #68 honored.
  TDD honored for every injectable fix and absent for the CSS change. #40 partly violated -
  the browser guard is real and watched firing, but the visual evidence for the layout fix
  again came from a fixture and a caption that could not falsify it. NORTH STAR violated by
  MAJOR-R2-2 and MAJOR-R2-1's three-surface false claim; honored by state-ledger.md,
  design.md 12b and the README's staleness disclosure for the four round-1 PNGs. Code
  citation standard #42 honored, including the fixture clause for the two PNGs.

  CONVERGENCE: the swirl trigger FIRES on two of three conditions. Counts ARE declining
  (4 Major to 3, 3 Minor to 2), so that condition is not met, but findings cluster in the
  same section across rounds (1 of 4 round-1 Majors in the superseded-rendering surface
  versus 3 of 3 this round) AND two of this round's three Majors are defects the round-1
  fix commit 3fa9b21 CREATED. Cap set at ONE further round; a Major in that same surface
  in round 3 escalates to the owner. The more useful ruling is about the instrument: both
  rounds inherited the unquestioned premise that a CSS change's rendered effect can be
  verified by reading plus one screenshot, and no gate in the repo can catch either
  manifestation. Recommended landing is a real-browser geometry assertion (distinct child
  top values per row equals 1; zero rows crossing .solari-rows' clientHeight), which reds
  by name when the one-line-per-row convention breaks. Fix cycle to the same car.

  BASE-DELTA 256d8d5 to 2861bfd, 17 files +698/-44. Visible delta measured: superseded
  history no longer orphans and now sits behind a collapsed per-row affordance; every
  dispatch row is now two lines instead of one and .solari-rows crops rows mid-row past
  about ten dispatches; no wire value a client observes changed.

  UNREPRODUCIBLE-EVIDENCE: none. Byte-clean exit, git status --porcelain empty at HEAD
  2861bfd8a99d8213ca0058a999ab91706f4ccc9b. Three files injected and restored with
  matching sha256 (app.js 9BBCB03A, assemble.go 852870A2, condition-open-state.js
  0C0BE511); three gitignored scratch store directories created and removed, zero remain.
  Nothing committed, nothing pushed, shared checkout read only.
task-id: view-69-71-review-r2
```</result>
<usage><subagent_tokens>206051</subagent_tokens><tool_uses>77</tool_uses><duration_ms>1373121</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>b1nb0mv00</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\b1nb0mv00.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI to terminal state for 48ae617 (background)" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>bp7eajbh0</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\bp7eajbh0.output</output-file>
<status>failed</status>
<summary>Background command "Watch CI to terminal state for 81be0f8 (background)" failed with exit code 10</summary>
</task-notification>

---

<task-notification>
<task-id>b0bngyff7</task-id>
<tool-use-id>toolu_018Zun4284ULHXusHm9QiGxE</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\b0bngyff7.output</output-file>
<status>completed</status>
<summary>Background command "Watch re-run to terminal state for 81be0f8 (background)" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>ae3d64acd969fcdf2</task-id>
<tool-use-id>toolu_0113sjwjscFo6N2N8X71TsJ8</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\ae3d64acd969fcdf2.output</output-file>
<status>completed</status>
<summary>Agent "Car r2 fix cycle #69+#71" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Byte-clean exit confirmed. All 5 findings from round-2 review fixed, red-first verified, and committed across 3 logical commits on `car/view-69-71`, HEAD `4b50a5c5513a7b7401188a4b4d41d989aac572a3`, base `2861bfd8a99d8213ca0058a999ab91706f4ccc9b`.

## Per-finding summary

**MAJOR-R2-1** (commit `8073f0b`) — Round 2's `flex-wrap: wrap` on `.solari-row` wrapped the subject/state/elapsed cluster too, not just the disclosure. Fix: `.solari-row` no longer sets `flex-wrap` at all; the cluster moved into its own non-wrapping wrapper `.solari-row-main`, and `.solari-row` became a column-direction flex container stacking `main` then the disclosure by DOM order. New `board/web/test/browser-dispatch-row-geometry.test.js` (real Chromium/Go binary, new `buildScratchStoreForDispatchRowGeometry` fixture) asserts rendered geometry — the class of test that didn't exist before. **Non-vacuity, verified by stashing the fix and rerunning the finalized test against round-2 HEAD**: observed red — `"row 'plain-subject-00' (no disclosure) spans 2 visual lines ... 2 !== 1"` and `"1 plain row(s) crop mid-row at .solari-rows' own boundary ... plain-subject-03 top=160 height=51"` — then restored (sha256 identical) and green (3/3).

**MAJOR-R2-2** (commit `8073f0b`) — `dom-writer.js`'s `"(line 141)"` citation (already false the instant round 2's own +51-line insertion moved the target to 192) converted to symbol form. **Audit performed**: `git diff 256d8d5..2861bfd` scoped to added lines found one more candidate, `condition-open-state.test.js`'s `loadOpenState:44-46, saveOpenState:66-71` — verified accurate (file never touched by any round-2 commit) but converted proactively anyway since it's the same fragile class. Two further candidates (`app.js:148`, `fold.go:112`) verified accurate and left as-is — both in files this train's commits never edit, so they can't self-invalidate.

**MAJOR-R2-3** (commit `8073f0b`) — Corrected the false claim that `ingest.js`'s validator gates `superseded[]` items. Verified empirically with the real vendored `cfworker-json-schema` validator: `superseded: [{kind:7}]` (missing `at`) → `valid=true, errors=0`. Comment now states the truth: the new `$def` is exercised only by `WirePayload.Tests.ps1`'s fragment extraction; `renderSupersededDisclosure`'s own filter is the entire runtime protection. Schema wiring itself untouched (ruled design posture, per `schema/yard-snapshot.schema.json:99`'s own `$comment`).

**MINOR-R2-1** (commit `9b899ab`) — New test pins that neither a car-chip's nor a dispatch-row's disclosure ever carries `open`. Non-vacuity: injected `details.setAttribute('open', '')`, observed red (`"a superseded disclosure carries an 'open' attribute ({\"open\":\"\"})"`), restored byte-identical, green (47/47).

**MINOR-R2-2** (commit `4b50a5c`) — Replaced the tautological `closest(...).contains(...)` witness with the falsifiable form (`closest(...) === ownRow` plus descendant and bounding-box checks, all measured `true` live) and pointed at `dom-writer.test.js`'s structural tests as the re-runnable proof. Recaptured screenshots using the new multi-row fixture at a narrow single-column viewport (the old single-subject fixture can't visually exhibit MAJOR-R2-1 at all — that row has no `elapsedSeconds`, so nothing to wrap under either version, the same "fixture can't falsify the claim" class rounds 1 and 2 were each caught on). **Honest disclosure, not fixed**: recapturing at a realistic desktop width surfaced a *different*, pre-existing artifact — some plain rows visually stretch taller due to CSS Grid's default `align-items: stretch` across shared row bands with taller disclosure rows. This is not MAJOR-R2-1's failure mode (content still renders one line), predates this round's fix, and is outside the five named findings — disclosed in the README rather than fixed, respecting the round's swirl cap rather than risking a new, unscoped defect.

## Final suite totals at HEAD `4b50a5c`

- Go `board/...`: **94 PASS / 0 FAIL** (unchanged — no Go files touched this round), `go vet ./...` clean.
- Node `board/web`: **212 tests / 212 pass / 0 fail** (round-2 final 208 + 4 new: 3 geometry + 1 collapsed-by-default pin).
- Pester `scripts/tests`: **223 passed / 0 failed / 4 skipped / 227 total** (matches round-2 final exactly).
- Pester `scripts/probes`: **24 passed / 0 failed**.
- `probe-yard-snapshot.mjs`: **PASS**.

Byte-clean exit: `git status --porcelain` empty, no scratch-store debris under the repo root.

## Deviations / disclosures

- The geometry test's fixture row count (7 plain rows) was deliberately chosen — not arbitrary — so that "does a plain row cross the container boundary" stays a decidable question rather than a coincidence of row-height divisibility (documented in the fixture's own comment).
- The CSS-grid row-band-stretch observation (MINOR-R2-2 section above) is a genuine finding of my own, disclosed but explicitly not fixed this round.

## Files touched (all under `C:\Users\Chris\git\starcar-wt\car-view-69-71`)

`board/web/css/board.css`, `board/web/js/dom-writer.js`, `board/web/test/dom-writer.test.js`, `board/web/test/condition-open-state.test.js`, `board/web/test/support/real-board-server.js`, `board/web/test/browser-dispatch-row-geometry.test.js` (new), `docs/screenshots/2026-07-26-view-69-71-candidates/README.md`, `dispatch-rows-multirow-collapsed-r3.png` (new), `dispatch-rows-multirow-expanded-r3.png` (new).

```starcar-artifact
outcome: done
findings: 0 Major, 0 Minor (all 5 round-2 findings fixed and verified; 1 disclosed out-of-scope observation for the record, not fixed, per the round's swirl cap)
abstract: |
  Fix cycle round 3 on view train #69+#71, all 5 findings from
  artifacts/reviews/2026-07-27-view-69-71-review-r2-REJECT.md addressed,
  red-first, on car/view-69-71 base 2861bfd through HEAD 4b50a5c, 3 commits
  (8073f0b, 9b899ab, 4b50a5c), working tree clean.

  MAJOR-R2-1: .solari-row no longer sets flex-wrap (round 2's fix wrapped
  the subject/state/elapsed cluster too, not just the disclosure, doubling
  every row's height and re-cropping .solari-rows mid-row). The cluster now
  lives in its own non-wrapping wrapper (.solari-row-main); .solari-row is
  a column-direction flex container stacking main then the disclosure by
  DOM order, needing no wrap. New browser-dispatch-row-geometry.test.js
  asserts rendered geometry via real Chromium/real Go binary - verified
  non-vacuous by stashing the fix, rerunning the finalized test against
  round-2 HEAD, observing red for the stated reason (2 visual lines instead
  of 1; a plain row cropping mid-row), then restoring (sha256 identical)
  and green (3/3).

  MAJOR-R2-2: the stale "(line 141)" citation (false the instant round 2's
  own insertion moved the target) converted to symbol form. Audited every
  other bare line-number reference the round-2 commits added: found and
  fixed one more (condition-open-state.test.js), verified two others
  accurate and left alone (both in files this train never edits).

  MAJOR-R2-3: corrected the false claim that ingest.js's validator gates
  superseded[] items - measured with the real vendored validator that a
  malformed item validates clean, because $defs.lane.data is never $ref'd
  to the payload defs (the schema's own $comment already explains this
  design posture, untouched). The renderer's own filter is the entire
  runtime protection.

  MINOR-R2-1: new test pins the owner-ruled collapsed-by-default posture -
  non-vacuity proven by injecting an open attribute, observing red, then
  restoring byte-identical.

  MINOR-R2-2: replaced the tautological closest().contains() witness with
  a falsifiable form (closest(...) === ownRow, descendant check, bounding-
  box containment), all measured true live, and pointed at dom-writer.
  test.js's structural tests as the re-runnable proof. Recaptured
  screenshots using a multi-row fixture (the old single-subject fixture
  cannot visually exhibit MAJOR-R2-1 at all - disclosed). Also disclosed,
  found incidentally: a pre-existing CSS-grid row-band-stretch cosmetic
  artifact at wide viewports, out of scope for this round's five named
  findings, not fixed, flagged for the record given the round's swirl cap.

  Final suite totals at HEAD 4b50a5c: Go board/... 94 PASS / 0 FAIL
  (unchanged, no Go files touched), go vet clean; Node board/web 212 tests
  / 212 pass / 0 fail (round-2 final 208 + 4 new); Pester scripts/tests 223
  passed / 0 failed / 4 skipped / 227 total (matches round-2 final
  exactly); Pester scripts/probes 24/24; probe-yard-snapshot.mjs PASS.
  Byte-clean exit, no scratch-store debris.
task-id: view-69-71-car-r3
```</result>
<usage><subagent_tokens>463152</subagent_tokens><tool_uses>354</tool_uses><duration_ms>1829901</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a8928de8b3b8e6b95</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\a8928de8b3b8e6b95.output</output-file>
<status>completed</status>
<summary>Agent "Delta review r2 #69+#71" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Byte-clean. Writing the round-3 verdict.

---

# VERDICT: APPROVE — 0 Major, 6 Minor

**The cap does not fire.** I found no Major anywhere, and none in the superseded-rendering surface. My round-2 REJECT-ESCALATED condition is not met.

**Base verified.** `git log -1 --format='%H'` → `4b50a5c5513a7b7401188a4b4d41d989aac572a3`, detached, `git status --porcelain` empty. Matches the brief.

## Suites — re-derived by me at 4b50a5c

| Suite | Observed | Car claimed | Verdict |
|---|---|---|---|
| Node `board/web` | **212 tests / 212 pass / 0 fail / 0 skip**, exit 0 | 212/212 | matches |
| Go `board/...` (`-count=1 -v`) | **94 top-level `--- PASS`**, 0 FAIL | 94, unchanged | matches — and correctly unchanged: `git diff 2861bfd..4b50a5c --name-only \| grep -c '\.go$'` → **0** |
| `go vet ./...` | exit 0, no output | clean | matches |
| Pester `scripts/tests` | **227 total / 222 passed / 0 failed / 5 skipped** | 223/0/4 | matches modulo the detached-HEAD skip |
| Pester `scripts/probes` | **24 / 24 / 0 failed** | 24/24 | matches |
| `probe-yard-snapshot.mjs` | `PROBE RESULT: PASS`, exit 0 | PASS | matches |

Fifth skip named, identical to rounds 1 and 2: `run-session-start-guards.sh: agent-pull runner (#50).running the REAL guards prints [ci-baseline]'s marker ONLY when attached to a branch`. Node 208 → 212 (+4: 3 geometry, 1 collapsed-by-default pin).

---

# THE NEW INSTRUMENT — it guards the property, and I watched it fire

`board/web/test/browser-dispatch-row-geometry.test.js` is the geometry regression pin I prescribed in round 2. Ruling: **it is real, not decoration.**

**At HEAD:** 3/3 green.

**Non-vacuity, re-derived by me** — not accepted from the car's report. I restored round 2's own rendering wholesale (`git checkout 2861bfd -- board/web/css/board.css board/web/js/dom-writer.js`; both files, because the fix spans both) and ran the finalized test file unchanged:

```
sha board.css      B4860579BAD2FF26E2752D5F4F153F5BB2E35E7A60F1BB655557481E6F830E6B -&gt; 8C06977D9B1D9A593C36A81C02DE6BE53CA123F6711589DA3F5B00204BF1C8C6
sha dom-writer.js  FC8F5ABD5D56401101FA1CBA08E35F195EE2AB87939CB803243B2E3BCC234B17 -&gt; 3E28D13F38A982236F1BB82B188BD57DD3DB7E7F847AE15BCD936B1D6F207BA9

not ok 1 - ... renders its subject/state/elapsed cluster on ONE visual line
    REGRESSION (MAJOR-R2-1): row 'plain-subject-00' (no disclosure) spans 2 visual lines
    for its subject/state/elapsed cluster - expected exactly 1
not ok 2 - ... zero PLAIN rows cross .solari-rows own clientHeight boundary mid-row
    REGRESSION (MAJOR-R2-1): 1 plain row(s) crop mid-row at .solari-rows' own boundary -
    [{"subject":"plain-subject-03","top":160,"height":51,"hasDisclosure":false,
      "distinctContentTopCount":2,"croppedMidRow":true}]
# tests 3 / # pass 1 / # fail 2
```

Red for **exactly** the two reasons I named in round 2, and the injected row height reads **51px** — the same number my round-2 probe measured. Both files restored, `sha256` identical, `git status --porcelain` empty.

Test 3 (expansion isolation) stays green under the injection. That is honest, not a hole: it guards a different property that round 2's layout happened to satisfy, and the file's own header scopes its non-vacuity claim to "this file's first test".

---

# MAJOR-R2-1 — re-derived with my own round-2 probes, at 4b50a5c

**My probe 3 (18 plain rows, none carrying a superseded entry — the fixture that made round 2 undeniable), 1600px viewport:**

| | round 2 (`2861bfd`) | **round 3 (`4b50a5c`)** | base counterfactual |
|---|---|---|---|
| `distinctRowHeights` | `[51]` | **`[22]`** | `[22]` |
| `containerScrollH` / `clientH` | 210 / 176 (overflowing) | **96 / 96** | 96 / 96 |
| `croppedMidRowCount` | **3** | **0** | 0 |
| `fullyVisibleRows` | 15 of 18 | **18 of 18** | 18 of 18 |

The `flex-wrap: nowrap` runtime override **now changes nothing** — because `.solari-row` no longer sets `flex-wrap` at all. The base value is genuinely restored, not merely compensated.

**My probe 2 (mixed fixture, same paint), per-child:** every plain row reports `nonDisclosureVisualLines: 1` (was 2), and `.solari-row` now holds a single `.solari-row-main` child of `h: 19` carrying the whole cluster on one line at one `top`.

**Real committed `artifacts/` store** (server default resolution, read-only): `distinctRowHeights: [38]` — down from `[73]` at round 2. All 4 rows carry a disclosure, so 38px = 22px cluster + ~16px collapsed summary. That is the owner-ruled cost — one quiet summary line on rows that have history — and nothing more. Structural assertions all hold: `allDirectChildrenAreRows: true`, `allCarsDirectChildrenAreChips: true`, 10 chips / 10 disclosures, `anyDisclosureOpenByDefault: false`.

**Chip side unaffected**, as the car claims and I verified rather than assumed: `.car-chip` measures `h: 23, visualLines: 1` at 4b50a5c, identical to round 2 and identical under the nowrap override. The column-direction change is scoped to `.solari-row`; `.car-chip` keeps `display:flex; flex-wrap:wrap` and `.car-superseded-disclosure` keeps `flex-basis: 100%`, correctly split into two rules (`board.css:387-395`).

**Expansion displaces only by the entry's own height**, measured: `rowH 52` collapsed → the expanded row grows by the entry's box; every other row keeps its height and rows above keep their `top` (geometry test 3, plus my own probe).

---

# MINOR FINDINGS

**MINOR-R3-1 (measured, undisclosed, created by this round's fix) — an EXPANDED superseded entry now wraps to two lines at most desktop widths.** This round moved `white-space: nowrap` off `.solari-row` (where it covered the whole subtree, including the entries) onto `.solari-row-main` (`board.css:636`, now scoped to the cluster only). The entry therefore computes `white-space: normal`. Swept 9 viewports, real Chromium, real server, one expanded entry:

```
vw= 340  cols=285px            entry 256x14  ws=normal ~lines=1  rowH=52
vw= 500  cols=210x2            entry 182x28  ws=normal ~lines=2  rowH=66
vw= 760  cols=219x3            entry 190x28  ws=normal ~lines=2  rowH=66
vw=1024  cols=220x3            entry 191x28  ws=normal ~lines=2  rowH=66
vw=1280  cols=223x4            entry 194x28  ws=normal ~lines=2  rowH=66
vw=1400  cols=253x4            entry 224x14  ws=normal ~lines=1  rowH=52
vw=1600  cols=237x5            entry 209x28  ws=normal ~lines=2  rowH=66
vw=1920  cols=237x5            entry 209x28  ws=normal ~lines=2  rowH=66
vw=2560  cols=237x5            entry 209x28  ws=normal ~lines=2  rowH=66
```

Six of nine, including 1600px and 1920px. At round 2 the same entry at 1600px measured `218x14` — one line. The cluster itself stays one line at every width (`clusterLines=1` throughout), so `MAJOR-R2-1` is genuinely fixed; this is a neighbouring surface.

**I considered Major and declined, with reasons on the page so the call can be second-guessed:** it contradicts no comment, no owner ruling, and no spec — the "One line per row" mockup convention documented at `board.css:628-635` is explicitly about the subject id cluster, and the car correctly moved that comment along with the rule it describes. Nothing is truncated, hidden, cropped, or mis-attributed; Law 1 is not engaged. It manifests only after a reader deliberately clicks a disclosure open, so HAVAGLANCE's glance is untouched. And the one-line rendering it replaces was incidental inheritance from a rule that this round correctly re-scoped, in a layout that was itself rejected. Calling this a Major would fire an owner escalation over a post-click cosmetic wrap that no authority forbids — the autoimmune mode. **Disposition: a one-line `white-space: nowrap` on `.solari-superseded-entry`, or a ticket.** Also worth recording: **the instrument I prescribed does not cover this** — it asserts the cluster's line count and plain-row cropping, not the entry's. A reviewer-specified instrument guards the property named and not its neighbour.

**MINOR-R3-2 (Law 1, comment vs code).** `board/web/test/browser-dispatch-row-geometry.test.js:16` — *"A NARROW viewport (500px) forces .solari-rows' auto-fill grid to a single column"*. The code at `:41` uses `viewport: { width: 340, height: 1000 }`. Measured: at 340px the grid is one 285px column (single column, as claimed); at 500px it is **two** 210px columns — so the comment's stated width would not even produce the property it claims. Wrong number, and wrong about its own consequence.

**MINOR-R3-3 (measured false, self-contradicted three lines later).** `docs/screenshots/2026-07-26-view-69-71-candidates/README.md`, the disclosed-artifact paragraph: *"structurally impossible to fully avoid in a CSS Grid with auto-sized row tracks and mixed-height siblings"*. Measured false — `align-items: start` on `.solari-rows` fully removes it (see the adjudication below), and the README **itself proposes exactly that** in its next sentence. Say "not worth fixing here" or "avoidable with `align-items: start`, deferred"; not "impossible".

**MINOR-R3-4 (caption inaccurate about the image it describes).** Same README, Round 3 section: *"every plain row and the OTHER disclosure row (`with-sup-1`) keep their original one-line height, unaffected."* Measured: `with-sup-1` is a **38px, two-line** row (19px cluster + 13px collapsed summary), never one line. The substance — *unaffected* — is correct and is what geometry test 3 asserts; the *"one-line"* qualifier is false for the one row it is attached to by name. This is the round-2 lesson in miniature: a caption asserting something the measurement contradicts.

**MINOR-R3-5 (the MAJOR-R2-2 audit is one instance short, and its stated reason is wrong for that instance).** I walked every added line across the whole train (`git diff 256d8d5..4b50a5c`) for line-number citations. Five hits, all verified at HEAD:

| Citation | In | Target file edited by this train? | True at HEAD? |
|---|---|---|---|
| `fold.go:112` | `assemble_test.go` | No | Yes — `\tSpend   any` |
| `gating-matrix.md:49` | `dom-writer.js`, `lanes.js` | No | Yes — the "cited by symbol, not line, per #65's own lesson" row |
| `"(line 141)"` | `dom-writer.js:222` | n/a | Quotation of the removed falsehood inside its own correction — not a live citation |
| **`app.js:148`** | `browser-conditions-persistence.test.js` | **YES — `+39/-1` at `256d8d5`** | Yes — `root.addEventListener('toggle', handleConditionToggle, true);` |

The citation itself is **true**, and `app.js` has not been touched since `256d8d5` (`git diff 256d8d5..4b50a5c -- board/web/js/app.js` → 0 files). But the audit's stated justification — *"in files this train never edits"* — is factually wrong for `app.js`, and its stability is circumstantial rather than structural: any future commit inserting above line 148 drifts it silently, and the #65 gate does not check in-range shifts (`CitationResolverPolicy.Tests.ps1:12`). Bounded: the justification lives only in the car's report, not in any committed surface (`grep` for it across the repo returns nothing), so no durable artifact carries the false claim.

**MINOR-R3-6 (false technical claim in a permanent comment; measured no-op).** `board/web/css/board.css:388-391`: *"`flex-basis:100%` in a COLUMN container sizes the cross-axis dimension (HEIGHT, not width), which would be wrong here."* Two errors: in a column flex container the vertical axis is the **main** axis, not the cross axis; and the asserted consequence does not occur. Measured — forcing `flex-basis: 100%` onto `.solari-superseded-disclosure` at 1400px changed **nothing** (`discH` 13 before and after, `rowBoxH` unchanged), because the row's height is content-driven and a percentage basis resolves against an indefinite height. The decision (omit the property) is fine by minimalism; only the reason is wrong. Blast radius zero, which is why this is Minor and not the class MAJOR-R2-3 was.

---

# THE DISCLOSED OUT-OF-SCOPE OBSERVATION — adjudicated

**Is it real?** Yes. Measured at 1400px (4 columns, 9 rows over 3 grid bands):

```
band 1: plain-subject-00..03   rowBoxH 22   mainH 19   mainVisualLines 1
band 2: plain-subject-04,05,06 rowBoxH 38   mainH 19   mainVisualLines 1   &lt;- stretched
        with-sup-0             rowBoxH 38   mainH 19   discH 13
band 3: with-sup-1             rowBoxH 38   mainH 19   discH 13
```

Plain rows measure 38px **only in the band that also holds a disclosure row**. Their content stays one line (`mainH: 19`, one distinct `top`) — the extra 16px is blank box, not wrapped text. Computed `align-items` on `.solari-rows` reads `normal`, which for a grid container behaves as `stretch`.

**Is the README's description accurate?** Yes, on every checkable claim: not content wrapping (confirmed — `mainVisualLines: 1`), caused by grid stretch across a shared row band (confirmed), 38px vs 22px (confirmed exactly), and distinct from `MAJOR-R2-1`'s failure mode (confirmed — no crop, cluster one line, geometry test still green). The one clause that fails is `MINOR-R3-3`'s "impossible to avoid".

**Is it pre-existing?** The **mechanism** predates the entire train: `.solari-rows`' rule is **byte-identical** at `b865cba` and at `4b50a5c` (`display: grid; grid-template-columns: repeat(auto-fill, minmax(13rem, 1fr)); gap: 0.15rem 1.5rem; max-height: 11rem; overflow-y: auto;`) with `align-items` never declared. What is new is the **trigger** — mixed-height siblings — which arrives with `#71`'s feature. The earliest commit where a *disclosure-bearing row* can stretch a plain row is `2861bfd` (round 2), where it was confounded because the wrap regression made every row 73px anyway; `4b50a5c` is the first commit where it is cleanly separable. The README's wording — *"not new to this round's fix … present since MAJOR-R1-1 first shipped round-2"* — is **precisely correct** and does not overclaim pre-existence relative to the train.

**Was not fixing it the right call under the cap?** **Yes.** It is outside all five named findings, it is cosmetic (blank box, no lost or misplaced information, no crop, Law 1 untouched), and a same-round unscoped CSS change to the shared grid rule is exactly the move that produced `MAJOR-R2-1`. Disclosing it and stopping is the correct application of the cap.

**Recommended disposition: TICKET, not live-with, and it is cheap.** Measured, same paint, `align-items: start` on `.solari-rows`: all seven plain rows drop **38px → 22px**, both disclosure rows correctly stay 38px, nothing else moves. One property, fully effective, verified. It wants its own ticket because it touches a shared grid rule that governs the whole dispatches lane and deserves its own geometry assertion — the same lesson this train paid three rounds for.

---

# FIVE-ID TABLE

| ID | Ruling | Evidence |
|---|---|---|
| **MAJOR-R2-1** (flex-wrap wraps every row; #62 crop hazard) | **Present** | `.solari-row` no longer sets `flex-wrap` (`board.css:640-646`); cluster moved to `.solari-row-main` (`dom-writer.js:533,541-548`), a no-wrap `display:flex` wrapper; `.solari-row` is `flex-direction: column`. My round-2 probe 3 re-run: 18 plain rows `[22]` (was `[51]`), scrollH 96 == clientH 96 (was 210 vs 176), **0 cropped** (was 3), 18/18 visible. Real store `[38]` (was `[73]`). `.car-chip` unchanged at 23px/1 line. New instrument reds on round 2's own CSS+DOM for both stated reasons, `height: 51` in the failure payload. |
| **MAJOR-R2-2** (`"(line 141)"` false at commit time) | **Present** | `dom-writer.js:136-141` now cites `renderBoard` **by symbol** with the falsehood recorded rather than erased. Full-train audit of added lines found 5 line-number citations; all 5 verified true at HEAD (`fold.go:112` → `Spend any`; `gating-matrix.md:49` → the #65-lesson row; `app.js:148` → the listener). Residual instance and wrong justification → `MINOR-R3-5`. |
| **MAJOR-R2-3** (schema/ingest overclaim) | **Present** | `dom-writer.js:142-166` now states the `$def` is unenforced on the runtime hop, names `$defs.lane.data` and the schema's own `$comment` as the reason, names `WirePayload.Tests.ps1` as the sole consumer, and says the renderer filter is the **entire** runtime protection. Re-measured at 4b50a5c with the real vendored cfworker validator: `kind:7` → `valid=true`, `missing at` → `valid=true`. Every claim true. No leak: `grep` for the old "defense-in-depth" framing returns only the corrected negation at `dom-writer.js:162`. |
| **MINOR-R2-1** (no collapsed-by-default pin) | **Present** | `dom-writer.test.js:899-966` pins both owners. Verified the shim can express it (`minidom.js:25-26` — `setAttribute` writes into `this.attributes`), then fault-injected `details.setAttribute('open','')` at `dom-writer.js:175`: `not ok 36 … REGRESSION (MINOR-R2-1): a superseded disclosure carries an 'open' attribute ({"open":""})`, `# tests 47 / # pass 46 / # fail 1`. Restored `sha256 FC8F5ABD…` identical. |
| **MINOR-R2-2** (tautological witness) | **Present** | README now records the tautology explicitly, states why it was one, and replaces it with `closest('.solari-row') === ownRow` plus `ownRow.contains(disclosure) &amp;&amp; ownRow !== disclosure` plus a bounding-box containment check — all three falsifiable, and it points readers at the `dom-writer.test.js` structural tests as the authoritative form. Also volunteers that the round-2 fixture could not exhibit `MAJOR-R2-1` at all (single row, no `elapsedSeconds`, 2-item cluster) — an honest disclosure I did not ask for. New PNGs opened and checked: `collapsed-r3` shows 7 one-line plain rows; `expanded-r3` shows the entry nested and indented inside `with-sup-0`'s own box with `with-sup-1` unchanged. Caption defect → `MINOR-R3-4`. |

---

# CONSTITUTION CHECK

- **Law 1.** Round 2's three violations are **all gone**, each verified by running rather than reading: the density/one-line claim is now true (measured `[22]`, 0 cropped); the `"(line 141)"` citation is symbol-form; the schema/ingest claim is corrected and its correction re-measured. **New, all Minor-tier:** `MINOR-R3-2` (500px vs 340px), `MINOR-R3-3` ("impossible to avoid", measured avoidable), `MINOR-R3-4` (with-sup-1 "one-line"), `MINOR-R3-6` (cross-axis, measured no-op). None asserts a protection that does not exist; none has reachable consequence.
- **Law 6.** Honored. The shared `.car-superseded-disclosure, .solari-superseded-disclosure` rule was correctly **split** rather than forced to keep serving two now-different layouts, with the divergence explained in place (`board.css:380-395`) — the right call, since a single rule spanning two mechanisms is the second-copy hazard inverted.
- **Law 7.** Honored — no taxonomy added; the geometry fixture is generated, not hardcoded to the real store.
- **Law 2 / #68.** Honored — entries stay in the DOM behind a counted, labelled affordance.
- **TDD / red-first.** Honored, and this is the round's strongest showing: the geometry instrument is red-first by construction and I re-derived its red myself; the collapsed-by-default pin reds under injection. The one gap is `MINOR-R3-1`, where a behaviour changed with no assertion covering it.
- **#40 (guards proven by watching them fire).** **Honored for the first time in this train.** Both new guards were watched firing, by me, at HEAD-adjacent injected states. The round-1 and round-2 pattern — visual evidence produced on a fixture that could not falsify the claim — is broken: the new screenshots use a 7-plain-row fixture that *does* exhibit the class, and the README says so explicitly about its own predecessors.
- **NORTH STAR.** Honored in substance: the design doc, state ledger and README were trued in the same commits, and the README supersedes rather than deletes its own stale round-2 material (the two round-2 PNGs are kept and captioned as unable to exhibit the class, which is more honest than removing them). The four Minor doc inaccuracies above are real but none leaves a reader with a false model of the mechanism.
- **#42 (bare `#N`).** Honored — `browser-dispatch-row-geometry.test.js` carries 5 `#69`/`#71` markers; the two new PNGs are cited through the sibling `README.md`, which carries its `Status:` line, exactly as the fixture clause prescribes.

---

# BASE-DELTA (#13)

- **Round 2 reviewed:** `2861bfd8a99d8213ca0058a999ab91706f4ccc9b`
- **This round reviewed:** `4b50a5c5513a7b7401188a4b4d41d989aac572a3`, detached, clean
- **`git diff 2861bfd..4b50a5c --stat`:** 9 files, **+475 / −56**, **zero Go files**

```
 board/web/css/board.css                                     |  74 +++++---
 board/web/js/dom-writer.js                                  |  72 +++++----
 board/web/test/browser-dispatch-row-geometry.test.js        | 159 +++++++++++++++++
 board/web/test/condition-open-state.test.js                 |  22 +--
 board/web/test/dom-writer.test.js                           |  70 ++++++++
 board/web/test/support/real-board-server.js                 |  56 ++++++
 docs/screenshots/.../README.md                              |  78 ++++++++-
 docs/screenshots/.../dispatch-rows-multirow-collapsed-r3.png| Bin 0 -&gt; 37052
 docs/screenshots/.../dispatch-rows-multirow-expanded-r3.png | Bin 0 -&gt; 43216
```

Three commits: `8073f0b` (MAJOR-R2-1 + MAJOR-R2-2 + MAJOR-R2-3), `9b899ab` (MINOR-R2-1), `4b50a5c` (MINOR-R2-2).

**Visible delta for a board reader, measured:** dispatch rows return to the base one-line, ~22px rendering; rows carrying history gain one quiet ~16px summary line (38px total) and nothing else; no row crops mid-row at any tested width; an expanded entry now wraps to two lines at most desktop widths (`MINOR-R3-1`). No wire value changed.

---

# CONVERGENCE RULING — converging, and the cap does not bind

| Round | Base | Majors | Minors | Where the Majors landed |
|---|---|---|---|---|
| 1 | `256d8d5` | **4** | 3 | four distinct surfaces |
| 2 | `2861bfd` | **3** | 2 | all three in the superseded-rendering surface; **2 created by the round-1 fix** |
| **3** | `4b50a5c` | **0** | 6 | — |

All three swirl conditions are now **clear**: counts declined 4 → 3 → **0**; the clustering dissolved rather than moved; and **no finding this round is a defect a previous fix created** except `MINOR-R3-1`, which is Minor-tier, contradicts no authority, and is the kind of neighbouring cosmetic consequence a bounded fix legitimately has. My round-2 cap — *any Major in the superseded-rendering surface → REJECT-ESCALATED to the owner* — **does not fire.**

**What actually changed between round 2 and round 3 was the instrument, not the effort.** My round-2 ruling named the premise both earlier rounds inherited: that a CSS change's rendered effect can be verified by reading plus one screenshot. This round replaced reading with measurement — a real-browser geometry assertion that I watched red on round 2's own defect, a fixture that can exhibit the class, and screenshots captured on that fixture with their predecessors honestly demoted. That is the probe doctrine landing as a durable artifact, and it is why the count went to zero rather than sideways. The six Minors are the residue of a bounded fix, not the signature of a swirl.

**Recommendation to the conductor: APPROVE and land.** Two follow-ups as tickets rather than another round, because none of them belongs to this train's scope and reopening the surface is the risk this cap exists to prevent:
1. `MINOR-R3-1` (entry wraps when expanded) plus the `align-items: start` grid-stretch artifact — one small CSS ticket, and it should land with a geometry assertion of its own, extending `browser-dispatch-row-geometry.test.js`.
2. The four documentation Minors (`R3-2`, `R3-3`, `R3-4`, `R3-6`) are one-line corrections that can ride the same ticket or a doc rider. `MINOR-R3-5`'s `app.js:148` conversion belongs there too.

If the conductor prefers, `R3-2`/`R3-3`/`R3-4`/`R3-6` are cheap enough to fold into this train as a comment-and-caption rider before merge, since none touches behaviour and each is a single sentence. That is a scope call, not a gate call — I do not require it for APPROVE.

---

# UNREPRODUCIBLE-EVIDENCE CALLOUT (#13)

**None.** Every finding rests on re-runnable evidence: a two-file `git checkout` injection with before/after `sha256` on both files, a one-line `setAttribute` injection with `sha256`, five real-browser probes against a real `board/server` Go binary (three synthetic fixtures, one nine-width viewport sweep, one against the repo's own committed `artifacts/` store), a Node probe against the real vendored validator, `git show` at `b865cba`/`256d8d5`/`2861bfd`, and a mechanical grep audit over `git diff 256d8d5..4b50a5c` added lines. Both new PNGs are in the repo and were opened.

One labelling caveat, disclosed so nobody misreads my transcript: my scratch probes `browser-probe2/3/4.mjs` carry round-2 header strings (`"AS SHIPPED (2861bfd CSS)"`) that I did not edit between rounds. Every run reported above was executed against the worktree at `4b50a5c`; the header text is stale, the measurements are not.

**Byte-clean exit.** `git status --porcelain` empty; HEAD `4b50a5c5513a7b7401188a4b4d41d989aac572a3`. Files injected and restored with matching `sha256`: `board/web/css/board.css` `B4860579BAD2FF26E2752D5F4F153F5BB2E35E7A60F1BB655557481E6F830E6B`, `board/web/js/dom-writer.js` `FC8F5ABD5D56401101FA1CBA08E35F195EE2AB87939CB803243B2E3BCC234B17`; `board/web/js/app.js` untouched this round and still `9BBCB03A2FC5008EE259E00A057841555FF44C3815091F8AC9D48735A32AB843`. One gitignored `.scratch-store-69-71-rev2b-*` directory left by a truncated pipe was removed; zero remain. Nothing committed, nothing pushed. The shared checkout was not touched at all this round.

**Reviewer probe scripts** (re-runnable, in the session scratchpad `C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\scratchpad\`): `browser-probe2.mjs`, `browser-probe3.mjs`, `browser-probe4.mjs` (round-2 recipes, re-run), `browser-probe5.mjs` (grid-stretch adjudication + flex-basis claim), `browser-probe6.mjs` (viewport sweep, `MINOR-R3-1`), `client-ref-probe.mjs` (`MAJOR-R2-3` re-verification).

```starcar-artifact
outcome: APPROVE
findings: 0 Major, 6 Minor
abstract: |
  Delta re-review of 4b50a5c (view train #69+#71 fix cycle round 3, three commits
  8073f0b/9b899ab/4b50a5c over round 2's reviewed base 2861bfd). APPROVE. The round-2 cap
  does NOT fire: zero Majors anywhere, and none in the superseded-rendering surface.

  Suites re-derived independently at HEAD: Node board/web 212 tests / 212 pass / 0 fail /
  0 skip, Go board 94 top-level PASS with -count=1 -v and 0 FAIL (correctly unchanged -
  zero Go files in the diff, verified), go vet exit 0 no output, Pester scripts/tests 227
  total / 222 passed / 0 failed / 5 skipped with the 5th skip named as the detached-HEAD
  run-session-start-guards.sh ci-baseline guard, Pester scripts/probes 24/24,
  probe-yard-snapshot.mjs PASS. Every count the car claimed reconciles.

  ALL FIVE ROUND-2 FINDINGS RULED PRESENT, each re-derived by running.

  MAJOR-R2-1 fixed and verified with my OWN round-2 probes re-run at 4b50a5c. .solari-row
  no longer sets flex-wrap; the subject/state/elapsed cluster moved into a non-wrapping
  .solari-row-main wrapper and .solari-row became flex-direction column. My 18-plain-row
  fixture at 1600px: distinctRowHeights [22] versus round 2's [51], containerScrollHeight
  96 equal to clientHeight 96 versus 210 against 176, ZERO rows cropped versus three, 18
  of 18 fully visible versus 15. The flex-wrap nowrap runtime counterfactual now changes
  nothing at all, proving the base value is genuinely restored rather than compensated. On
  the repo's real artifacts store, 38px versus round 2's 73px, the 16px difference being
  the owner-ruled collapsed summary line on rows that carry history. Per-child: every
  plain row reports one visual line, one distinct top. The chip side is untouched -
  .car-chip measures 23px and one visual line at 4b50a5c, identical to round 2 and
  identical under the override.

  THE NEW INSTRUMENT IS REAL, NOT DECORATION, and I watched it fire rather than trusting
  the car's report. board/web/test/browser-dispatch-row-geometry.test.js is 3 of 3 green at
  HEAD. I restored round 2's rendering wholesale via git checkout 2861bfd of BOTH
  board.css and dom-writer.js (sha256 recorded both directions, both restored identical)
  and ran the finalized test unchanged: test 1 red with "row 'plain-subject-00' (no
  disclosure) spans 2 visual lines for its subject/state/elapsed cluster - expected exactly
  1" and test 2 red with "1 plain row(s) crop mid-row at .solari-rows' own boundary"
  carrying height 51 in its payload - the exact 51px my round-2 probe measured. Test 3
  stays green under injection, which is honest rather than a hole: it guards expansion
  isolation, a property round 2's layout happened to satisfy, and the file's own header
  scopes its non-vacuity claim to the first test.

  MAJOR-R2-2 fixed: the citation is symbol-form and the falsehood is recorded rather than
  erased. I audited the WHOLE train's added lines (git diff 256d8d5..4b50a5c) for
  line-number citations myself: five hits, all verified true at HEAD - fold.go:112 resolves
  to "Spend any", gating-matrix.md:49 to the #65-lesson row, app.js:148 to the listener
  line, plus the quoted "(line 141)" inside its own correction narrative. MAJOR-R2-3
  fixed: the comment now states the $def is unenforced on the runtime hop, names
  $defs.lane.data and the schema's own $comment as the reason, names WirePayload.Tests.ps1
  as the sole consumer, and calls the renderer filter the entire runtime protection.
  Re-measured with the real vendored cfworker validator at 4b50a5c: kind:7 and missing-at
  both still validate clean, so every claim is true. No leak - grep finds the old
  defense-in-depth framing only inside the corrected negation. MINOR-R2-1 pinned and
  fault-injected: setAttribute open reds test 36 with the intended message, 46 of 47,
  restored identical; I first verified minidom's setAttribute writes into .attributes so
  the pin can actually express the property. MINOR-R2-2 fixed with a falsifiable witness,
  and the car volunteered unprompted that its round-2 fixture could not exhibit MAJOR-R2-1
  at all. Both new PNGs opened and checked against their captions.

  SIX MINORS, all measured, all one-line fixes, none contradicting an authority.
  MINOR-R3-1 is the notable one and I explain why I declined to call it Major: this round
  moved white-space nowrap off .solari-row onto .solari-row-main, so an EXPANDED
  superseded entry now wraps to two lines at 6 of 9 swept viewports including 1600 and
  1920 (entry 209x28, whiteSpace normal, versus 218x14 at round 2). It contradicts no
  comment, ruling or spec - the "One line per row" convention is explicitly about the
  subject cluster and the car correctly moved that comment with its rule - nothing is
  truncated, cropped or mis-attributed, Law 1 is not engaged, and it manifests only after
  a deliberate click, so the glance is untouched. Calling it Major would fire an owner
  escalation over a post-click cosmetic wrap, which is the autoimmune mode. Recorded with
  the observation that the instrument I prescribed guards the property I named and not its
  neighbour. MINOR-R3-2: the geometry test's comment says a 500px viewport where the code
  uses 340px, and measurement shows 500px yields TWO columns, so the stated width would not
  produce the claimed property. MINOR-R3-3: the README calls the grid-stretch artifact
  "structurally impossible to fully avoid" three lines before proposing the one property
  that fully avoids it, measured. MINOR-R3-4: the expanded-r3 caption says with-sup-1 keeps
  its "original one-line height" - measured 38px and two lines. MINOR-R3-5: app.js:148 is a
  line citation into a file this train DID edit (+39/-1 at 256d8d5), stable only because no
  later commit touched it; the citation is true but the audit's stated reason ("files this
  train never edits") is wrong for it, and that reason lives only in the car's report, not
  in any committed surface. MINOR-R3-6: board.css calls the vertical axis of a column flex
  container the cross axis (it is the main axis) and asserts a consequence measurement
  shows does not occur - forcing flex-basis 100% onto the disclosure changed nothing.

  OUT-OF-SCOPE OBSERVATION ADJUDICATED. Real: at 1400px, plain rows sharing a grid band
  with a disclosure row measure 38px instead of 22px while their content stays one line
  (mainH 19, one distinct top), computed align-items normal behaving as stretch. The
  README's description is accurate on every checkable claim except MINOR-R3-3's
  "impossible" clause. Pre-existing: the MECHANISM predates the whole train - .solari-rows'
  rule is byte-identical at b865cba and 4b50a5c with align-items never declared - while the
  TRIGGER, mixed-height siblings, arrives with #71's feature; the earliest commit where a
  disclosure row can stretch a plain row is 2861bfd, where it was confounded by the wrap
  regression, so 4b50a5c is the first commit where it is cleanly separable. The README's
  "not new to this round's fix, present since MAJOR-R1-1 first shipped round-2" is
  precisely correct and does not overclaim. Not fixing it under the cap was the RIGHT call:
  out of scope, cosmetic, and an unscoped change to the shared grid rule is exactly the
  move that produced MAJOR-R2-1. Disposition TICKET rather than live-with, and cheap:
  measured in the same paint, align-items start drops all seven plain rows from 38px to
  22px while both disclosure rows correctly stay 38px.

  CONSTITUTION: round 2's three new Law 1 violations are ALL GONE, each verified by
  running. Four new Law-1-adjacent inaccuracies exist but are all Minor-tier with no
  reachable consequence and none asserting a protection that does not exist. Law 6 honored
  and notably well - the shared disclosure rule was SPLIT rather than forced to serve two
  now-different layouts, with the divergence explained in place. Law 7, Law 2/#68 honored.
  TDD honored, the round's strongest showing. #40 HONORED FOR THE FIRST TIME IN THIS TRAIN:
  both new guards were watched firing by me, and the round-1/round-2 pattern of visual
  evidence built on a fixture that could not falsify it is broken. NORTH STAR honored in
  substance, with the stale round-2 PNGs superseded and captioned rather than deleted.
  #42 honored including the fixture clause for the two new PNGs.

  CONVERGENCE: 4 Majors to 3 to ZERO. All three swirl conditions now clear - counts
  declined, the clustering dissolved rather than moved, and no finding this round is a
  defect a previous fix created except MINOR-R3-1, which is Minor-tier and contradicts no
  authority. The cap does not bind. What changed between rounds 2 and 3 was the INSTRUMENT,
  not the effort: measurement replaced reading, exactly the premise my round-2 ruling named,
  and that is why the count went to zero rather than sideways. Recommendation: APPROVE and
  land; two follow-up tickets (the entry-wrap plus the grid-stretch artifact as one CSS
  ticket carrying its own geometry assertion; the four documentation Minors plus the
  app.js:148 conversion as a doc rider), rather than reopening this surface for a round 4.

  BASE-DELTA 2861bfd to 4b50a5c, 9 files +475/-56, zero Go files. Visible delta measured:
  dispatch rows return to the base one-line ~22px rendering; rows carrying history gain one
  quiet ~16px summary line; no row crops mid-row at any tested width; an expanded entry
  wraps to two lines at most desktop widths. No wire value changed.

  UNREPRODUCIBLE-EVIDENCE: none. One labelling caveat disclosed - my scratch probes carry
  stale round-2 header strings; every run reported was executed against the worktree at
  4b50a5c. Byte-clean exit, git status --porcelain empty at HEAD
  4b50a5c5513a7b7401188a4b4d41d989aac572a3. Files injected and restored with matching
  sha256: board.css B4860579, dom-writer.js FC8F5ABD (twice - the two-file round-2 restore
  and the setAttribute injection); app.js untouched this round at 9BBCB03A. One gitignored
  scratch directory left by a truncated pipe was removed, zero remain. Nothing committed,
  nothing pushed, shared checkout not touched at all this round.
task-id: view-69-71-review-r3
```</result>
<usage><subagent_tokens>280881</subagent_tokens><tool_uses>115</tool_uses><duration_ms>805799</duration_ms></usage>
</task-notification>

---

sound good roll the next train

---

<task-notification>
<task-id>biwogyfdn</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\biwogyfdn.output</output-file>
<status>completed</status>
<summary>Background command "Watch CI to terminal state for merge 7320f5c (background)" completed (exit code 0)</summary>
</task-notification>

---

why is the planned vs actual distinction lost? I think that's the more interesting question

---

yeah that seems to be the smarter and better way to go, update the ticket and lets run through the design while it's still early in the morning here

---

errm. yeah ok, that seems more sane. send it.

---

yeah agreed, land the probe as a pinned test. THat's our whole ecosystem and circle of life working for us. that just saved us about 2 hours and lord knowns how many tokens.

---

<task-notification>
<task-id>ae9c59ae16e969916</task-id>
<tool-use-id>toolu_017bvW9o6eiKiR678szWBTHR</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\ae9c59ae16e969916.output</output-file>
<status>completed</status>
<summary>Agent "Car #75 dispatch row titles" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Final report

**Base verified:** worktree `C:\Users\Chris\git\starcar-wt\car-view-75`, branch `car/view-75`, base commit `bc4c28fc9e0c7fe14fbe6f78633664ebf1774d21` — matched, working tree clean, both confirmed before any edit.

**Final HEAD:** `82f64b5f145885cee56d1337370dfe121d3e4d2b`, 4 commits, working tree clean, 10 files changed (+593/-13).

### Per-hop results

**1. STORE (verify only, no change).** Confirmed `task_id` already flows: `board/store/store.go:134` — `typedRecord.TaskID string \`json:"task_id"\`` (landed under #51); `board/store/store.go:263-266` decodes the same bytes into the generic `map[string]any` Fields that `readOne` returns, so `Record.Fields["task_id"]` is already populated on any record that carries the key. No store change made.

**2. ASSEMBLE — HONEST STOP on the brief's literal placement, implemented at the precedent's actual location.** Commit `d43dac8`.
- Brief step 2 said to add `TaskID` to `board/fold/fold.go`'s `DispatchEntry`, citing recordDir as precedent. `grep -n "recordDir\|RecordDir" board/fold/fold.go` returns nothing — recordDir is **not** on `DispatchEntry`; it lives at `board/assemble/assemble.go` (`recordDirBySubject`, `dispatchWireMap`), confirmed by reading `board/assemble/assemble.go:306-352` and `types.go:19-27`.
- The reason: `schema/vectors/README.md:5-7` states fold is the **cross-language conformance kernel** — "every fold implementation - the landed pwsh detector (`scripts/Detect-Dispatches.ps1`) and the Go port - conforms to THESE fixtures, and the D18 cross-verifier CI job runs BOTH against them, failing on any divergence." `grep -n "recordDir" scripts/Detect-Dispatches.ps1` returns nothing, confirming recordDir was deliberately kept off the shared kernel.
- Implemented instead at `board/assemble/assemble.go`: `taskIDsBySubject`/`taskIDForReturnedSubject` (winner-aware, mirroring `findingsForReturnedSubject`'s exact subject+kind==returned+at==at match — **not** `recordDirBySubject`'s first-write-wins, which would silently miss a returned record's task_id whenever the path-sorted scan orders a task_id-less dispatched sibling first). Reaches `DispatchesPayload` (`dispatchWireMap`'s new `"taskId"` key, omitted when empty) and `TrainCar.TaskID`.
- Red: `TestAssembleDispatchTaskIdFromReturnedWinnerNotFirstScanOrder` → `d["taskId"] = &lt;nil&gt;, want ... "solo-1-winning-task-id"`; `TestAssembleTrainCarTaskId` → `car.TaskID = "", want "carA-task-id"` (both observed after a throwaway field made the package compile). The two absence-guard tests pass vacuously by design (pin the Law-1 floor).
- Green: `board/assemble` 21/21, full `board/...` 98 PASS (94+4) 0 FAIL, `go vet ./...` clean. **`board/fold` untouched** — confirmed via `git diff --stat bc4c28f..HEAD` (no `board/fold/*.go` in the diff) and `TestFoldConformsToVectors`'s 18/18 subtests still pass, proving the D18 kernel carries zero risk from this change.

**3. SCHEMA.** Commit `c5a036d`. Optional `taskId` (string) added to `trainsPayload.cars.items.properties` and `dispatchesPayload.dispatches.items.properties`, matching recordDir's exact convention (not in `required`). `node board/web/probe-yard-snapshot.mjs` PASS (compiles, sample validates, non-vacuity missing-`lanes` rejection still fires). `WirePayload.Tests.ps1` 7/7 unchanged — no sample needed updating (optional field, no helper demands one).

**4. CLIENT.** Commit `b08bfff`. `render.js`: `taskId: d.taskId ?? null` / `c.taskId ?? null`, following recordDir's own pattern. `dom-writer.js`: `.solari-subject` and `.car-subject` now render `taskId ?? subject`; **the native `title` attribute always carries the raw `subject` hash** (verbatim, never the task-id) so the record-dir hash stays reachable either way; the provenance link (`buildRecordLink` keyed on `recordDir`) is untouched. Red-verified by temporarily `git stash`-reverting `render.js`/checking out pre-commit `dom-writer.js` and re-running: `render.test.js` failed `expected: 'view-75-car-r1', actual: undefined`; `dom-writer.test.js` failed 3/4 new cases (the "no-taskId, unchanged" case passes vacuously). Green after fix: `render.test.js` 47/47, `dom-writer.test.js` 51/51, full `board/web` node --test 218/218.

**Surfaces enumerated (brief step 5):** `.solari-subject` (dispatches lane) and `.car-subject` (trains lane car-chip, both car- and gate-role members — the chip loop is role-agnostic) both got the treatment; `.car-subject` had **no** title tooltip before this ticket (new). `.signal-name` (gates lane) does **not** — `Gate.Name` is the member's own declared gate/role name (e.g. "design review round 1"), never a subject hash (`board/assemble/types.go:55-58`), so no fallback treatment applies there.

**5. BROWSER NON-VACUITY.** Commit `82f64b5`. `buildScratchStoreForTaskIdTitles` (real-board-server.js) + `browser-dispatch-row-taskid.test.js`, following `browser-dispatch-row-geometry.test.js`'s real-Chromium pattern. Fault injection: restored pre-#75 `dom-writer.js` (`git checkout d43dac8~1 -- board/web/js/dom-writer.js`, sha256 `fc8f5abd...` — the *same* hash the #69/#71 round-3 verdict recorded, confirming the file was untouched between that landing and this ticket) and ran the new file unmodified: **2/5 red** for the stated reasons (`actual: 'view-75-with-task-id', expected: 'view-75-car-r1'`; and `actual: null` for the chip title lookup since the chip carried no title attribute pre-#75), 3/5 pass vacuously (no-taskId fallback, ellipsis truncation, geometry — none touched by #75). Restored (`sha256 92258d44...`, matching HEAD, `git status --porcelain` clean before and after). Green: 5/5; full `board/web` 223/223; `browser-dispatch-row-geometry.test.js` re-run explicitly: **3/3, no geometry regression**.

### Documentation check

Searched `docs/design/2026-07-21-v0-yard-skeleton-design.md`, `docs/specs/2026-07-23-yard-board-spec.md`, `docs/contracts/{gating-matrix,state-ledger}.md`, `docs/glossary.md`, `README.md`, `docs/setup.md` for claims about the dispatches-row title text, `subject`/`task_id` field lists, or embedded live-UI screenshots. Found none rendered stale: recordDir's own landing (#28/#69/#71) never updated the spec's `dispatchesPayload` line (`docs/specs/2026-07-23-yard-board-spec.md:81-83`) when it added a field, and that line asserts provenance ("mirrors the fold's shape plus assigned"), never a closed field-list, so it isn't contradicted by this addition either. `docs/glossary.md:65-80`'s subject/task_id/mint definitions are unaffected (this ticket surfaces the field in the view; it doesn't change subject/task_id semantics). No embedded README screenshot depicts live dispatch-row content. Disclosing this as a checked-and-clear finding rather than silently assuming it.

### Final suite counts at HEAD `82f64b5`

Node `board/web`: **223/223**. Go `board/...`: **98 PASS, 0 FAIL**, `go vet ./...` clean. Pester `scripts/tests`: **227/223/0/4** (unchanged from baseline — no pwsh touched). Pester `scripts/probes`: **24/24**. `probe-yard-snapshot.mjs`: **PASS**. `CodeCitationPolicy.Tests.ps1`: **13/13** (every new file cites bare `#75`).

### Deviations/disclosures

1. **Honest stop** on brief step 2's literal `fold.go` placement (see above) — implemented functionally equivalent behavior at the architecturally correct location instead of the described one.
2. `.gitnexus/run.cjs` does not exist in this worktree and no GitNexus MCP tool was exposed to this session — impact analysis / `detect_changes()` could not be run through the mandated tooling; substituted manual `grep`/`git diff` verification of blast radius (only `board/assemble`, `board/web`, `schema` touched; `board/fold` and all pwsh scripts confirmed untouched). Disclosing rather than silently skipping.
3. Did not add a taskId-bearing sample to `WirePayload.Tests.ps1` (optional field, no helper/required-field demands one — NIRTS).
4. `npm install` + `npx playwright install --only-shell chromium` were run in `board/web/` to establish the baseline (gate-triggered per `docs/setup.md`'s own posture — `node_modules/` is gitignored, nothing committed there).

Relevant files (all absolute paths under `C:\Users\Chris\git\starcar-wt\car-view-75\`): `board/store/store.go`, `board/assemble/assemble.go`, `board/assemble/types.go`, `board/assemble/assemble_test.go`, `schema/yard-snapshot.schema.json`, `board/web/js/render.js`, `board/web/js/dom-writer.js`, `board/web/test/render.test.js`, `board/web/test/dom-writer.test.js`, `board/web/test/support/real-board-server.js`, `board/web/test/browser-dispatch-row-taskid.test.js`.

```starcar-artifact
outcome: done-with-findings
findings: Honest-stop on brief step 2 (fold.go DispatchEntry placement contradicted by schema/vectors/README.md's D18 cross-language conformance kernel and by recordDir's own actual precedent location, which is board/assemble/assemble.go, not fold.go as the brief described) - implemented functionally equivalent task-id threading at the architecturally correct layer instead, with the reasoning and grep evidence recorded in commit d43dac8. GitNexus MCP tooling was unavailable in this session (no .gitnexus/run.cjs in the worktree, no GitNexus tool exposed) - substituted manual diff/grep verification of blast radius. No documentation found stale by this change (checked design, spec, contracts, glossary, README, setup docs - recordDir's own precedent never required updating the spec's dispatchesPayload field description either). All four commits are red-first with quoted observed failures and green counts stated per hop; final suites at HEAD 82f64b5: Node board/web 223/223, Go board 98 PASS/0 FAIL/vet clean, Pester scripts/tests 227/223/0/4, Pester scripts/probes 24/24, probe-yard-snapshot.mjs PASS, CodeCitationPolicy 13/13.
abstract: Threaded the human task-id handle (#47 envelope echo) through the dispatches lane and trains-lane car-chip, following the recordDir precedent's real location (board/assemble, not fold.go, to protect the fold package's cross-language pwsh/Go conformance contract) - winner-aware resolution (never first-write-wins), optional wire field, honest hash fallback with the full subject reachable via native title tooltip on both surfaces, proven red-first at every hop plus a real-Chromium non-vacuity fault injection.
task-id: view-75-car-r1
```</result>
<usage><subagent_tokens>267628</subagent_tokens><tool_uses>163</tool_uses><duration_ms>1771979</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>b95zyil5p</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\b95zyil5p.output</output-file>
<status>failed</status>
<summary>Background command "Start board server from merged dev tip (background)" failed with exit code 255</summary>
</task-notification>

---

<task-notification>
<task-id>a7b9079a789c94890</task-id>
<tool-use-id>toolu_01BTCRJ7Z4YuB4nd3Ftip77a</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\a7b9079a789c94890.output</output-file>
<status>completed</status>
<summary>Agent "Car #79 pin manifest-board join probe" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Committed cleanly at `c1c4d2d` on `car/probe-79`, working tree clean. Final report follows.

## Summary

**Base verified:** worktree `C:\Users\Chris\git\starcar-wt\car-probe-79`, branch `car/probe-79`, HEAD `82bfb6e0938fb552b51f759c1e6ee2af622dadc8`, `git status --porcelain` empty — matched the brief exactly.

**File landed:** `C:\Users\Chris\git\starcar-wt\car-probe-79\scripts\probes\ManifestBoardJoin.Probes.Tests.ps1` (new), plus two friction-log entries in `C:\Users\Chris\git\starcar-wt\car-probe-79\docs\friction-log.md`. Committed at `c1c4d2dcee4023f721f2d5d433fe3bc2f3583dc1` on `car/probe-79`. No files touched under `board/`, `schema/`, or `scripts/tests/` (confirmed by `git status --porcelain` showing only the two intended files).

**Baselines (base 82bfb6e, my own runs):** `scripts/probes` 24/24 passed; `scripts/tests` 223 passed / 0 failed / 4 skipped.

**Post-change:** `scripts/probes` 24 + 5 new = **29/29 passed**. `scripts/tests` **unchanged** (no files under `scripts/tests/` touched by this commit).

**The five assertions and their fault-injection reds** (each performed by editing the `New-ScratchStore -Root $script:StorePath` call in the committed file, running `Invoke-Pester -Path ./scripts/probes/ManifestBoardJoin.Probes.Tests.ps1 -CI`, then reverting and sha256-checking):

Base file sha256 (before and after every injection, all five matched): `b21e0c5eb4b6c7c341d9735289bb1b2573132f4996173e6ffca18559263b3599`

- (a) `-GateARole 'car'`: `Expected 1, because exactly one member (gateA) is role=gate with a returned record; a wrong count means role or state gating broke, but got 0.`
- (b) `-EmptyMembers $true`: `Expected a value, because carA is a declared member with a dispatched+returned record, but got $null or empty.`
- (c) `-IncludeGateBMember $false`: `Expected 'gateB' to be found in collection $null, because gateB is declared in the manifest but has zero dispatched/returned records anywhere in the store, but it was not found.`
- (d) `-IncludeCarBMember $false`: `Expected $true, because carB is claimed by train:probe-79's manifest membership, but got $false.`
- (guard) `-SecondManifestClaimsCarA $true`: `Expected 0, because observed board conditions: manifest-membership-collision, but got 1.`

**Two real problems found and fixed while building this** (both landed in the friction log, class-level):
1. A raw `.add_OutputDataReceived({...})` .NET event delegate on the spawned `board/server` process crashed the whole pwsh host (no runspace on the threadpool thread) — fixed with `Register-ObjectEvent`.
2. A just-killed process can transiently delete-lock its own `.exe` past `WaitForExit()` — a single `Remove-Item -ErrorAction SilentlyContinue` silently left debris in `%TEMP%`; fixed with a bounded 10×300ms retry, verified debris-free by explicit `Get-ChildItem` audit after the fix.

**Deviations/disclosures:** none against the brief's scope. `docs/setup.md`'s existing "Probe suite in CI" row (line 48) makes no specific-count claim invalidated by this landing, so it needed no edit — verified by grep before concluding this. No `board/`, `schema/`, or `scripts/tests/` file was read-write touched, only read (consistent with the scope boundary given #75 is live in `board/`).

```starcar-artifact
outcome: done
findings: Landed scripts/probes/ManifestBoardJoin.Probes.Tests.ps1 pinning #76's manifest-to-board join as a substrate probe: 5 new Its (gates lane, trains lane, declaredNotObserved, assigned, zero-conditions guard), all passing against the real board/server binary over a sealed scratch store. Non-vacuity proven for all five assertions via direct fault injection to this file, five quoted reds observed, all five reverted to byte-identical sha256 b21e0c5eb4b6c7c341d9735289bb1b2573132f4996173e6ffca18559263b3599. Two genuine PowerShell/Windows process-handling defects found and fixed during build (event-delegate runspace crash; delete-lock retry for no-debris compliance), both logged to docs/friction-log.md at the class level per standing doctrine. scripts/probes 24/24 to 29/29; scripts/tests unchanged (223/0/4, no files touched). No board/, schema/, or scripts/tests/ edits - scope boundary honored while #75 works live in board/. Committed c1c4d2dcee4023f721f2d5d433fe3bc2f3583dc1 on car/probe-79, not pushed.
abstract: Car probe-79 landed a pinned Pester substrate probe in scripts/probes/ that runs the real board/server binary against a sealed scratch store and asserts, by name, the four manifest-to-board join properties issue #76's owner-ruled PROBE RESULT comment measured (gates lane fill, trains consist rendering, declaredNotObserved completeness, dispatch-assigned flow), plus a zero-board-conditions non-vacuity guard. All five assertions were proven non-vacuous via direct, reverted fault injection to the fixture with quoted red messages and sha256 before/after checks. Two Windows/PowerShell process-handling defects (event-handler runspace crash, exe delete-lock on cleanup) were found and fixed in the process, with class-level friction-log entries. scripts/probes moved 24/24 to 29/29; scripts/tests untouched at 223/0/4. Committed locally on car/probe-79, never pushed.
task-id: probe-79-car-r1
```</result>
<usage><subagent_tokens>203651</subagent_tokens><tool_uses>112</tool_uses><duration_ms>1422735</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a353ad2db7e6ad627</task-id>
<tool-use-id>toolu_01G9PxxqMJQgoSyvgaGHg4GD</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\a353ad2db7e6ad627.output</output-file>
<status>completed</status>
<summary>Agent "Review #75 dispatch row titles" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result># VERDICT: **REJECT** (1 Major, 3 minor)

The engineering is good and the car's three contested judgments are all **correct** — I ruled for the car on every one, and proved two of them by fault injection rather than by reading. The REJECT rests on a single Major: an affirmative "documentation surface is clear" claim that covers a surface the car never opened, and which this diff does stale.

---

## BASE-DELTA (#13)

Base `bc4c28fc9e0c7fe14fbe6f78633664ebf1774d21` → HEAD `82f64b5f145885cee56d1337370dfe121d3e4d2b`. Verified FIRST: `git log -1 --format='%H'` → `82f64b5f145885cee56d1337370dfe121d3e4d2b`, detached (`git rev-parse --abbrev-ref HEAD` → `HEAD`), `git status --porcelain` empty. Worktree `C:\Users\Chris\git\starcar-wt\review-view-75`. `git diff bc4c28f..82f64b5 --stat` = 10 files, **+593/−13**, exactly as briefed. 4 commits: `d43dac8`, `c5a036d`, `b08bfff`, `82f64b5`.

---

## Suites — RUN BY ME at 82f64b5

| Suite | Observed | Car claimed |
|---|---|---|
| `go test ./...` (board) | **98 PASS / 0 FAIL / 0 SKIP** | 98/0 ✔ |
| `go vet ./...` | exit 0 | clean ✔ |
| Go fold conformance vectors | `TestFoldConformsToVectors` **18/18 subtests PASS** | — |
| Pester `scripts/tests/Detector.Tests.ps1` (pwsh vector runner) | **22/22** | — |
| `node --test` in `board/web` | **223 tests / 223 pass / 0 fail / 0 skipped** | 223/223 ✔ |
| `browser-dispatch-row-geometry.test.js` | **3/3 pass** | — |
| `browser-dispatch-row-taskid.test.js` | **5/5 pass** | — |
| Pester `scripts/tests` | **227 total / 222 passed / 0 failed / 5 skipped** | 227/223/0/4 |
| Pester `scripts/probes` | **24/24** | 24/24 ✔ |
| `CodeCitationPolicy.Tests.ps1` | **13/13** | 13/13 ✔ |
| `WirePayload.Tests.ps1` | **7/7** | — |
| `board/web/probe-yard-snapshot.mjs` | **PASS**, exit 0 | PASS ✔ |

**The extra skip, identified BY NAME as the brief required:** `run-session-start-guards.sh: agent-pull runner (#50).running the REAL guards prints [ci-baseline]'s marker ONLY when attached to a branch - conditional, never asserted as always-emitting`. Detached-HEAD-conditional, exactly as predicted. Every other car number re-derived exactly.

---

## ADJUDICATION 1 — the car was RIGHT to deviate. The conductor's premise was FALSE.

**Ruling: the car's honest stop was correct, and the brief's stated precedent does not exist.**

- `board/fold/fold.go:103-125` — `DispatchEntry` is `Subject, State, At, Superseded, winnerKind, Outcome, Spend, ElapsedSeconds, BudgetSeconds, BudgetSource`. **There is no `RecordDir` field.** A grep of `board/fold/*.go` for `recordDir|RecordDir` returns **zero** hits. The conductor's "recordDir is the precedent for that location" is disproved by opening the file.
- recordDir actually lives in `board/assemble`: `recordDirBySubject` at `assemble.go:346`, augmenting the wire map at `assemble.go:247-249`. The car put `taskId` in exactly the same place (`assemble.go:426`, `:250-252`).
- **The car's stated reason is also true.** `schema/vectors/README.md:4-9` — "the landed pwsh detector (`scripts/Detect-Dispatches.ps1`) and the Go port ... conform to THESE fixtures, and the D18 cross-verifier CI job runs BOTH against them, failing on any divergence"; step 3 of the runner contract compares `dispatches` **DEEP-EQUAL**. A Go-only field on `fold.DispatchEntry.MarshalJSON` would have broken every vector's `dispatches` comparison on one side of a two-implementation contract.
- **No leakage.** `git diff --name-only bc4c28f..82f64b5 -- board/fold scripts/` returns **empty**. Vectors still pass in both languages (Go 18/18 subtests; Pester Detector 22/22).

Had the car obeyed the brief, it would have shipped a cross-language contract break. Honest stop = success outcome, and it earned it.

## ADJUDICATION 2 — first-write-wins IS wrong here, and the car's matcher DOES pick the fold winner. Proven by injection.

**Ruling: correct, and stronger than the car argued.**

- `board/store/store.go:193` — `sort.Strings(paths)`. For one subject, `dispatched-*.json` sorts before `returned-*.json` (`d` &lt; `r`), so `recordDirBySubject`'s first-write-wins (`assemble.go:353-355`) would land on the task_id-less dispatched record.
- **I constructed the multi-record case and observed it** (reviewer-owned browser probe, real Go server, scratch store): subject `c0ffee1234567890abcdef0123456789` with `dispatched@09:10`, `returned@09:20 task_id=LOSER-earlier-task-id`, `returned@09:30 task_id=WINNER-later-task-id`. Observed wire: `"taskId":"WINNER-later-task-id"`. The **fold winner**, not first-or-last-scanned.
- **Fault injection at the Go layer**, disclosed below: removing only the `at == d.At` winner pin from `taskIDForReturnedSubject` produced
  `d["taskId"] = solo-1-earlier-task-id, want the WINNING (newer, 11:00) record's task_id "solo-1-winning-task-id"` — the guard fires with the exact wrong value, so it is not vacuous.
- **The one hole I looked for and did not find:** two returned records at the EXACT same `at`. Measured (scratch store, `returned-a.json` REJECT/`A-first-in-path-order`, `returned-b.json` APPROVE/`B-second-in-path-order`): wire renders `"outcome":"REJECT"` **and** `"taskId":"A-first-in-path-order"` — outcome and task-id come from the SAME record the fold chose. Coherent. No finding.

## ADJUDICATION 3 — the enumeration is COMPLETE.

**Ruling: correct.** I walked every identity-label render site in `board/web/js/`:

- `.solari-subject` (`dom-writer.js:552`) — treated ✔
- `.car-subject` (`dom-writer.js:459`) — treated, role-agnostic ✔
- `.signal-name` (`dom-writer.js:507`) renders `gate.name`, which is `render.js:352` ← `assemble.go:157` `Name: name` ← `assemble.go:131-134` `name := m.Gate; if name == "" { name = m.Role }` — a **declared** manifest gate name or role word, **never a subject hash**. Correct exclusion.
- Three further candidates I checked that the car did not enumerate, all correctly out of scope: `.declared-not-observed` (`dom-writer.js:481`) renders manifest members with **no dispatch entry at all**, so no `taskIDs` map entry can exist by construction (`assemble.go:426-430` iterates `in.Fold.Dispatches`); `.solari-superseded-entry`/`.car-superseded-entry` (`dom-writer.js:181`) render `kind at`, never a subject; `healthTrendBadgeText` (`dom-writer.js:85-99`) renders arrows/counts only. Nothing missed.

---

## THE WIRE SENTENCE — `task_id`, producer to pixel, every hop

1. **Producer, RETURNED only — CONFIRMED.** `scripts/Produce-Artifact.ps1:358` — `if ($Kind -eq 'returned' -and $taskId) { $record['task_id'] = $taskId }`. The `-and $taskId` guard also means an empty/whitespace value is never stamped. Sourced at `:243` from the envelope (`if ($env.PSObject.Properties['TaskId']) { $taskId = $env.TaskId }`), declared optional at `:240-242`. The `else` branch at `:265` is the dispatched path — it stamps `subject_basis`/`model`/`provenance`, **never `task_id`**. Issue #75's probe comment holds.
2. **Store parse.** `board/store/store.go:134` — `TaskID string \`json:"task_id"\`` (landed by #51), so the field is a *declared* key and does not fire `record-unrecognised-fields` (`store.go:310-324`). Content access is the generic decode at `store.go:263`, surfaced as `Record.Fields` (`store.go:308`).
3. **Resolution.** `assemble.go:61` `taskIDs := taskIDsBySubject(in.Records, in.Fold.Dispatches)` → `assemble.go:426` (skip any `d.State != "returned"`) → `assemble.go:450` `taskIDForReturnedSubject` matches `subject` + `kind=="returned"` + `at == the fold's own winning At`. **Single owner** — no second derivation anywhere (Law 6).
4. **Assemble/wire hop, two consumers of that one map.** Trains: `assemble.go:123` `TaskID: taskIDs[m.Subject]` → `types.go:36` `TaskID string \`json:"taskId,omitempty"\``. Dispatches: `assemble.go:193` passes `taskIDs[d.Subject]` into `dispatchWireMap`, which at `assemble.go:250-252` sets `m["taskId"]` **only when non-empty** — key omitted, never `""`.
5. **Schema constraint.** `schema/yard-snapshot.schema.json:131-134` (trains car) and `:207-210` (dispatch entry), both `{"type":"string"}`, both **outside** the unchanged `required` arrays (`["subject","role","state","at"]`, `["subject","state","at","assigned"]`). Absent-form validates unchanged — evidenced by `WirePayload.Tests.ps1` 7/7 and `probe-yard-snapshot.mjs` PASS (`sample-validates: PASS`, non-vacuity `rejected as expected`) against samples that carry no `taskId`.
6. **Client view model, null-not-undefined.** `render.js:320` `taskId: c.taskId ?? null`; `render.js:374` `taskId: d.taskId ?? null`.
7. **DOM render + fallback.** `dom-writer.js:459` `car.taskId ?? car.subject` then `carSubject.setAttribute('title', car.subject)`; `dom-writer.js:552` `d.taskId ?? d.subject` then `:559` `subject.setAttribute('title', d.subject)`.
8. **Provenance link is untouched by the label.** `links.js:16-19` `buildRecordLink(cfg, recordDir)` takes **only** `recordDir`; both call sites still pass `*.recordDir`. Observed, real browser, `STARCAR_GITHUB_REPO=polecatspeaks/StarCar STARCAR_GITHUB_REF=dev`: visible text `reviewer-75-handle-r1`, `title="reviewer75hashwithtaskid"`, `href="https://github.com/polecatspeaks/StarCar/tree/dev/&lt;prefix&gt;/reviewer75hashwithtaskid"`, `tag=A`, `class="solari-subject provenance-link"`. Unset → `tag=SPAN`, `href=null`. Both branches honest.
9. **Hand-maintained mirrors.** `WirePayload.Tests.ps1` carries no `taskId` sample — **I rule this NOT a finding**, and better-supported than the car argued: (a) `#28`'s `recordDir` set the identical precedent and carries no sample either; (b) `schema/yard-snapshot.schema.json` has **no `additionalProperties` anywhere** (grep: zero hits), so an added optional key cannot break any existing sample; (c) `dom-writer.js:150-162`'s own *measured* comment records that `$defs.lane.data` does not `$ref` these payload defs, so the browser's real validator applies **no** constraint to lane data either way. The new field's type constraint is therefore structurally inert at both runtimes; a pwsh sample would exercise nothing the Go `string` type does not already guarantee. NIRTS call: correct.

---

## RENDERING CHECK (#40) — real browser, real served board, OBSERVED values

Reviewer-owned Playwright probe against a real `board/server` Go binary and a real scratch store, viewport 1024×900.

**Wire, fetched by the browser itself:**
```
{"subject":"a103c...cafef00d","state":"returned","taskId":"view-75-extremely-long-human-task-id-handle-that-should-truncate-not-wrap-r1","taskIdPresent":true,"recordDir":"a103c...cafef00d"}
{"subject":"b7f19...facefeed","state":"returned","taskIdPresent":false,"recordDir":"b7f19...facefeed"}
```

**Branch A (task_id present, 76-char id):** `textContent` = `view-75-extremely-long-human-task-id-handle-that-should-truncate-not-wrap-r1`; `title` = `a103c4067cf9267a3deadbeefcafef00d` — **the hash, verbatim, character-for-character**. Computed `overflow: hidden`, `text-overflow: ellipsis`, parent `.solari-row-main` `white-space: nowrap`. Geometry: `clientWidth 123px`, `scrollWidth 713px` → **truncating = true**; `distinctTopCount = 1`; row height **38px**.

**Branch B (no task_id):** `textContent` = `b7f19e2c44aa1188ffeed00dfacefeed` = the subject exactly; `title` identical; `clientWidth 123 / scrollWidth 300`; `distinctTopCount = 1`; row height **38px** — **byte-identical treatment to pre-#75**.

**GEOMETRY REGRESSION — clear.** The 76-char task-id row and the hash-fallback row have the **same 38px height** and the same single-line cluster. **The long id truncates via ellipsis, it does not wrap — measured, not read.** `browser-dispatch-row-geometry.test.js` run by me: **3/3 pass**.

**Car chip:** `textContent` = `WINNER-later-task-id`, `title` = `c0ffee1234567890abcdef0123456789`. The chip carried no title attribute before #75; it does now. Hash reachable.

---

## NON-VACUITY (#40) — re-derived, plus four further injections

**Every injection is disclosed; every file restored and verified byte-identical by sha256; final `git status --porcelain` empty and HEAD unmoved.**

| # | Injection | Pre-sha256 | Observed | Restored |
|---|---|---|---|---|
| 1 | `git checkout bc4c28f -- board/web/js/dom-writer.js` (car's own) | `92258D44…C1BE` | taskid file **3 pass / 2 fail**; `dom-writer.test.js` **48 pass / 3 fail** | ✔ `92258D44…C1BE` |
| 2 | `d.taskId ?? d.subject` → `d.taskId` | `92258D44…C1BE` | taskid file **4 pass / 1 fail** — **test 2 reds** | ✔ `92258D44…C1BE` |
| 3 | strip `text-overflow: ellipsis` from `board.css` `.solari-subject` | `B4860579…0E6B` | **test 3 reds** | ✔ `B4860579…0E6B` |
| 4 | drop `m["taskId"] = taskID` from `dispatchWireMap` | `F53C0CAF…4EE0` | `d["taskId"] = &lt;nil&gt;, want … "solo-1-winning-task-id"` | ✔ `F53C0CAF…4EE0` |
| 5 | blank `taskIDForReturnedSubject` → `return ""` | `F53C0CAF…4EE0` | 2 FAIL: `d["taskId"] = &lt;nil&gt;` and `car.TaskID = "", want "carA-task-id"` | ✔ `F53C0CAF…4EE0` |
| 6 | drop the `at == at` winner pin (first-match-wins) | `F53C0CAF…4EE0` | `d["taskId"] = solo-1-earlier-task-id` | ✔ `F53C0CAF…4EE0` |

**Ruling on the 3 vacuous passes: honestly scoped, none decorative.** Test 2 guards the fallback and reds under injection 2. Test 3 guards board.css and reds under injection 3. Test 5 guards geometry (and I independently exercised it with a 76-char id). Each is non-vacuous against *its own* defect class; none of them *should* red on a dom-writer revert, and the car said so.

**Go injections red for their stated reasons** (injections 4–6). Note the correct scoping: injection 4 reds only the wire test (the trains-lane test reads `car.TaskID`, a different path), and the three absence-asserting tests correctly stay green — they assert an absent key, which the injection satisfies.

**Honest correction to my own evidence (#13):** in the *full-suite* run under injection 1 I saw 9 failures, four of which were `browser-health-trend-cascade.test.js`. Re-running that file **alone** under the identical injection gave **4/4 pass** — those four were a **flake** from concurrent Chromium+Go-server load, not an injection consequence. Reported rather than quietly dropped.

---

## CITATION CHECK (#42) — clear

`CodeCitationPolicy.Tests.ps1` **13/13 pass**. The one new file, `board/web/test/browser-dispatch-row-taskid.test.js:1`, opens with `// browser-dispatch-row-taskid.test.js (#75) …`. All 10 changed files carry `#75` (counts 2–13 each). **No `file:line` citation was added anywhere in the diff** (grep of added lines for `\.(go|js|ps1|json|md|css):\d+` → zero hits), so the symbol-form rule this family paid two Majors for is not implicated.

---

## FINDINGS

### R1-M1 (MAJOR) — the "documentation surface is clear" claim omits `docs/screenshots/`, which this diff does stale

The car reports it searched design, spec, contracts, glossary, README and setup docs and found nothing stale. That enumeration never includes `docs/screenshots/`, and CLAUDE.md's NORTH STAR lists screenshots **by name** as user-facing documentation in scope, then names screenshot drift as "both the likeliest and the most embarrassing rot this repo can ship."

Evidence, all re-derivable:
- `board/web/verify-registers.mjs:104` — `await captureState(browser, 'calm-yard', {})`, **no `storePath` override**, so `calm-yard.png` is captured against the **ambient real `artifacts/` store**. `:105` `hot-yard` uses `buildScratchStoreWithInFlightDispatch()`, which `cpSync`s that same real store (`real-board-server.js:61`).
- Measured against the real store in this worktree: **84 of 248 records carry `task_id`**, including `a076bf6c0a94e302f → process-42-car-r1 / -r2 / -r3`.
- I opened both images. `docs/screenshots/2026-07-26-register-check-candidates/calm-yard.png` and `docs/screenshots/2026-07-26-view-28-12-candidates/calm-yard.png` **both** show a dispatches row rendered `a076bf6c0a94e…`. After this train the identical capture renders `process-42-car-r3`. The images no longer depict what the code draws for the exact surface this train redefines — and it is the exact hash-as-identity complaint issue #75 was filed to remove.
- **The house has a landed, same-class remedy from this same train family, one day old:** `docs/screenshots/2026-07-26-view-69-71-candidates/README.md` carries a `**DISCLOSED STALENESS (fix cycle round 2, MAJOR-R1-1, 2026-07-27):**` block for a rendering its images "also incidentally show." That precedent was created under a Major. `2026-07-26-register-check-candidates/` has **no README at all** in which such a note could live, and the four preceding view trains (#62, #28/#12, #67, #69/#71) each captured their own evidence directory; #75 captured none.

**The counter-argument, stated so the owner can rule cheaply rather than re-derive it:** these are dated per-train evidence captures; the only prose that cites one (`docs/design/mockups/2026-07-26-claude-design-board/README.md:65`, a 1920×1080 responsive-layout claim) remains **true**; and an ambient-store capture is data-stale on every commit that lands a record, which no rule requires recapturing. I still rule Major, because what changed here is the **rendering rule**, not the data — precisely the class the #69/#71 README disclosed rather than dismissed — and because the report's clearance is an affirmative claim over a surface that was never opened. Remedy is cheap: the #69/#71-shaped disclosure note, same commit.

### R1-m1 (minor) — a schema-valid `taskId: ""` renders a BLANK identity label

Measured, real browser, real served modules (`/js/render.js`, `/js/dom-writer.js`):

```
EMPTY-STRING   viewModelTaskId=""              renderedText=""                    title="a103c4067cf9267a3"
ABSENT         viewModelTaskId=null            renderedText="a103c4067cf9267a3"   title="a103c4067cf9267a3"
REAL           viewModelTaskId="view-75-car-r1" renderedText="view-75-car-r1"     title="a103c4067cf9267a3"
```

`schema/yard-snapshot.schema.json:132,208` declare `{"type":"string"}` with no `minLength`, and the schema has no `additionalProperties` gate anywhere, so `""` is wire-valid; `??` at `render.js:374` and `dom-writer.js:552` does not treat `""` as absent. The row loses its visible identity entirely. **Not Major:** unreachable through the only shipped producer (`assemble.go:250` guards `taskID != ""`; `types.go:36` is `omitempty`). Worth fixing because the sibling field's guard at `links.js:17` (`!recordDir`) *is* truthiness-based, so #75 introduced an inconsistency with its own nearest precedent; the fix is one character.

### R1-m2 (minor) — `browser-dispatch-row-taskid.test.js:57` is named for a scenario its fixture does not construct

The test is titled *"a **long** task-id still gets `.solari-subject`'s ellipsis truncation"*, but `buildScratchStoreForTaskIdTitles` (`real-board-server.js:472`) seeds `task_id: 'view-75-car-r1'` — **14 characters**, shorter than the 20-char hash it replaces. Measured in that fixture: `clientWidth 123 / scrollWidth 131` — it overflows by **8px**, by accident of the 13rem grid column, not because the id is long. The assertion itself is length-**independent** (computed `overflow`/`text-overflow` only), so it cannot fail for a length reason. No Major, because I independently measured the genuinely long case clean (76 chars: `scrollWidth 713 / clientWidth 123`, `distinctTopCount 1`, row height unchanged at 38px) — but the name promises coverage the assertion does not deliver, which is the instrument-honesty class this shop guards.

### R1-m3 (nit) — a comment left stale by the commit that invalidated it, 24 lines above the change

`board/web/js/dom-writer.js:528-529` still reads *"DISPATCHES: … dense monospace rows: **subject**, state word, elapsed"*. As of `:552` the first cell is task-id-or-subject. Same-file, same-function, same commit.

---

## CONSTITUTION CHECK

- **Law 1 (never a confident falsehood; unknown renders as unknown)** — HONORED on the shipped path, with one latent gap. Absent `task_id` renders the hash: observed `textContent == "b7f19e2c44aa1188ffeed00dfacefeed"` for a returned record carrying none, and the key is omitted entirely at `assemble.go:250-252` rather than emitted as `""`; `render.js:374`'s `?? null` keeps null distinct from undefined (`render.test.js` pins `null, 'absent wire taskId must render as null, never undefined'`). **Gap: R1-m1** — a schema-valid `""` renders an empty label.
- **Law 6 (no second copy)** — HONORED. `taskIDsBySubject` (`assemble.go:426`) is the single resolution owner; both consumers (`assemble.go:123`, `:193`) read the same map; the client never re-derives from `subject`. `dispatchWireMap` still delegates the conditional key set to `fold.DispatchEntry.MarshalJSON` (`assemble.go:236`).
- **Law 7 (no hardcoded taxonomy)** — HONORED. `taskId` is an opaque pass-through string with no enumeration. The `State != "returned"` / `kind != "returned"` literals at `assemble.go:429,443` are the fold's closed-by-mechanism liveness gradient, identical to the pre-existing `findingsForReturnedSubject` at `assemble.go:365+`, not a new open taxonomy.
- **Law 4 (never silently dropped)** — HONORED. The subject hash survives verbatim in the `title` attribute on **both** surfaces (`dom-writer.js:460`, `:559`), observed in a real browser; the provenance link still resolves by `recordDir`, observed.
- **NORTH STAR, documents rank equal to code** — **VIOLATED**, see R1-M1.
- **#42 code citation** — HONORED, gate green 13/13.
- **TDD red-first** — HONORED and re-derived: six independent injections, all red for their stated reasons.
- **Dispatch hygiene (no push, no shared checkout)** — HONORED by me: nothing committed, nothing pushed, all six injections restored byte-identical by sha256, `git status --porcelain` empty at `82f64b5`, shared checkout never written.

**Unreproducible-evidence callout (#13):** none. Every finding above rests on a command or measurement in this report. The one thing I could *not* fully re-derive — the four `#12` failures inside my injected full-suite run — I chased down and reported as a flake rather than leaving it as evidence.

**To clear:** land the screenshot-staleness disclosure (R1-M1) in the fix commit; R1-m1/m2/m3 are cheap and I would take them in the same pass, but only R1-M1 is binding.

Files: `C:\Users\Chris\git\starcar-wt\review-view-75\board\assemble\assemble.go`, `...\board\web\js\dom-writer.js`, `...\board\web\js\render.js`, `...\board\web\test\browser-dispatch-row-taskid.test.js`, `...\board\web\test\support\real-board-server.js`, `...\board\web\verify-registers.mjs`, `...\schema\yard-snapshot.schema.json`, `...\docs\screenshots\2026-07-26-register-check-candidates\calm-yard.png`, `...\docs\screenshots\2026-07-26-view-28-12-candidates\calm-yard.png`, `...\docs\screenshots\2026-07-26-view-69-71-candidates\README.md`

```starcar-artifact
outcome: REJECT
findings: 1 Major, 3 minor. R1-M1 (MAJOR): the car's "documentation surface checked and clear" claim omits docs/screenshots/, which this diff does stale - board/web/verify-registers.mjs:104 captures calm-yard against the AMBIENT real artifacts store; that store holds 84 of 248 records carrying task_id including a076bf6c0a94e302f mapping to process-42-car-r3, whose row is visibly rendered as the hash "a076bf6c0a94e..." in both docs/screenshots/2026-07-26-register-check-candidates/calm-yard.png and docs/screenshots/2026-07-26-view-28-12-candidates/calm-yard.png (images opened and read, not inferred); the house has a landed same-class remedy one day old in docs/screenshots/2026-07-26-view-69-71-candidates/README.md's DISCLOSED STALENESS block, created under MAJOR-R1-1, and the register-check directory has no README at all; counter-argument stated in full in the verdict for cheap owner adjudication. R1-m1 (minor): a schema-valid taskId of empty string renders a BLANK identity label - measured in a real browser against the real served modules, EMPTY-STRING gives renderedText of empty string while ABSENT correctly gives the hash; schema/yard-snapshot.schema.json lines 132 and 208 declare type string with no minLength and the schema has no additionalProperties gate anywhere, and the nullish-coalescing operator does not treat empty string as absent, whereas the sibling recordDir path at links.js:17 uses a truthiness guard that does; unreachable through the only shipped producer (assemble.go:250 guards non-empty, types.go:36 is omitempty) so not Major. R1-m2 (minor): browser-dispatch-row-taskid.test.js:57 is titled "a LONG task-id still gets ellipsis truncation" but its fixture seeds a 14-character task_id (real-board-server.js:472) that overflows by only 8px by accident of the 13rem grid column, and the assertion is length-independent (computed style only); no Major because the reviewer independently measured a 76-character task-id truncating cleanly (scrollWidth 713 vs clientWidth 123, one visual line, row height 38px identical to the fallback row). R1-m3 (nit): board/web/js/dom-writer.js:528-529's section comment still says rows show "subject, state word, elapsed", stale as of line 552 in the same commit. ALL THREE ADJUDICATIONS RULED FOR THE CAR. Adjudication 1: the conductor's premise was FALSE - board/fold/fold.go:103-125's DispatchEntry has no RecordDir field and grep of board/fold for recordDir returns zero hits; the car's chosen layer (board/assemble) is correct and its stated reason true - schema/vectors/README.md:4-9 confirms both the pwsh detector and the Go port conform to the vectors under a D18 cross-verifier with DEEP-EQUAL comparison of dispatches, so a Go-only field would have broken a two-implementation contract; board/fold and scripts/ are untouched in the diff and vectors pass in both languages. Adjudication 2: first-write-wins IS wrong (store.go:193 sorts by path, dispatched sorts before returned) and the car's matcher DOES select the fold winner - constructed a three-record case and observed WINNER-later-task-id on the wire, then fault-injected removal of the at-winner pin and observed the guard fire with the exact wrong value solo-1-earlier-task-id; also probed the exact-at-tie hole and found outcome and task-id come from the same record. Adjudication 3: enumeration COMPLETE - signal-name renders gate.name which traces to the manifest's declared gate or role word (assemble.go:131-134,157), never a hash; three further candidates checked (declared-not-observed, superseded entries, health-trend badge) all correctly out of scope. Suites re-derived at 82f64b5 - Go 98 PASS 0 FAIL vet clean, fold vectors 18 of 18 Go subtests plus Detector 22 of 22 pwsh, node board/web 223 of 223, geometry test 3 of 3, taskid test 5 of 5, Pester scripts/tests 227 total 222 passed 0 failed 5 skipped with the extra skip named as the detached-HEAD-conditional session-start-guards ci-baseline marker test, probes 24 of 24, CodeCitationPolicy 13 of 13, WirePayload 7 of 7, probe-yard-snapshot PASS. Six fault injections all restored byte-identical by sha256 with the tree clean at 82f64b5.
abstract: Adversarial sentence-check review of view train #75 (task-id handle threaded to the dispatch-row and car-chip titles with an honest hash fallback), base bc4c28f to head 82f64b5, 10 files +593/-13. REJECT on one Major. The engineering is sound and all three contested car judgments were ruled correct, two of them proven by fault injection rather than reading - notably the car's honest stop against a defective brief, whose stated precedent (recordDir on fold.DispatchEntry) does not exist in the code and whose instruction would have put a Go-only field into a deep-equal cross-language conformance contract. The full wire sentence was traced hop by hop with file:line from the pwsh producer's returned-only stamping through store parse, resolution, wire map, schema, view model null-vs-undefined, DOM render and provenance link, and both rendering branches plus a 76-character task-id were measured in a real browser against a real Go server (truncation not wrap, identical 38px row geometry, subject hash verbatim in the tooltip, link still keyed by recordDir). The Major is a documentation-surface defect: an affirmative "checked and clear" claim that never opened docs/screenshots/, where two committed board images captured against the ambient real store depict the exact hash-as-identity rendering this train replaces, with a landed same-class disclosure precedent from this train family one day old.
task-id: view-75-review-r1
```</result>
<usage><subagent_tokens>224239</subagent_tokens><tool_uses>124</tool_uses><duration_ms>1644184</duration_ms></usage>
</task-notification>

---

ok since we keep bumping our heads into this, should this be part of each reviewers brief to ensure they hold to it?

---

I lean a, and here's why. Screenshots are a validation device. They exist to validate changes at the time of change, not keep an on going tally of the screen rendering. So other than using it for validations purposes, I don't see a reason why it would be a living document, nor how it can be.

---

<task-notification>
<task-id>ae9c59ae16e969916</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\ae9c59ae16e969916.output</output-file>
<status>completed</status>
<summary>Agent "Car #75 dispatch row titles" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>All confirmed. Final report:

## Fix cycle round 2 report

**Base verified:** worktree `C:\Users\Chris\git\starcar-wt\car-view-75`, branch `car/view-75`, `git log -1 --format='%H'` → `82f64b5f145885cee56d1337370dfe121d3e4d2b` (my own prior HEAD), tree clean — matched before any edit.

**Final HEAD:** `5d4baaec05b0aaafe6375536452747b2f23d40b0`, 2 new commits, tree clean.

### R1-M1 (MAJOR) — verified before writing, then disclosed. Commit `a2e0068`.

I did not write the note on the strength of the verdict's claim; I re-derived every element:
- `board/web/verify-registers.mjs:104` — `captureState(browser, 'calm-yard', {})`, confirmed no `storePath` override → captures the ambient real store. Also traced `hot-yard.png`'s builder (`buildScratchStoreWithInFlightDispatch`, `real-board-server.js:61`) and found it `cpSync`s the same ambient store — a broader match than the verdict's calm-yard-only citation, so I disclosed **both** images in **both** directories (4 PNGs total), not just the one the verdict named.
- Walked the real `artifacts/` store myself: **248 total records, 84 carry `task_id`**, and `a076bf6c0a94e302f` has three `returned` records, the latest (`2026-07-26T17:28:56Z`) carrying `task_id: "process-42-car-r3"` — matches the verdict exactly.
- Opened all four PNGs (`Read` tool, visual inspection): all four show the dispatches-lane row for that subject rendered as `a076bf6c0a94e...`, confirmed.
- Confirmed neither `2026-07-26-register-check-candidates/` nor `2026-07-26-view-28-12-candidates/` had a README before this commit (`ls`).
- Read `docs/screenshots/2026-07-26-view-69-71-candidates/README.md` in full to match its DISCLOSED STALENESS shape.
- Checked `health-trend-and-provenance.png`'s own builder (`verify-registers.mjs:117`, `buildScratchStoreWithHealthTrendFamilies()`) — self-contained, no ambient-store path — and confirmed it is **unaffected**, stating that explicitly in the new README rather than silently.

Two new READMEs created (images **not** recaptured, per instruction).

### R1-m1 (Law 1) — one-line-per-field fix, pinned red-first. Commit `5d4baae`.

Red: `#75 fix cycle r2 (R1-m1, Law 1): a car with a wire taskId of ""...` → `'' !== null`; dispatches-lane equivalent same failure. Fixed `render.js`'s `taskId: c.taskId ?? null` / `d.taskId ?? null` → `|| null` (both sites), matching `links.js:17`'s truthiness guard. Green: `render.test.js` 49/49.

### R1-m2 (instrument honesty) — same commit.

Seeded a genuinely long task_id (79 chars, `real-board-server.js`) replacing the 14-char one that overflowed only by grid-column accident; renamed the test and added a length-dependent assertion (`scrollWidth &gt; clientWidth`) alongside the existing computed-style checks, so it can no longer pass for the wrong reason. Green: `browser-dispatch-row-taskid.test.js` 5/5 (measured `scrollWidth`/`clientWidth` genuinely differ this time, not merely styled).

### R1-m3 (nit) — same commit.

`dom-writer.js:528-529`'s stale "subject, state word, elapsed" comment corrected in place, same file, same function.

### Suites at final HEAD `5d4baae`

Node `board/web`: **225/225** (223 baseline + 2 new R1-m1 tests). `browser-dispatch-row-taskid.test.js` run alone: **5/5**. `browser-dispatch-row-geometry.test.js`: **3/3**. Go `board/...`: **98 PASS, 0 FAIL**, `go vet ./...` clean (untouched this round). Pester `scripts/tests`: **227/223/0/4** (untouched). `CodeCitationPolicy.Tests.ps1`: **13/13**. `probe-yard-snapshot.mjs`: **PASS**.

### Deviation disclosed

One full-suite run of `board/web` (all files together) showed `browser-dispatch-row-taskid.test.js`'s last test failing with `bind: Only one usage of each socket address ... is normally permitted` — a port-collision, not a code defect. Re-ran the file alone: **5/5 pass**. Re-ran the full suite again: **225/225 clean**. This is the same class of flake the reviewer already disclosed for `browser-health-trend-cascade.test.js` under concurrent Chromium+Go-server load — recognized as such, not chased, per the coordinator's own note.

Relevant files (absolute paths under `C:\Users\Chris\git\starcar-wt\car-view-75\`): `board/web/js/render.js`, `board/web/js/dom-writer.js`, `board/web/test/render.test.js`, `board/web/test/browser-dispatch-row-taskid.test.js`, `board/web/test/support/real-board-server.js`, `docs/screenshots/2026-07-26-register-check-candidates/README.md` (new), `docs/screenshots/2026-07-26-view-28-12-candidates/README.md` (new).

```starcar-artifact
outcome: done
findings: R1-M1 (Major, screenshot staleness) resolved: independently verified all of the reviewer's claims (verify-registers.mjs:104 no storePath override, 84/248 real records carry task_id, a076bf6c0a94e302f maps to process-42-car-r3, both PNGs visually confirmed) before writing, and extended the disclosure beyond what was cited - found hot-yard.png in both directories is also affected via the same ambient-store cpSync mechanism, and confirmed health-trend-and-provenance.png is unaffected - stating each explicitly rather than assuming. R1-m1 (Law 1, empty-string taskId) fixed one character in two sites, pinned red-first (two new tests, both observed '' !== null pre-fix). R1-m2 (instrument honesty) fixed by seeding a genuinely long task-id and adding a length-dependent scrollWidth-vs-clientWidth assertion so the test cannot pass for the wrong reason again. R1-m3 (stale comment) corrected. One transient full-suite port-collision flake observed and reproduced-away (5/5 alone, 225/225 on full-suite re-run) - recognized as the same load-flake class the reviewer already disclosed, not a regression.
abstract: Fix cycle round 2 on view train #75 addressed a REJECT (1 Major, 3 minor) where all three of the car's contested engineering judgments were upheld by the reviewer. Landed the screenshot-staleness disclosure the reviewer required (two new READMEs, images left uncaptured per instruction, every claim independently re-verified against the real store and the real images before writing), fixed the empty-string Law 1 gap to match the sibling recordDir field's truthiness convention, corrected an instrument-honesty gap in the long-task-id truncation test by making its fixture and assertion actually earn the test's name, and fixed one stale comment - all red-first where a test was possible, with full suites re-derived at final HEAD.
task-id: view-75-car-r2
```</result>
<usage><subagent_tokens>333325</subagent_tokens><tool_uses>223</tool_uses><duration_ms>666242</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a7b9079a789c94890</task-id>
<tool-use-id>toolu_01Bo64kmoM96GfEJzPS2xJDf</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\a7b9079a789c94890.output</output-file>
<status>completed</status>
<summary>Agent "Car #79 pin manifest-board join probe" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Committed cleanly at `a3579c4bd93839c40490dffbf28590fbcf81361c` on `car/probe-79`, working tree clean, base `c1c4d2dcee4023f721f2d5d433fe3bc2f3583dc1` verified before editing.

## Fix cycle round 2 report

**Base verified:** worktree `C:\Users\Chris\git\starcar-wt\car-probe-79`, branch `car/probe-79`, HEAD `c1c4d2dcee4023f721f2d5d433fe3bc2f3583dc1`, `git status --porcelain` empty before editing — matched the brief.

**Verdict read in full:** `C:\Users\Chris\git\starcar\artifacts\reviews\2026-07-27-probe-79-review-r1-REJECT.md` (read-only, outside worktree, as instructed).

### Per finding

**R1-M1 (MAJOR, Law 1)** — verified myself before touching anything: opened `board/server/wireschema_test.go:20-152` and confirmed it writes a plan-shaped manifest as real files via `os.WriteFile` (`board/server/poll_test.go:23-32`), calls `srv.PollOnce` (`board/server/poll.go:253`, the real scan-through-fold-through-assemble pipeline), and asserts `len(gatesPayload.Gates) != 1` / `RecordDir`/`Findings` (`:129-137`) and `len(trainsPayload.Trains) != 1 || len(...Tickets) != 2` / `carA.RecordDir` (`:112-123`). Ran it myself: `go test ./server -run TestAssembledSnapshotValidatesAgainstWireSchema -v` → `--- PASS: TestAssembledSnapshotValidatesAgainstWireSchema (0.02s)`, matching the reviewer's observation exactly. My header's "nothing asserts that join mechanically" claim was false. Rewrote the WHY-THIS-EXISTS paragraph to name the file by path, state precisely what it does and does not assert, and adopt issue #79's own accurate "TOGETHER" framing instead of the flattened absolute.

**R1-m1 (minor)** — fixed: the NON-VACUITY paragraph's "the four quoted red messages" now correctly reads "all five" (rewritten alongside R1-n2's fix, same paragraph).

**R1-m2 (minor)** — verified myself: `grep -n` confirmed `.github/workflows/ci.yml:214` ("Run substrate-floor probes") precedes `:263` ("Set up Go (board/)"), both matrix legs (`:115-116`). Added one disclosure bullet to PROBED FACTS naming this dependency and its consequence (hard-fail, not skip, if Go is absent). Did not touch `ci.yml`, per the brief.

**R1-m3 (minor)** — verified myself before writing the sentence (behavioral claim, run don't read): temporarily replaced the computed digest with `'sha256:' + ('0'*64)` in `New-Sealed`, ran the suite: **5 passed / 0 failed**, confirming the seal is unguarded. Reverted; sha256 matched base (`b21e0c5e...3599`) before/after. Added a scoping paragraph directly above `New-Sealed` stating the suite pins the join, never the seal.

**R1-n1 (nit)** — fixed: `typedRecord` field enumeration now lists `producer`.

**R1-n2 (nit)** — fixed: non-vacuity note now points at the re-derivable `New-ScratchStore` switches themselves and at `artifacts/reviews/2026-07-27-probe-79-review-r1-REJECT.md` (confirmed this file does not yet exist in my worktree/branch — `ls` failed, `git log --all` shows it landed via commit `3eb1bf3` on `dev` after my branch was cut, per the brief's own note — the citation is a stable repo path that resolves once merged, matching the reviewer's own external-doc citation convention).

**Self-found while fixing R1-n2:** the header cited fault-injection (a)'s parameter as `GatesRoleOverride`; the real parameter is `GateARole` (`scripts/probes/ManifestBoardJoin.Probes.Tests.ps1:164`, unchanged). Since I'd just written "re-derivable straight from the switches quoted below," that claim needed to be true — fixed the citation.

### What was NOT changed
No assertion logic was altered — the diff is comments only (`git diff` confirms every changed line is a `#` comment or absent). `docs/friction-log.md` was left untouched this round per the coordinator's note (diverged on `dev`, merge is the conductor's to resolve by union). Nothing under `board/`, `schema/`, or `scripts/tests/` touched.

### Suite totals
- `scripts/probes` at `c1c4d2d` (round-1 baseline, my own run): 29/29.
- `scripts/probes` post-fix (own run): **29/29 unchanged**.
- `scripts/tests` at `c1c4d2d` (round-1 baseline): 223 passed / 0 failed / 4 skipped.
- `scripts/tests` post-fix (re-derived, own run, attached branch): **223 passed / 0 failed / 4 skipped, unchanged** — reconciles with the reviewer's 222/0/5 on a detached HEAD (one delta test, `SessionStartGuardRunner.Tests.ps1`'s branch-conditional case, matching the reviewer's own reconciliation).
- `scripts/tests/CodeCitationPolicy.Tests.ps1`: 13/13 green (re-run after edits).
- No orphan `board-server-probe.exe` process, no leftover `%TEMP%\mbj-probe-*` directories, verified after every run.

Final file sha256: `664f20cfbf3ee7cac88542593b2ea504479632c61732be7867f44ce5bfbbe517`.

```starcar-artifact
outcome: done
findings: All 6 findings from artifacts/reviews/2026-07-27-probe-79-review-r1-REJECT.md addressed as documentation-only fixes; no assertion logic touched, matching the reviewer's own conclusion that the mechanism is sound. R1-M1 (MAJOR, Law 1) verified myself before rewriting - opened board/server/wireschema_test.go:20-152, confirmed it writes a real on-disk store and asserts gates/trains-lane properties via the real PollOnce pipeline, ran go test ./server -run TestAssembledSnapshotValidatesAgainstWireSchema myself and observed PASS in 0.02s matching the reviewer's claim exactly - my header's "nothing asserts that join mechanically" was false and is now corrected to name the file, state what it does/does not assert, and adopt issue #79's own accurate "TOGETHER" framing. R1-m1 four to five fixed. R1-m2 disclosed CI step-ordering dependency (ci.yml:214 before :263) without editing ci.yml. R1-m3 verified myself (all-zero digest still 5/5 green) before writing the scoping sentence that the suite pins the join, never the seal. R1-n1 producer added to the field enumeration. R1-n2 non-vacuity evidence repointed at the re-derivable fixture switches and the landed verdict path. Self-found and fixed while addressing R1-n2: a stale parameter-name citation (GatesRoleOverride vs the real GateARole) that would have made the "re-derivable from the switches quoted below" claim false. Suites: scripts/probes 29/29 (own run, both before and after), scripts/tests 223 passed/0 failed/4 skipped (own run, unchanged, reconciles with reviewer's detached-HEAD 222/0/5 by the same named delta test). CodeCitationPolicy 13/13. No orphan processes or temp debris. docs/friction-log.md deliberately untouched this round per the conductor's note about its dev-branch divergence.
abstract: Car addressed fix-cycle round 2 (review artifacts/reviews/2026-07-27-probe-79-review-r1-REJECT.md, REJECT, 1 Major/3 minor/2 nits) on scripts/probes/ManifestBoardJoin.Probes.Tests.ps1 with documentation-only edits, verifying every finding against real code (running the cited Go test, running the suite under each claimed fault condition) before writing any corrected text, rather than trusting the verdict's prose. The binding Major (a false coverage claim contradicted by a passing board/server/wireschema_test.go) was rewritten to name the file, state precisely what it does and does not assert, and match issue #79's own accurate narrower wording. All three minors and two nits were fixed, plus one additional citation bug (a stale parameter name) found and fixed in the course of addressing a nit. No assertion logic changed. scripts/probes stayed 29/29 and scripts/tests stayed 223/0/4 across the round, both re-derived by the car's own runs. Committed a3579c4bd93839c40490dffbf28590fbcf81361c on car/probe-79, not pushed.
task-id: probe-79-car-r2
```</result>
<usage><subagent_tokens>251368</subagent_tokens><tool_uses>151</tool_uses><duration_ms>466136</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>bsdr7xe7n</task-id>
<tool-use-id>toolu_01UcumTkCwSpCrK2JuuMfrZR</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\bsdr7xe7n.output</output-file>
<status>completed</status>
<summary>Background command "Commit friction row and watch CI (background)" completed (exit code 0)</summary>
</task-notification>

---

<task-notification>
<task-id>a353ad2db7e6ad627</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\a353ad2db7e6ad627.output</output-file>
<status>completed</status>
<summary>Agent "Review #75 dispatch row titles" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result># VERDICT: **REJECT** (1 Major, 3 minor) — with a swirl cap set

The code half has converged and is clean. The Major is in the new documentation the round-1 fix produced: a caption that claims an image proves something the image does not show, contradicted by the very source file the same README cites two lines earlier.

**One thing to say plainly first, without hedging:** on R1-M1 the car went *beyond* my citation and was right to. I cited two images; it found four, correctly reasoned that `hot-yard.png` inherits the ambient store via `cpSync`, and volunteered an explicit "unaffected" ruling on the fifth. I verified all three parts independently. That is the behaviour this shop wants.

---

## BASE-DELTA (#13)

Round-1 reviewed HEAD `82f64b5f145885cee56d1337370dfe121d3e4d2b` → round-2 HEAD `5d4baaec05b0aaafe6375536452747b2f23d40b0`. Verified FIRST: `git log -1 --format='%H'` → `5d4baaec05b0aaafe6375536452747b2f23d40b0`, detached, `git status --porcelain` empty. `git diff 82f64b5..5d4baae --stat` = **7 files, +174/−9**. Two commits: `a2e0068` (R1-M1), `5d4baae` (R1-m1/m2/m3). **No Go files in the delta** — `git diff --name-only 82f64b5..5d4baae -- '*.go'` returns empty, as claimed.

## Suites — RUN BY ME at 5d4baae

| Suite | Observed | Car claimed |
|---|---|---|
| `node --test` board/web | **225 tests / 225 pass / 0 fail / 0 skipped** | 225/225 ✔ (223 baseline + 2 new ✔) |
| `browser-dispatch-row-taskid.test.js` alone | **5/5** | 5/5 ✔ |
| `browser-dispatch-row-geometry.test.js` | **3/3** | 3/3 ✔ |
| `go test ./...` / `go vet ./...` | **98 PASS / 0 FAIL / 0 SKIP**, vet exit 0 | 98, clean ✔ |
| Pester `scripts/tests` | **227 total / 222 passed / 0 failed / 5 skipped** | 223/0/4 attached |
| `CodeCitationPolicy.Tests.ps1` | **13/13** | 13/13 ✔ |
| `DocPolicy.Tests.ps1` | **2/2** | — |
| `CitationResolverPolicy` (in the 227) | pass — scans all `git ls-files` minus `artifacts/**`, so both new READMEs' `file:line` citations were mechanically resolved | — |
| `probe-yard-snapshot.mjs` | **PASS**, exit 0 | PASS ✔ |

**The extra skip, named:** `run-session-start-guards.sh: agent-pull runner (#50).running the REAL guards prints [ci-baseline]'s marker ONLY when attached to a branch` — the same detached-HEAD-conditional test as round 1.

---

## THE FOUR-ID WALK

| ID | Round-1 finding | Status | Evidence |
|---|---|---|---|
| **R1-M1** | doc-clearance claim omitted `docs/screenshots/` | **Present** (fix correct + correctly broadened) — **but the fix introduced R2-M1** | see below |
| **R1-m1** | schema-valid `taskId: ""` renders a blank label | **Present** — fixed, provably | both new pins red on revert; falsy sweep clean |
| **R1-m2** | test named for a scenario its fixture does not construct | **DRIFTED** → R2-m1 | direction-B measurement below |
| **R1-m3** | stale `renderDispatches` comment | **Present** | `dom-writer.js:528-532` now reads "identity (task-id when the wire has one, else the record-dir hash subject…)" — accurate |

### R1-M1 — all three parts verified independently

**(a) Is the broadened scope CORRECT?** **Yes.** `board/web/verify-registers.mjs:105` → `captureState(browser, 'hot-yard', { storePath: buildScratchStoreWithInFlightDispatch() })`, and `real-board-server.js:61` → `cpSync(join(REPO_ROOT, 'artifacts'), storeDir, { recursive: true })`. Both cited lines say exactly what the READMEs claim. I opened **both** `hot-yard.png` files: each shows `a076bf6c0a94e…` in the **bottom-right cell of the dispatches grid**, exactly as captioned. Four images affected, not my two.

**(b) Is the "unaffected" claim TRUE?** **Yes — observed, not read.** An absence claim gets measured, so I ran `buildScratchStoreWithHealthTrendFamilies` through the real Go server and dumped the wire: all 5 dispatch rows and all 5 train cars report `taskIdPresent=false`. No `cpSync`, no `REPO_ROOT` in the builder. Claim holds.

**(c) Do the new READMEs accurately describe their images?** **Mostly — one Major and one minor.** What I verified as TRUE: capture commits `45b0e12` (`git show` confirms it added exactly `calm-yard.png` + `hot-yard.png` to register-check, subject "#62: board visual pass") and `3076a40` (added all three view-28-12 PNGs, subject "clickable provenance links (#28) + car health bar (#12)"); `verify-registers.mjs:104` and `real-board-server.js:61` both exact; `a076bf6c0a94e302f`'s **latest** returned record (17:28:56) is indeed `process-42-car-r3`; `browser-register-cascade.test.js` exists; `verify-registers.mjs:19-23` does say "evidence for a human/reviewer to read … never a pass/fail gate"; and calm-yard's "nominal register across all five lanes" measured **TRUE** (all five compose `nominal`).

**Against the doctrine that landed this morning:** I read `dev`'s CLAUDE.md (this worktree's copy predates it — the branch is based on `bc4c28f`). The ruling requires a README "stating what the images validated, the commit they were captured at, and any known divergence since", with labelling *never* recapture, mechanically floored by DocPolicy's `Status:` gate. **Both READMEs satisfy that shape**: what was validated (§ bullets), capture commit (`45b0e12`/`3076a40`), divergence (the DISCLOSED STALENESS block), `Status: Current` at line 3. I confirmed DocPolicy is non-vacuous here — stripping the Status line from the register-check README made it fail **naming that exact file**. The car's "recapturing would still be dated per-train evidence" reasoning also lands on the right side of the new ruling.

### R1-m1 — fixed, and the second half of the question answered

Fix: `render.js:325` `taskId: c.taskId || null` and `render.js:379` `taskId: d.taskId || null`. **Non-vacuity re-derived:** reverting both to `?? null` reds **exactly and only** the two new pins (`render.test.js`: 47 pass / 2 fail).

**"Did it alter behaviour for any other falsy-but-meaningful value?" — swept through the real served modules, no value lost:**
```
absent  -&gt; null -&gt; "sweepsubject"      zero  -&gt; null -&gt; "sweepsubject"
""      -&gt; null -&gt; "sweepsubject"      false -&gt; null -&gt; "sweepsubject"
null    -&gt; null -&gt; "sweepsubject"      NaN   -&gt; null -&gt; "sweepsubject"
real handle "sweep-task-r1" -&gt; "sweep-task-r1" -&gt; "sweep-task-r1"
```
There is no falsy string that is a meaningful task-id, so nothing meaningful changed — and `||` is *strictly safer* than `??` for the type-violation cases (`0`/`false`/`NaN`), which `??` would have rendered as literal `"0"`/`"false"`/`"NaN"` labels.

**Observation, deliberately NOT raised as a finding:** a whitespace-only `" "` is truthy and still renders a blank-looking label. I checked reachability before deciding: `Envelope.psm1:138` `.TrimEnd()` + `:154` `IsNullOrEmpty → $null` means an all-whitespace task-id never reaches a record. It is *less* reachable than the `""` case I raised, which was already only a minor. Raising it would be ratcheting, so I am recording it, not charging it.

---

## FINDINGS

### R2-M1 (MAJOR) — the new README claims an image proves something it does not show

`docs/screenshots/2026-07-26-view-28-12-candidates/README.md:21-23`:
&gt; `health-trend-and-provenance.png` - the #28 ticket-link/**record-dir** provenance anchors and the #12 car health-trend badge, **both rendering** against a controlled review-round fixture.

**I opened the image.** The `#12` ticket link renders as a real anchor (visibly underlined under the train title). The health-trend badges render (`▼ converged (3→0)`, `● stalled (3→4→4)`). But every dispatch-row subject (`conv-review-r1`, `stall-review-…`) and every car chip (`conv-review-r1 Gate returned REJECT round 1`) renders as **plain text with no anchor affordance** — no dotted underline, in direct visual contrast to `#12` in the same frame.

`board/web/verify-registers.mjs:109-114` — the file this same README cites at line 7 — says so explicitly:
&gt; DISCLOSED: this scratch store lives under os.tmpdir(), outside the repo root, so config.githubArtifactsPrefix resolves empty here … **car/gate/dispatch RECORD-DIRECTORY links do NOT render in this particular screenshot for that reason** (proven separately, in-repo, by board/server's TestBuildSnapshotGitHubConfigConfigured and this file's own dom-writer.test.js suite)

So the README **misattributes evidence**: it tells a reader this repo holds a visual capture proving record-dir provenance anchors render, when the capture proves the opposite and the real proof lives in two named tests elsewhere. Under the ruling that landed this morning, a screenshot README's *entire* function is to state truthfully what its images validated — "an image with no record of what it validated … is not evidence at all". A wrong record is worse than none. This is the same overclaiming class as my round-1 Major, one layer over, and it is a defect the round-1 fix created. **Remedy is one clause:** name only the ticket link and the badge, and note that record-dir anchors do not render here (per `verify-registers.mjs:109-114`), with the in-repo proof pointed at instead.

### R2-m1 (minor) — R1-m2 is DRIFTED: the test still cannot fail for a length reason

The fixture and name changed; the substance my finding named did not. Measured **entirely out of repo**, replicating the car's own fixture builder against the real Go server and real Chromium at 1024×900:

```
LANDED (79 chars)    len= 79  scrollWidth= 741  clientWidth=123  -&gt;  scrollWidth&gt;clientWidth = PASS
ROUND-1 (14 chars)   len= 14  scrollWidth= 131  clientWidth=123  -&gt;  scrollWidth&gt;clientWidth = PASS
```

**Direction A holds** (removing `text-overflow: ellipsis` from `board.css` reds the test — injected and observed). **Direction B fails:** shortening the id back to round-1's 14-char `view-75-car-r1` **still passes**. `.solari-subject`'s box is 123px — a 13rem `auto-fill` grid column — so essentially any plausible id overflows it. The new assertion is length-*sensitive* in principle but not length-*discriminating* in the range that matters, which is precisely what my round-1 finding said ("it overflows by 8px, by accident of the 13rem grid column, not because the id is long"). The test name (`browser-dispatch-row-taskid.test.js:79`) still asserts that being "GENUINELY long (79 chars, longer than the hash it replaces)" is what produces the truncation; measured, it is not.

The instrument did improve and now asserts a true, non-vacuous thing — hence minor, not escalated. **Cheapest honest fix (NIRTS):** rename to what it proves — the task-id genuinely overflows its box and the box truncates — rather than engineering a width at which a short id fits.

### R2-m2 (minor) — an unmeasured size claim in a new comment

`board/web/test/support/real-board-server.js:471`: "GENUINELY LONG (79 chars, longer than the **33-char** subject hash it replaces)". Measured: the task_id is **79** chars (true), but the subject it replaces in this fixture is `view-75-with-task-id` = **20** chars. The figure 33 matches nothing in the fixture (real record-dir hashes are 17 chars). The load-bearing half is true; the cited number is not. CLAUDE.md's GUIDE STAR scar names "an unmeasured size claim" as one of the four recorded failures this shop writes down.

### R2-m3 (minor) — an inaccurate mechanism gloss in the new README

`docs/screenshots/2026-07-26-register-check-candidates/README.md:17-18`: "(both lanes carrying that dispatch render red)". I opened `hot-yard.png`: **three** live lanes render red (DISPATCHES / GATES / TRAINS, all "stale, 3060s"). And they do so not because they "carry that dispatch" — the in-flight probe is unassigned yard inventory, present in the dispatches lane's data alone — but because `board/server/poll.go:428` `computeLiveFreshness` is a **single board-global computation** over all records and all dispatches, applied to every live lane. A reader learning the freshness model from this caption would draw a false conclusion about the mechanism.

---

## THE FLAKE RULING: genuine fragility, PRE-EXISTING, out of scope — ticket it

**Not merely a flake.** `board/web/test/support/real-board-server.js:616`:
```js
const resolvedPort = port ?? 4700 + (process.pid % 200);
```
That is a **fixed, PID-derived port**, not port 0. `pid % 200` yields only ~50 distinct values for Windows' multiple-of-4 PIDs, and `node --test` runs each of the **five** `browser-*.test.js` files as a separate concurrent process, each spawning a Go server. The observed `bind: Only one usage of each socket address is normally permitted` is Windows `WSAEADDRINUSE` — the exact signature of a fixed-port collision, not random noise.

**But it is entirely pre-existing and untouched by this train.** `git log -L 616,616` attributes that line to `7fabf8a` ("test(board): drive a real browser for issue #31's cascade defect class (#33)") — the file's creation. Neither round of #75 touched it.

**Incidence, measured:** I ran the full board/web suite **four times** at `5d4baae` — 225/225 each, **zero** bind errors — plus every individual-file run in this review. Consistent with an intermittent modulo collision rather than a systematic break.

**Ruling:** real defect, correctly kept out of this train's scope (rewrite-vs-extend and right-sizing both say so), **not a finding against #75**. It should get its own ticket; the remedy is already proven in-house — bind port 0 and read the bound address back, as #79's probe suite does. `dev`'s friction log (`0964d65`) already records this accurately and honestly says it was deliberately not fixed here; that entry is correct as written.

---

## CONVERGENCE — trigger met on its face; I set a cap rather than escalate now

Round 1: **1 Major, 3 minor.** Round 2: **1 Major, 3 minor.** Against CLAUDE.md's swirl-and-churn test: Major count **flat** (not declining) ✔, findings **clustered** in the documentation/instrument-honesty area both rounds ✔, and round 2's Major **is a defect the round-1 fix created** ✔ — the sharpest of the three. On the letter of the rule that is escalation territory.

I am not escalating yet, and here is the reasoning rather than a reflex: the **code** has converged hard — R1-m1 is provably closed, R1-m3 is closed, Go is untouched at 98 PASS, 225/225 node, every gate green. All four remaining items are **single-line prose or naming corrections** with no open design question and no relocation of a mechanism. This is a documentation-accuracy tail, not a design churning in place.

**The cap, which the conductor is bound by:** ONE more fix cycle. If a round 3 produces another Major in this same documentation-accuracy class, **escalate to the owner instead of dispatching round 4** — at that point the instrument, not the author, is the problem, and the question ("what premise makes every round of doc captions come out wrong?") is not one a reviewer gate can answer.

---

## CITATION (#42) AND DOC CHECK

`.md` is **declared exempt** from #42 (`CodeCitationPolicy.Tests.ps1:49-50`: "Documentation has its own mechanical floor - DocPolicy.Tests.ps1's Status-line gate"), so the two new READMEs need no bare `#N` — they carry `#75`/`#62`/`#28`/`#12` anyway. Gate green 13/13. DocPolicy 2/2, and **proven non-vacuous against these exact files** (injection named the register-check README by path). Every `file:line` inside both READMEs opens to what it claims, and the citation resolver — which scans all tracked non-`artifacts` files — passes.

## CONSTITUTION CHECK

- **Law 1 (never a confident falsehood)** — **VIOLATED** by R2-M1 (a false evidentiary claim on a documentation surface) and dented by R2-m2/R2-m3. **Honored** in the code: the falsy sweep shows every non-handle value degrading to the honest subject, never a blank or invented label.
- **NORTH STAR / screenshots-as-validation-device (dev ruling, 2026-07-27)** — **honored in shape** (both READMEs carry what-validated, capture commit, divergence, `Status:`), **failed in one claim** (R2-M1).
- **Law 6 (no second copy)** — honored; `taskIDsBySubject` remains the single resolution owner, Go untouched this round.
- **Law 7 (no hardcoded taxonomy)** — honored; `taskId` remains an opaque pass-through.
- **Law 4 (never silently dropped)** — honored; subject hash still verbatim in `title` on both surfaces, observed.
- **TDD red-first** — honored and re-derived: four injections this round, each red for its stated reason; plus one measurement (direction B) that came back **green when it should have reded**, which is R2-m1.
- **Blameless is not polite** — the car's R1-M1 work was better than my finding asked for and I said so first; the Major stands anyway, because effort is not a mitigating factor.
- **Read-only discipline** — nothing committed, nothing pushed, shared checkout never touched. Four disclosed injections (`render.js`, `board.css`, register-check `README.md`, plus the out-of-repo direction-B replication), **every one restored byte-identical by sha256**; final `git status --porcelain` empty at `5d4baae`. Two probes ran entirely outside the repo, as preferred.

**Unreproducible-evidence callout (#13):** none. Every finding rests on a command, an injection, or an image I opened, all restated above. The one claim I could not reproduce — the car's disclosed bind-error flake — I chased to its root cause (`real-board-server.js:616`) and ruled on with a measured incidence (0 in 4 full runs) rather than accepting or dismissing it.

**To clear:** R2-M1 is binding. R2-m1/m2/m3 are one-line corrections I would take in the same pass.

Files: `C:\Users\Chris\git\starcar-wt\review-view-75\docs\screenshots\2026-07-26-view-28-12-candidates\README.md`, `...\docs\screenshots\2026-07-26-register-check-candidates\README.md`, `...\board\web\test\support\real-board-server.js`, `...\board\web\test\browser-dispatch-row-taskid.test.js`, `...\board\web\js\render.js`, `...\board\web\verify-registers.mjs`, `...\board\server\poll.go`

```starcar-artifact
outcome: REJECT
findings: 1 Major, 3 minor. Four-ID walk - R1-M1 PRESENT (fix correct and correctly broadened, but it introduced R2-M1), R1-m1 PRESENT (fixed provably), R1-m2 DRIFTED (see R2-m1), R1-m3 PRESENT. R2-M1 (MAJOR): docs/screenshots/2026-07-26-view-28-12-candidates/README.md lines 21-23 claims health-trend-and-provenance.png shows "the #28 ticket-link/record-dir provenance anchors and the #12 car health-trend badge, both rendering" - I opened the image and the record-dir anchors are NOT rendered (every dispatch-row subject and car chip is plain text with no anchor affordance, in visible contrast to the underlined #12 ticket link), and board/web/verify-registers.mjs lines 109-114, a file this same README cites at line 7, states this explicitly ("car/gate/dispatch RECORD-DIRECTORY links do NOT render in this particular screenshot") and names the two in-repo tests that do prove it; the README therefore misattributes evidence, in a document whose entire function under the owner ruling that landed this morning is to state truthfully what its images validated, and it is a defect the round-1 fix created. R2-m1 (minor, R1-m2 DRIFTED): the two-direction question comes back FALSE in one direction - measured entirely out of repo against the real Go server and real Chromium at 1024x900, the landed 79-char id gives scrollWidth 741 vs clientWidth 123 (assertion passes) but the round-1 14-char id gives scrollWidth 131 vs clientWidth 123, so the new assertion STILL PASSES for the short id my finding was about; .solari-subject's box is only 123px (a 13rem auto-fill grid column) so essentially any id overflows it, and the test name still asserts that being genuinely long is what produces the truncation when measurement says it is not; direction A does hold (removing text-overflow ellipsis reds the test, injected and observed), so the instrument improved and this stays minor. R2-m2 (minor): board/web/test/support/real-board-server.js line 471 claims "79 chars, longer than the 33-char subject hash it replaces" - the 79 is true but the subject it replaces in that fixture is view-75-with-task-id, 20 chars, and 33 matches nothing (real record-dir hashes are 17); an unmeasured size claim, the class CLAUDE.md's GUIDE STAR scar names. R2-m3 (minor): docs/screenshots/2026-07-26-register-check-candidates/README.md lines 17-18 says "both lanes carrying that dispatch render red" - I opened hot-yard.png and THREE live lanes render red, and they do so because board/server/poll.go line 428's computeLiveFreshness is a single board-global computation over all records and dispatches, not because a lane carries that dispatch (the in-flight probe is unassigned yard inventory present in the dispatches lane alone). R1-M1 three-part verification all CONFIRMED - the broadened scope is correct (verify-registers.mjs line 105 plus real-board-server.js line 61 cpSync, and I opened both hot-yard images and found a076bf6c0a94e in the bottom-right dispatches cell of each), the unaffected claim is TRUE (observed not read - ran buildScratchStoreWithHealthTrendFamilies through the real server, all 5 dispatch rows and all 5 train cars report taskIdPresent false), and both capture-commit citations are true (git show confirms 45b0e12 added exactly the two register-check PNGs and 3076a40 added the three view-28-12 PNGs). Both READMEs satisfy the new validation-device doctrine's required shape (what validated, capture commit, divergence, Status line) and DocPolicy was proven non-vacuous against them by injection. R1-m1 fully verified - reverting the two || back to ?? reds exactly and only the two new pins, and a falsy sweep through the real served modules shows absent/empty/zero/false/NaN/null all degrading to the honest subject with no meaningful value lost. FLAKE RULING - genuine fragility, pre-existing, out of scope, ticket it: real-board-server.js line 616 binds a fixed PID-derived port (4700 plus pid modulo 200, only about 50 distinct values for Windows multiple-of-4 PIDs) across five concurrent browser test files, so the observed bind error is WSAEADDRINUSE from a real collision hazard, not random noise; git log -L attributes that line to 7fabf8a (issue #33, the file's creation), untouched by either round of this train; measured incidence zero bind errors in four full-suite runs at 5d4baae; remedy already proven in-house is port 0 with read-back per #79. CONVERGENCE - round 1 was 1 Major 3 minor, round 2 is 1 Major 3 minor, so the swirl trigger is met on its face (Major count flat, findings clustered in the documentation-accuracy area, and this round's Major created by the previous round's fix); I did not escalate because the code has converged hard and all four remaining items are single-line prose or naming corrections with no open design question, but I SET A CAP the conductor is bound by - one more fix cycle, and if round 3 produces another Major in this same documentation-accuracy class, escalate to the owner rather than dispatching round 4. Suites re-derived at 5d4baae - node board/web 225 of 225 (four separate full runs), taskid file 5 of 5, geometry 3 of 3, Go 98 PASS 0 FAIL vet clean with no Go files in the delta, Pester scripts/tests 227 total 222 passed 0 failed 5 skipped with the extra skip named as the detached-HEAD session-start-guards ci-baseline test, CodeCitationPolicy 13 of 13, DocPolicy 2 of 2, probe-yard-snapshot PASS. Four disclosed fault injections plus two out-of-repo probes, every file restored byte-identical by sha256, tree clean at 5d4baae.
abstract: Delta review round 2 of view train #75, base 82f64b5 to head 5d4baae, two fix commits, 7 files +174/-9, no Go touched. REJECT on one Major. The car's four fixes were walked individually and re-derived by running rather than reading: R1-m1 and R1-m3 are properly closed (the empty-string pins red on revert and a falsy sweep through the real served modules shows no meaningful value lost), R1-M1's screenshot disclosure is correct and was correctly broadened beyond the reviewer's own citation from two images to four, with an explicit and independently verified unaffected ruling on the fifth. R1-m2 is DRIFTED: an out-of-repo replication of the car's fixture proved the new length assertion still passes for the short id the original finding was about, because the element's box is only 123px wide, so the test still cannot fail for a length reason. The Major is in the new documentation the fix produced - a caption asserting that a committed screenshot shows record-dir provenance anchors when the image does not show them and the source file cited two lines earlier says so explicitly. The swirl-and-churn trigger is met on its face with a flat Major count and a fix-created Major, so a hard cap was set at one further fix cycle before owner escalation. The disclosed browser-test bind error was traced to a fixed PID-derived port in a helper created by issue #33 and ruled a real but pre-existing out-of-scope defect deserving its own ticket.
task-id: view-75-review-r2
```</result>
<usage><subagent_tokens>298420</subagent_tokens><tool_uses>178</tool_uses><duration_ms>983162</duration_ms></usage>
</task-notification>

---

maybe a better way to attack this is to store all the documentation writes until the very end of the change? so that it can accuratly reflect what the change is post implementation? I don't know if I'm articulating this correctly

---

yeah file this off please, we'll get to it, just not right now I think. I need to think this through, it's becoming a lot more complex than I think is good. this started out as a showcase experiment, but if we're starting to trip over our own two feet, that's not good. We can't be getting in our own way.

---

<task-notification>
<task-id>b7okdfd7x</task-id>
<tool-use-id>toolu_01NomE4SJ72sxbdxpSvEVSrd</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\b7okdfd7x.output</output-file>
<status>completed</status>
<summary>Background command "Board statuses, worktree cleanup, watch CI (background)" completed (exit code 0)</summary>
</task-notification>

---

ok my other thought was using entire's check point commits as citation and provenence, which might work as well, and require less git check outing? Just grab that checkpoing, compare?

---

ok I think git hashing is the right move, for right now. Until we find a problem with it or come up with another brain wave from the good idea fairy

---

yeah that's the correct read

---

<task-notification>
<task-id>a353ad2db7e6ad627</task-id>
<tool-use-id>toolu_019SSbXo3FinXSzS5aaCRrZe</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\a353ad2db7e6ad627.output</output-file>
<status>completed</status>
<summary>Agent "Review #75 dispatch row titles" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result># VERDICT: **APPROVE** — 0 Major, 1 minor, 2 nits. **The cap does NOT fire; the series converged.**

Every assertion this round makes about what an artifact shows is **true**, and I verified each mechanically rather than by reading the car's report or squinting at pixels. The two prior Majors were both false assertions about artifact content; this round contains none. Findings shrank *and* moved — the opposite of the swirl signature.

---

## BASE-DELTA (#13)

Round-2 reviewed HEAD `5d4baaec05b0aaafe6375536452747b2f23d40b0` → round-3 HEAD `854aff41eb9b50f5e4d73f2cd8e10aa585b0a067`. Verified FIRST: detached, `git status --porcelain` empty. `git diff 5d4baae..854aff4 --stat` = **4 files, +88/−31** — matches the car's report exactly. One commit: `854aff4`. **No Go in the delta** (`git diff --name-only … -- '*.go'` empty).

**Scratch debris: none.** `git diff --name-only` contains no `scratch`/`measure` path; `git ls-files board/web` has none; `git log --all --diff-filter=A -- 'board/web/_scratch-measure-taskid.mjs'` returns **empty on every branch** — it was never committed anywhere. Tree clean, `-uall` shows nothing outside `node_modules`.

## Suites — RUN BY ME at 854aff4

| Suite | Observed |
|---|---|
| `node --test` board/web | **225/225, 0 fail** — three consecutive full runs, **0 bind errors in all three** |
| `browser-dispatch-row-taskid.test.js` alone | **5/5** |
| `browser-dispatch-row-geometry.test.js` alone | **3/3** |
| `go test ./...` / `go vet ./...` | **98 PASS / 0 FAIL**, vet exit 0 (untouched, as claimed) |
| Pester `scripts/tests` | **227 total / 222 passed / 0 failed / 5 skipped** |
| `CodeCitationPolicy.Tests.ps1` | **13/13** |
| `DocPolicy.Tests.ps1` | **2/2** |
| `probe-yard-snapshot.mjs` | **PASS**, exit 0 |

**Detached-HEAD delta, named:** `run-session-start-guards.sh: agent-pull runner (#50).running the REAL guards prints [ci-baseline]'s marker ONLY when attached to a branch` — the same conditional test as rounds 1 and 2 (car sees 4 skips attached, I see 5 detached).

**#81 flake:** not encountered. 0 bind errors in 3 runs here, on top of 0-in-4 at `5d4baae` — **0 in 7 full-suite runs** across two rounds. The car's 1-in-3 is consistent with an intermittent PID-modulo collision at `real-board-server.js`'s `resolvedPort`, still pre-existing and still out of scope.

---

## THE FOUR-ID WALK

| ID | Round-2 finding | Status | How verified |
|---|---|---|---|
| **R2-M1** | README claimed the image shows record-dir anchors | **Present** | real-browser anchor enumeration under the exact capture conditions |
| **R2-m1** | R1-m2 DRIFTED: assertion not length-discriminating | **Present** | rename matches my measurement; numbers reproduced exactly; still non-vacuous |
| **R2-m2** | false "33-char" comparison | **Present** | all replacement figures measured; 17-char claim uniformly true |
| **R2-m3** | "both lanes carrying that dispatch render red" | **Present** | mechanism traced hop-by-hop against real code |

### R2-M1 — verified mechanically, and it neither over- nor under-states

I did not take the car's word or trust my own pixel-reading. I reproduced `health-trend-and-provenance.png`'s **exact** capture conditions (`verify-registers.mjs:117-120`: `buildScratchStoreWithHealthTrendFamilies()` + `STARCAR_GITHUB_REPO`/`STARCAR_GITHUB_REF`) against the real Go server in real Chromium and enumerated every `&lt;a&gt;` on the page:

```
ALL &lt;a&gt; elements on the page: 1
   class="ticket-link provenance-link" text="#12" href=https://github.com/polecatspeaks/StarCar/issues/12
dispatch-row subjects: SPAN "conv-review-r1"  SPAN "conv-review-r2"  SPAN "stall-review-r2"  SPAN "stall-review-r3"   (no href)
car chips:  SPAN "conv-review-r1" … "stall-review-r3"                                        (no href)
```

**Exactly one anchor — the `#12` ticket link. Every `.solari-subject` and `.car-subject` is a plain `SPAN`.** The corrected bullet's every clause holds: the ticket link renders as a real anchor; the badges render (`▼ converged (3→0)`, `● stalled (3→4→4)` in red — confirmed by opening the image); record-dir anchors render nowhere.

**Under-statement check (the failure mode that followed the last correction) — clean.** I also reproduced `calm-yard`'s conditions (ambient store, no env): **zero `&lt;a&gt;` elements on the entire page.** So no image in that directory shows record-dir anchors, and the tightened second sentence's "never its record-dir-anchor half" is true across all three — it denies nothing that is actually there.

**Citations verified:** `TestBuildSnapshotGitHubConfigConfigured` exists (`board/server/githubconfig_test.go:28`) and the README cites it **by symbol, not line** ✔. `verify-registers.mjs:112-114` does name **both** that test and `dom-writer.test.js` ✔, and the comment sits at 106-116, **immediately above** the call at 117 ✔. The car also silently improved the source comment's own slip ("this file's own dom-writer.test.js suite" → "this repo's own"), which is correct — `verify-registers.mjs` is not that file.

**Live corroboration, unplanned but worth recording:** running calm-yard's conditions *today* rendered dispatch rows as `view-69-71-review-r1`, `friction-mine-0726-evening`, `view-69-71-review-r3`, `view-69-71-car-r3` — task-ids where the committed image shows hashes. Direct proof that the DISCLOSED STALENESS block describes something real.

### R2-m1 — the rename matches the measurement, not merely the sound of it

The car reproduced my numbers **exactly**: 14-char → `scrollWidth 131 / clientWidth 123`; 79-char → `741 / 123`. Identical to my round-2 out-of-repo measurement.

My finding was that the assertion is **box-width-dependent, not length-discriminating**. The new name — *"a task-id wider than `.solari-subject`'s own (narrow) box truncates via ellipsis rather than wrapping"* — states the box-width framing directly, and the header comment now says so explicitly, including that round-1's 14-char id "ALSO overflows it … and would ALSO pass this assertion." That is my finding written into the artifact, not paraphrased away. The comment correction "length-dependent" → "BOX-width-dependent" is right.

**Still non-vacuous:** injected removal of `text-overflow: ellipsis` from `board.css` (sha256 `B4860579…0E6B` before and after, restored byte-identical) reds exactly this test. Assertion logic unchanged, as claimed.

### R2-m2 — every replacement figure measured, and the 17-char claim is stronger than stated

`'view-75-with-task-id'.Length = 20` ✔, `'a076bf6c0a94e302f'.Length = 17` ✔, task_id `= 79` ✔. The claim "any real record-dir hash (measured: 17 chars)" is not merely an example — I grouped every hash-shaped directory in the real store: **all 66 are exactly 17 characters.** Uniformly true.

**No surviving unmeasured size claim in that comment.** I swept every number in it: 79 ✔, 33 (correctly quoted as the *wrong old* value) ✔, 20 ✔, 17 ✔, "~123px" ✔ (measured both rounds), "13rem `auto-fill` grid column" ✔ (`board.css:619` `repeat(auto-fill, minmax(13rem, 1fr))`).

### R2-m3 — mechanism trace verified line by line

Every hop the caption asserts, opened:
- `poll.go:334` — `liveFreshnessVal = computeLiveFreshness(scanResult.Records, out.Dispatches, …)`, called **once**, over every record and every dispatch ✔
- `poll.go:341-345` — the `laneRegistry` loop, `case "live": lane.Freshness = liveFreshnessVal`, one result to every live lane alike ✔
- `laneregistry.go:20-24` — dispatches/gates/trains `"live"` (**three**), freight `"dark"`, fuel `"bagged"` ✔
- `poll.go:367-368` — `default: lane.Freshness = Freshness{Kind: "not-applicable"}` — a constant literal ✔

**The self-caught overclaim was caught correctly.** The draft "carry no freshness computation at all" would have been false — those lanes *do* receive a freshness value, just a constant one. The committed wording, "a constant, never computed from record age", is precise. Disclosing that mid-writing correction is exactly the behaviour this shop codes as a success outcome, and it is worth saying so.

Against the image: `hot-yard.png` shows DISPATCHES/GATES/TRAINS all red at "Live / stale, 3060s", FREIGHT "Dark" and FUEL "Bagged" unchanged. All citations here are **symbol/shape form, zero line numbers** — the symbol-form rule this family paid two Majors for is honored.

---

## FINDINGS

### R3-m1 (minor) — a citation points at prose that does not contain the claim

`docs/screenshots/2026-07-26-register-check-candidates/README.md:20-24`:
&gt; not because each "carries" the in-flight probe (**it is unassigned yard inventory, present only in the dispatches lane's own data**, `board/web/test/support/real-board-server.js`'s `buildScratchStoreWithInFlightDispatch` **doc comment**)

I opened that doc comment (`real-board-server.js:41-58`). It says the builder `cpSync`s the real store and "adds exactly ONE additional, genuinely IN-FLIGHT `dispatched` record (no returned or presumed-lost successor)". It says **nothing** about the record being unassigned, nothing about yard inventory, and nothing about which lane it appears in. A reader following the pointer finds none of the two facts attributed to it.

**Both facts are true** — I verified them independently, and the *image itself* is the better citation: `hot-yard.png`'s own header reads **"80 in yard inventory (unassigned)"** against `calm-yard.png`'s **"79"**, and hot-yard's TRAINS lane carries the same ten chips as calm-yard with no probe among them. So this is a citation defect, not a false statement — which is why it is minor and not Major. Cheapest fix: cite the image's own "80 in yard inventory (unassigned)" line, or drop the pointer.

### R3-n1 (nit) — a 4-of-5 illustrative list without "etc."

`docs/screenshots/2026-07-26-view-28-12-candidates/README.md:25-27`: "every dispatch-row subject (`conv-review-r1`, `stall-review-r1/r2/r3`)". The image shows **five** dispatch rows — `conv-review-r2` is omitted. The sibling parenthetical in the same sentence does carry "etc."; this one does not, so it reads as an enumeration. The load-bearing claim ("record-dir anchors do NOT render anywhere in this image") is true and complete, so nothing is misrepresented.

### R3-n2 (nit / observation) — a test name asserting a property its sibling proves

`browser-dispatch-row-taskid.test.js:93` — "…truncates via ellipsis **rather than wrapping**". This test asserts computed `overflow`/`text-overflow` plus real overflow; the *non-wrapping* half is asserted by test 5 in the same file (`distinctTopCount === 1`) and by `.solari-row-main`'s `white-space: nowrap`. The property is genuinely guarded, just not by this test alone. Recording it rather than charging it.

---

## DOC CHECK — both READMEs satisfy the dev screenshot doctrine

Read against `dev`'s CLAUDE.md NORTH STAR (screenshots are a **validation device**, not a living document; the obligation is **labelling — what was validated, at which commit, known divergence — never recapture**):

| Requirement | register-check | view-28-12 |
|---|---|---|
| What the images validated | ✔ per-image bullets | ✔ per-image bullets |
| Capture commit | ✔ `45b0e12` (verified: added exactly those two PNGs, subject "#62: board visual pass") | ✔ `3076a40` (verified: added all three PNGs, subject "clickable provenance links (#28) + car health bar (#12)") |
| Known divergence since | ✔ DISCLOSED STALENESS block | ✔ same |
| DocPolicy `Status:` floor | ✔ line 3 | ✔ line 3 |
| Recapture avoided | ✔ explicitly reasoned | ✔ |

DocPolicy 2/2 and proven non-vacuous against these exact files in round 2. The citation resolver (#65) — which scans all tracked files minus `artifacts/**` — passes, so every `file:line` in both READMEs mechanically resolves. **#42:** `.md` is declared exempt (`CodeCitationPolicy.Tests.ps1:49-50`, documentation's floor being DocPolicy), gate green 13/13; both READMEs carry `#75`/`#62`/`#28`/`#12` regardless.

---

## CONVERGENCE AND THE CAP RULING

| Round | Majors | minors | nits |
|---|---|---|---|
| 1 | 1 | 3 | — |
| 2 | 1 | 3 | — |
| **3** | **0** | **1** | **2** |

**The cap does NOT fire.** I set it to trigger on "another Major in the same documentation-accuracy class" — and that class was specifically *false assertions about what an artifact shows*. This round contains none: every such assertion is true and independently verified, including the ones I had to build a browser probe to settle. Findings **declined** (1M/3m → 0M/1m/2n) and **moved** (from false content to a citation pointer and two nits), which is the converging signature rather than the swirl one. No round-3 finding is a defect created by a round-2 fix.

The round's binding rule — *open the artifact, describe what is actually there, then write the sentence* — is visible in the work product: the car reproduced my measurements independently, opened both images, traced the mechanism to four exact code sites, and disclosed catching its own draft overclaim before committing. That last one is the tell that the instrument changed rather than the wording.

**Escalation to the owner is NOT warranted.** R3-m1 and the two nits are one-line corrections that can ride the merge or be dropped; none blocks.

## CONSTITUTION CHECK

- **Law 1 (never a confident falsehood)** — **honored**. Every artifact claim verified true: 1 anchor in health-trend, 0 in calm/hot-yard, three red live lanes, the `default:` constant, 79/20/17/~123px/13rem. R3-m1 is a misplaced pointer to a true fact, not a falsehood.
- **NORTH STAR / screenshots-as-validation-device** — **honored**; both READMEs carry all four required elements.
- **Law 6 (no second copy)** — honored; Go untouched, `taskIDsBySubject` still the single resolution owner.
- **Law 7 (no hardcoded taxonomy)** — honored; nothing taxonomic changed.
- **Law 4 (never silently dropped)** — honored; `title` tooltip behaviour unchanged, 225/225.
- **Symbol-form citation rule** — honored; zero line-number citations added anywhere in the diff.
- **TDD / non-vacuity** — honored; the renamed test still reds under CSS fault injection, assertion logic unchanged.
- **GUIDE STAR (blameless, not vague; success outcomes reported as wins)** — the car's self-caught overclaim disclosure is a success outcome and I have named it as one, without hedging.
- **Read-only discipline** — nothing committed, nothing pushed, shared checkout untouched. One disclosed injection (`board.css`), restored **byte-identical** by sha256 (`B4860579…0E6B`); two probes ran entirely outside the repo. Final `git status --porcelain` empty at `854aff4`.

**Unreproducible-evidence callout (#13):** none. Every claim above rests on a command, an injection, an image I opened, or a browser probe restated here. One honest calibration note: my anchor-enumeration probe returned 4 dispatch rows where the committed image shows 5 (`stall-review-r1` absent) — a poll-timing artifact of my own probe, not a repo defect; it does not affect the finding, since every row it *did* return was a plain `SPAN`.

**Recommendation:** merge. R3-m1 is worth a one-line fix if a commit is passing anyway; the two nits are optional.

Files: `C:\Users\Chris\git\starcar-wt\review-view-75\docs\screenshots\2026-07-26-register-check-candidates\README.md`, `...\docs\screenshots\2026-07-26-view-28-12-candidates\README.md`, `...\board\web\test\browser-dispatch-row-taskid.test.js`, `...\board\web\test\support\real-board-server.js`, `...\board\server\poll.go`, `...\board\server\laneregistry.go`, `...\board\web\verify-registers.mjs`

```starcar-artifact
outcome: APPROVE
findings: 0 Major, 1 minor, 2 nits. THE CAP DOES NOT FIRE - the series converged (round 1 1M/3m, round 2 1M/3m, round 3 0M/1m/2n), findings both declined and moved, and no round-3 finding is a defect a round-2 fix created. Four-ID walk - R2-M1 PRESENT, R2-m1 PRESENT, R2-m2 PRESENT, R2-m3 PRESENT, all four verified by running or opening rather than from the car's report. R2-M1 verified mechanically rather than by pixel-reading: I reproduced health-trend-and-provenance.png's exact capture conditions (buildScratchStoreWithHealthTrendFamilies plus STARCAR_GITHUB_REPO, per verify-registers.mjs lines 117-120) against the real Go server in real Chromium and enumerated every anchor element on the page - exactly ONE exists, class ticket-link provenance-link, text #12, href to issues/12, while every solari-subject and every car-subject is a plain SPAN with no href; the corrected bullet is therefore exactly right. Under-statement checked too: reproducing calm-yard's conditions (ambient store, no env) yields ZERO anchor elements on the whole page, so no image in that directory shows record-dir anchors and the tightened second sentence denies nothing that is actually present. TestBuildSnapshotGitHubConfigConfigured confirmed at board/server/githubconfig_test.go line 28 and cited BY SYMBOL not line; verify-registers.mjs lines 112-114 confirmed to name both that test and dom-writer.test.js, and to sit immediately above the capture call. R2-m1: the car reproduced my numbers exactly (14-char gives scrollWidth 131 vs clientWidth 123, 79-char gives 741 vs 123) and the rename to "a task-id wider than solari-subject's own narrow box" states the box-width framing my measurement established rather than merely sounding better, with the header comment now recording explicitly that round-1's 14-char id would ALSO pass; still non-vacuous, since injecting removal of text-overflow ellipsis from board.css reds exactly this test. R2-m2: all replacement figures measured correct - subject 20 chars, example hash 17, task_id 79 - and the 17-char claim is stronger than stated, since ALL 66 hash-shaped record directories in the real store measure exactly 17; no unmeasured size claim survives in that comment (79, 20, 17, ~123px and the 13rem auto-fill column at board.css line 619 all verified). R2-m3: mechanism trace verified hop by hop against real code - poll.go line 334 calls computeLiveFreshness ONCE over every record and dispatch, lines 341-345 assign that one result to every position live lane, laneregistry.go lines 20-24 declare three live lanes plus freight dark and fuel bagged, and lines 367-368 are the default branch assigning a constant Freshness Kind not-applicable; the car's self-caught draft overclaim correction ("carry no freshness computation at all" to "a constant, never computed from record age") is the right correction and disclosing it mid-writing is a success outcome. R3-m1 (minor): docs/screenshots/2026-07-26-register-check-candidates/README.md lines 20-24 attributes "unassigned yard inventory, present only in the dispatches lane's own data" to buildScratchStoreWithInFlightDispatch's doc comment, but that comment (real-board-server.js lines 41-58) says only that the builder cpSyncs the real store and adds one in-flight dispatched record - it contains neither fact; both facts are independently TRUE (hot-yard.png's own header reads 80 in yard inventory unassigned against calm-yard's 79, and hot-yard's trains lane carries the same ten chips with no probe), so this is a misplaced pointer rather than a falsehood, hence minor not Major. R3-n1 (nit): the view-28-12 README names four of five visible dispatch-row subjects without an "etc." while the sibling parenthetical in the same sentence has one. R3-n2 (nit): the renamed test's "rather than wrapping" clause is proven by its sibling test 5 and by white-space nowrap, not by this test alone. Scratch debris check clean - git log --all --diff-filter=A shows board/web/_scratch-measure-taskid.mjs was never committed on any branch, nothing tracked or untracked. Suites at 854aff4 - node board/web 225 of 225 across THREE consecutive full runs with zero bind errors (0 in 7 runs across two rounds, so the #81 fixed-port flake was not encountered and remains pre-existing and out of scope), taskid file 5 of 5, geometry 3 of 3, Go 98 PASS vet clean with no Go in the delta, Pester 227 total 222 passed 0 failed 5 skipped with the detached-HEAD delta named as the session-start-guards ci-baseline test, CodeCitationPolicy 13 of 13, DocPolicy 2 of 2, probe-yard-snapshot PASS. Both READMEs satisfy the dev screenshot doctrine on all four points (what validated, capture commit verified by git show, known divergence, DocPolicy Status line) and every citation added this round is symbol-form with zero line numbers. One disclosed injection, restored byte-identical by sha256, tree clean at 854aff4.
abstract: Delta review round 3 of view train #75 - the capped round. Base 5d4baae to head 854aff4, one fix commit, 4 files +88/-31, no Go, no scratch debris. APPROVE with zero Majors, so the cap I set in round 2 does not fire and no owner escalation is warranted. All four round-2 findings are Present, each re-derived by running or opening rather than from the car's report: the R2-M1 correction was verified by driving a real browser under the screenshot's exact capture conditions and enumerating every anchor element (exactly one, the ticket link, with every subject and car chip a plain span), and the opposite failure mode was tested too by reproducing calm-yard's conditions and finding zero anchors, confirming the tightened sentence under-states nothing. The R2-m3 mechanism trace was checked against four exact code sites and the car's self-caught draft overclaim correction proved to be the right one. R2-m1's rename matches the measurement I made rather than merely sounding better, and R2-m2's replacement figures are not only correct but uniformly so across all 66 hash-shaped record directories. What remains is one minor citation pointer aimed at a comment that does not contain the fact attributed to it, plus two nits. The series went 1M/3m to 1M/3m to 0M/1m/2n, with findings both declining and moving, which is convergence rather than swirl.
task-id: view-75-review-r3
```</result>
<usage><subagent_tokens>343437</subagent_tokens><tool_uses>200</tool_uses><duration_ms>576058</duration_ms></usage>
</task-notification>

---

ok what's the card for the trains lane that is still showing the v0 walking skeleton?

---

let's do that one, I think that's what I need next to see in flight and working before I can make any calls on it.

---

ohh bug! [Image #2] as soon as that car got dispatched, it flipped it's labeling to the old format.

---

ok so let me see if I understand this correctly, until the car is dispatched it has no human readable labels, just the hash, but after it's dispatched, it has labels. Would a easier and more elegant solution be that as a train is building up, we just label it as "boarding" meaning the train is getting its cars loaded etc, and after it dispatches it flips from boarding to what ever the human lables are?

---

ok after this, let's light up the freight lane as well, it's dark right now.

---

<task-notification>
<task-id>ad9b6aad6153127f4</task-id>
<tool-use-id>toolu_01UNUgMTFLVUMtrkGwEhwZc1</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\ad9b6aad6153127f4.output</output-file>
<status>completed</status>
<summary>Agent "Car #76 train manifest minting" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## Final Report — Car, issue #76 (train-manifest minting tooling)

**Base verified:** worktree `C:\Users\Chris\git\starcar-wt\car-tooling-76`, branch `car/tooling-76`, HEAD `3458499716f1ec933e2ede539198216b2ee0b09d` at start, clean tree. Final commit: **`8f9916c3e8911e2421565405bb3d6d8fa16907d3`**.

### What landed

1. **`scripts/New-TrainManifest.ps1`** — mints/amends plan (intent-kind) manifest records, sealed via `scripts/Artifact.psm1`'s `Get-Sha256Hex` (Law 6, never reimplemented), validated against **both** `schema/starcar-artifact.schema.json` and `schema/starcar-manifest.schema.json` before write.
2. **`schema/starcar-manifest.schema.json`** — contract change: member `required` moves from `["subject","role"]` to `["role"]` plus an `anyOf` requiring at least one of `task_id`/`subject`. `task_id` is new.
3. **`scripts/tests/New-TrainManifest.Tests.ps1`** — 21 tests, black-box invocation (matches `Detector.Tests.ps1`'s convention).
4. **Issue #85 filed** (`area:view`, on project board 6) for a disclosed board-side gap, out of this ticket's scope.

### The mid-flight amendment and my verification of it

The coordinator's binding amendment (member carries `task_id` always, `subject` added by amendment, never replacing `task_id`) arrived while I was still in the schema-investigation phase. Before implementing I **verified rather than assumed** two things it explicitly asked me to check:

- **Schema required-array conflict:** making `task_id` unconditionally required would have broken the six already-landed `artifacts/train-board-v0/intent-*.json` records (subject-only members, no `task_id`). Ran `scripts/store-checks/StoreIntegrity.Tests.ps1` at each candidate schema shape to confirm. Resolved via `anyOf` (task_id or subject), never backfilling history — confirmed 267/267 store-checks pass unchanged.
- **The board join, empirically, twice:** I built `board/server` from source (`go build ./server`) and ran it against real scratch stores via `STARCAR_STORE_PATH`, exactly the #79 probe's method.
  - First run (old schema, `subject` still required): the record was **quarantined** (`record-quarantined`, "missing property 'subject'").
  - Second run (schema updated to allow task_id-only members): **zero board conditions**, but the trains lane rendered `"cars":[], "declaredNotObserved":[]"` — the member was **invisible**, not `declaredNotObserved` as the amendment hoped.

Root cause: `board/assemble/assemble.go`'s `manifestPayload` function reads `mm["subject"].(string)`, type-asserts absent to `""`, and its own `if subject == "" { continue }` guard drops the member from the parsed `members` slice before `Assemble`'s per-member loop ever runs the `dispatchBySubject` lookup that populates `DeclaredNotObserved`. This is a real, disclosed contradiction between the corrected design and current `board/` code. **I did not touch `board/`** (out of scope, confirmed by diff: only `schema/`, `scripts/New-TrainManifest.ps1`, `scripts/tests/` changed). I filed it as **issue #85** and pinned it as a documented red (`scripts/tests/New-TrainManifest.Tests.ps1`'s last `Context`, which builds the real server and asserts today's true — contradictory — behavior).

### TDD honesty note (disclosed deviation)

Strict red-first was interrupted by the necessary schema-contradiction investigation (I had to write the implementation to discover the `Test-Json`-before-`integrity`-exists bug and the `Join-Path`-on-absolute-path bug). To compensate, I captured a **genuine red** after writing the test file: moved `New-TrainManifest.ps1` aside, ran the suite — **18/21 failed** with `CommandNotFoundException` (script not found), the correct reason — then restored the script. Two real implementation bugs surfaced this way and were fixed:
- `Join-Path` doesn't discard a rooted second argument the way `Path.Combine` does — an absolute `-StoreRoot` (TestDrive) produced `IOException`. Fixed with `[System.IO.Path]::IsPathRooted` guard.
- Schema validation was attempted before `integrity` existed, but the base schema requires it — chicken-and-egg. Fixed by validating a throwaway copy with a placeholder `sha256:` value (shape-only check; pattern satisfied identically by the real hash).

### Suite arithmetic

Baseline at `3458499` (own run, stashing my edits first): `scripts/tests` **223/0/4 skipped**, `scripts/probes` **29/29**.

Final, at `8f9916c`:
- `scripts/tests`: **244 passed / 0 failed / 4 skipped** (223 baseline + 21 new)
- `scripts/store-checks`: **267/267** (unaffected — confirms no-backfill on the 6 historical manifests)
- `scripts/probes`: **29/29** (unaffected)
- Go board (`go test ./...` from `board/`, `go1.26.5`): **98 PASS / 0 FAIL**
- `board/web` node tests: **209 pass / 5 fail** — the 5 failures are the `browser-*.test.js` files requiring Playwright, and `node_modules` is absent in this worktree (never installed here). This is a pre-existing worktree environment gap, unrelated to my diff (I never touched `board/web`), and the total (214) doesn't match the brief's cross-check figure (225/225) — disclosed rather than silently reconciled.

### Worked example (conductor commands)

Mint:
```powershell
pwsh -NoProfile -File scripts/New-TrainManifest.ps1 `
  -Subject 'train:tooling-76' -Title 'Train manifest minting tooling' -Tickets @('#76') `
  -Members @(
    @{ task_id = 'tooling-76-car-r1'; role = 'car' },
    @{ task_id = 'tooling-76-review-r1'; role = 'gate'; gate = 'car-review r1' }
  )
```
Amend (at dispatch, once the car's real agent id is known):
```powershell
pwsh -NoProfile -File scripts/New-TrainManifest.ps1 `
  -Subject 'train:tooling-76' -ReplaceTaskId 'tooling-76-car-r1' -WithSubject 'a1b2c3d4e5f6789ab'
```

### Ruling on item 4 (documentation)

Updated both the schema's top-level `description` and the `task_id`/`subject` property descriptions in the same commit as the contract change, stating the plan-then-amend lifecycle honestly, including the disclosed board-side contradiction. This is NOT an honest stop — the contract change was defensible and reasoned on the page (`anyOf`, not unconditional `task_id` requirement) once I verified the historical-record conflict.

### Files touched (all absolute paths)

- `C:\Users\Chris\git\starcar-wt\car-tooling-76\schema\starcar-manifest.schema.json`
- `C:\Users\Chris\git\starcar-wt\car-tooling-76\scripts\New-TrainManifest.ps1`
- `C:\Users\Chris\git\starcar-wt\car-tooling-76\scripts\tests\New-TrainManifest.Tests.ps1`

```starcar-artifact
outcome: done
findings: Built scripts/New-TrainManifest.ps1 (mint/amend, both-schema validation, Get-Sha256Hex sealing) and the schema/1 member-identity contract change (subject optional, task_id added, anyOf requiring one) per the coordinator's binding mid-flight amendment. Verified the amendment's premise empirically rather than assuming it: built and ran the real board/server against scratch stores twice, found a genuine contradiction (a task_id-only member is silently dropped by assemble.go's manifestPayload function, not rendered declaredNotObserved as expected) - disclosed, pinned as a red test, and filed as issue #85 (board/ untouched, out of scope). Also verified the anyOf schema shape does not retroactively break the six already-landed train:board-v0 manifests (NO-BACKFILL doctrine honored, store-checks 267/267 unaffected). Two real implementation bugs found and fixed via a genuine red-then-green cycle after test authoring: a Join-Path-on-absolute-path defect and a validate-before-integrity-exists ordering defect. Suites at final commit 8f9916c3e8911e2421565405bb3d6d8fa16907d3: scripts/tests 244/0/4skip (223 baseline + 21 new), scripts/store-checks 267/267, scripts/probes 29/29, Go board 98/0. Disclosed discrepancy: board/web node tests ran 209/214 pass (5 Playwright-dependent browser tests fail due to missing node_modules in this worktree, pre-existing and unrelated to this diff) rather than the brief's cross-check figure of 225/225.
abstract: Delivered the producer-side train-manifest minting tooling for #76 - New-TrainManifest.ps1 (mint and amend modes) plus the schema/1 contract change the coordinator's mid-flight amendment required, with the amendment's own premise (subject-less members render declaredNotObserved) empirically disproven via a live board/server run and disclosed as new issue #85 rather than silently assumed or fixed out-of-scope. All work confined to schema/, scripts/New-TrainManifest.ps1, and scripts/tests/ - board/ never touched. 244/0/4skip scripts/tests, 267/267 store-checks, 29/29 probes, 98/0 Go board, all green at commit 8f9916c.
task-id: tooling-76-car-r1
```</result>
<usage><subagent_tokens>229456</subagent_tokens><tool_uses>109</tool_uses><duration_ms>1575064</duration_ms></usage>
</task-notification>

---

let's run that probe for 84 and get the results into the ticket please

---

I concur with the new kind: ticket, that keeps things clean with a good demarcation

---

ok let's queue up 84 then 85. My reasoning is that 84 gets more of the whole yard picture and then 85 gets more visiblity and we can judge how the trains move through the lanes

---

<task-notification>
<task-id>a60fcb87fea862f02</task-id>
<tool-use-id>toolu_01FDP211LHDfwUUH88C28dqV</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\a60fcb87fea862f02.output</output-file>
<status>completed</status>
<summary>Agent "Review #76 manifest minting" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>---

# VERDICT: **REJECT** — 3 Major, 5 Minor

The car's empirical work is genuinely excellent and I confirm its headline claim independently. The REJECT rests on documentation staleness, an unnecessary contract widening that a measured alternative defeats, and an incomplete disclosure that leaves the train's headline goal undelivered and untracked.

## BASE-DELTA (#13)

Worktree `C:\Users\Chris\git\starcar-wt\review-tooling-76`, HEAD `8f9916c3e8911e2421565405bb3d6d8fa16907d3`, **detached, 0 dirty** — verified before any command. Base `3458499716f1ec933e2ede539198216b2ee0b09d`.

```
 schema/starcar-manifest.schema.json       |  14 +-
 scripts/New-TrainManifest.ps1             | 342 +++++++++++++++++++++++++++
 scripts/tests/New-TrainManifest.Tests.ps1 | 316 ++++++++++++++++++++++++
 3 files changed, 669 insertions(+), 3 deletions(-)
```

Shared checkout `C:\Users\Chris\git\starcar` verified untouched (0 dirty, HEAD `afadc84`).

## Suites — observed by me at 8f9916c

| Suite | Car claimed | I observed | Reconciles |
|---|---|---|---|
| `scripts/tests` | 244 P / 0 F / 4 S | **243 P / 0 F / 5 S** (248 total) | yes — detached-HEAD delta, below |
| baseline (minus new file) | 223 | **222 P / 5 S** (227 total) | yes — 223 P / 4 S on a branch |
| `scripts/store-checks` | 267/267 | **267 P / 0 F** | exact |
| `scripts/probes` | 29/29 | **29 P / 0 F** | exact — #79 `ManifestBoardJoin` **not** reded |
| Go board | 98 PASS | **98 top-level PASS**, 118 incl. subtests, `go vet` clean, exit 0 | exact on the top-level convention |
| `board/web` node | 209 pass / 5 fail | **209 pass / 5 failing files** | exact |

**Detached-HEAD skip delta (mine, not a defect):** the one-test difference is `SessionStartGuardRunner.Tests.ps1`'s *"running the REAL guards prints [ci-baseline]'s marker ONLY when attached to a branch"*, which skips on my detached HEAD and passed on the car's branch. 243+1 = 244, 5−1 = 4. Total 248 both ways.

**Go count note (not a finding):** the car's "98" is top-level test functions; CI's own counter (`ci.yml:287-290`, distinct `run` actions) counts **118**. Both defensible; flagging so nobody reconciles against the wrong number later.

---

# ADJUDICATION 1 — the car disproved the conductor's premise. **The car is right. Say it plainly.**

I did not take this on report. I built `board/server` from source (`go build -o … ./server`, exit 0), constructed a scratch store with a **four-shape member matrix** in one manifest, and read real HTTP `/api/snapshot`.

Observed, verbatim:

```json
{"id":"train:rev76-matrix","title":"Reviewer matrix probe",
 "cars":[{"subject":"bbbbbbbbbbbbbbbbb","role":"car","state":"returned",...,"taskId":"rev76-B-both"}],
 "declaredNotObserved":["ccccccccccccccccc","ddddddddddddddddd"],
 "tickets":["#76"]}
```

Member A (`task_id: rev76-A-taskid-only`, **no subject**) appears in **neither `cars` nor `declaredNotObserved`**, and **zero board conditions** were raised. Members C and D (subject present, no record) *do* reach `declaredNotObserved`. The task_id-only member is silently gone.

**(a) Root-cause analysis: correct AND complete.** `board/assemble/assemble.go:304` `subject, _ := mm["subject"].(string)` type-asserts an absent key to `""`; `:307` `if subject == "" { continue }` drops the member from the returned slice; `Assemble`'s per-member loop at `:106` iterates only survivors, so the `DeclaredNotObserved` append at `:112` is unreachable for that member. Every hop the car named is exactly right, including that the drop precedes the `dispatchBySubject` lookup at `:110`.

**(b) The pin is HONEST.** `New-TrainManifest.Tests.ps1:308-314` asserts `cars.Count = 0` **and** `declaredNotObserved.Count = 0` — today's real behaviour, not the hoped-for behaviour. That is Law 1 done properly: it gives a future board fix a red to turn green rather than asserting a fiction. I fault-injected it (F1) and it reds correctly when the producer stops emitting subject-less members.

**(c) Staying out of `board/` was the RIGHT call.** `board/` was excluded by the dispatch comment; #79's probe guards that surface; and the fix is a design question about what a subject-less member should render as. Filing #85 (verified: OPEN, `area:view`, on the project board, technically accurate) rather than improvising is the honest-stop doctrine working.

**This is the second time in two days a car has disproved a conductor premise by running the thing. That is the process working, and it is the best part of this diff.** The conductor's binding amendment asserted the behaviour "needs no board change"; the car checked instead of trusting, and the conductor's sentence was wrong.

---

# ADJUDICATION 2 — the schema contract change. **This is where the diff fails.**

**(b) The `anyOf` DOES enforce what it claims — measured, all seven directions:**

| Member shape | Result |
|---|---|
| `task_id` + `role` | **VALID** |
| `subject` + `role` (legacy) | **VALID** |
| both + `role` | **VALID** |
| **neither**, `role` only | **REJECTED** — `Required properties ["task_id"] are not present` \| `… ["subject"] …` |
| `task_id`, no `role` | REJECTED |
| `subject`, no `role` | REJECTED |
| `{}` | REJECTED |

`role` stays unconditionally required in every combination. The gate is real, not decorative.

**(c) NO-BACKFILL doctrine HONORED — verified, and the car's evidence is stronger than I first credited.** All six `artifacts/train-board-v0/intent-*.json` records validate against **both** schemas at HEAD; every one of their 31 members is `subject+role` with no `task_id`. Crucially, `StoreIntegrity.Tests.ps1:121-136` **does** layer `starcar-manifest.schema.json` over every `^train:` record — so the car's "267/267 unaffected" is genuine NO-BACKFILL evidence, not a base-schema-only result. No landed record was retrofitted or retroactively invalidated. **This part is right.**

### **(a) The relaxation is NOT demonstrably necessary — R1-M1 (Major).**

The brief forbade quietly relaxing `required` to make an implementation pass. The car did not do it *quietly* — it reasoned on the page in three places. But it never examined the alternative that removes the need entirely, so the widening is unjustified rather than merely undocumented.

**The unexamined alternative:** declare a plan member with `task_id` **and** `subject` both set to the shop-minted handle; amendment then overwrites `subject` with the runtime id while `task_id` survives untouched. I built and ran it:

```json
{"id":"train:alt-shape","title":"Alternative shape","cars":[],
 "declaredNotObserved":["tooling-76-car-r1","tooling-76-review-r1"],"tickets":["#76"]}
```

Measured properties of the alternative:
- **Validates against the PRE-#76 `required: ["subject","role"]` schema — `True`.** Zero contract widening; `task_id` lands as a purely additive optional property.
- Renders **readable human names** in `declaredNotObserved` — precisely the surface the corrected design demanded ("*renders in declaredNotObserved with a READABLE name … that IS the plan-vs-actual surface*").
- Zero board conditions, zero board changes.
- Satisfies the mid-flight amendment's actual *requirement* — "a member carries BOTH identities … the human name is stable before launch, in flight, and after return" — because `task_id` is never destroyed.

The shipped shape widens the contract to enable a member that is **invisible**; the un-widened alternative delivers the design's stated goal. The car's own rule "*Never a placeholder: a member's subject is either the genuine dispatch id or not present at all*" (`schema/starcar-manifest.schema.json`, `members.subject` description) is self-authored in this commit and is what forecloses the alternative — it is not inherited doctrine. The conductor's amendment did invite "*a defensible alternative*" and an honest stop. Having discovered that its chosen shape defeats the brief's own purpose, the car had the evidence in hand to escalate; it shipped and ticketed instead.

**(d) Description texts — mostly TRUE, one overclaim (R1-m3).** The `subject` description's board-side disclosure is verbatim accurate against `assemble.go:304-307`; I confirmed every clause. But the `task_id` description asserts "*present on every member declared by New-TrainManifest.ps1 going forward*", and `-Members`' param doc (`:83-85`) calls `task_id` "mandatory". **Measured: a subject-only member mints successfully** (`train:subjonly` written, valid, sealed). Two documents claim an invariant the code does not enforce.

---

# ADJUDICATION 3 — the TDD deviation. **Compensation was weaker than claimed; the tests are nonetheless non-vacuous.**

The car's `CommandNotFoundException` demonstration proves only that the tests *invoke* the script. It does **not** prove behaviour-sensitivity. So I injected six **behavioural** faults on an out-of-repo clone and ran the suite each time (baseline in clone: **21/21 green**).

| Fault (behaviour, not existence) | Result | Red for its own stated reason? |
|---|---|---|
| **F1** mint emits a placeholder `subject` | 18 P / **3 F** | Yes — "*Expected $null or empty, but got string REDACTED*"; also reds the board-gap pin |
| **F2** seal over NON-compact JSON (2nd hashing rule) | 20 P / **1 F** | Yes — integrity mismatch, `sha256:38b38a…` vs `sha256:ac31bb…` |
| **F3** amend replaces `task_id` instead of adding `subject` | 20 P / **1 F** | Yes — the task_id-keyed lookup finds nothing |
| **F4** amend writes in place (no supersession) | 13 P / **8 F** | Yes — byte-identical, new-record, fold-winner and non-vacuity tests all red |
| **F5** schema validation skipped | 18 P / **3 F** | Yes — both `anyOf`/`role` rejection tests red |
| **F6** amend silently no-ops on unmatched task_id | 20 P / **1 F** | Yes — the named-error test reds |

**Ruling: every test I probed is behaviour-sensitive; none passes regardless of behaviour.** The suite is genuinely non-vacuous, so the deviation caused no coverage harm — but the car's own compensation did not establish this, and the report presents it as if it did. Disclosed-and-weaker, not disclosed-and-wrong. I record it as a Note, not a finding, because the substance holds.

**Restore verified byte-identical:** clone `git status` = **0 dirty**; restored `scripts/New-TrainManifest.ps1` sha256 = `9BFC4097E2363D8B3796B7C3B1471CA806F840579893EE607DD2C1D9E21D5417`. All injection ran on the out-of-repo clone; the review worktree was never mutated.

---

# ADJUDICATION 4 — the two self-found bugs. **Both fixes are sound; the placeholder leaves no real gap.**

**(i) `Join-Path` rooted-second-argument.** Fix at `:303-307` uses `[System.IO.Path]::IsPathRooted` before combining. Correct, and load-bearing — every absolute `-StoreRoot` in my probes worked.

**(ii) The placeholder-sealed validation copy (`:212-215`).** I tested the gap directly rather than reasoning about it.

- **Definitive test:** minted two records into the clone's **real** `artifacts/` store, then ran the real suite — **store-checks 269/269, 0 failed**. The minted records pass the base schema, the layered manifest schema, **and** the independent integrity recompute in `StoreIntegrity.Tests.ps1:64-70`.
- **Malformed is structurally unreachable:** `Get-Sha256Hex` (`scripts/Artifact.psm1:140`) returns `BitConverter.ToString(...) -replace '-','' | ToLower()` — always 64 lowercase hex, always matching `^sha256:[0-9a-f]{64}$`.
- **Wrong-value hunt:** five hostile titles (non-ASCII `ñ`/`é`, `&lt;b&gt;&amp;&lt;/b&gt;`, embedded quotes + backslashes, an embedded newline, forward slashes) — **all five: `integrityVerifies=True, baseSchemaValid=True, integrityFormat=True`.** No serialization round-trip divergence found.

**Ruling: no practical validation gap.** The placeholder is honest engineering — it validates shape when shape is all that can be validated, and the value it stands in for is generated by a function that cannot produce a non-conforming result.

---

# THE SENTENCE CHECK — producer → store → scan → fold → assemble → wire → client

| Hop | Location | What crosses |
|---|---|---|
| 1. Member normalise | `scripts/New-TrainManifest.ps1:137-146` | ordered dict, only present keys, fixed order `task_id, subject, role, gate` |
| 2. Payload assembly | `:184`, `:195-199` | `manifest{title,tickets,members}` |
| 3. Pre-write validation | `:212-215` | throwaway copy + placeholder integrity, **both** schemas (`:156-159`) |
| 4. **Seal** | `:217-218` → `scripts/Artifact.psm1:124-141` | `sha256:` + `Get-Sha256Hex(compact body)` |
| 5. Write | `:266-269`, `:271-273` | `&lt;store&gt;/&lt;subject ':'→'-'&gt;/intent-&lt;compactAt&gt;.json`, UTF-8 no BOM |
| 6. Scan | `board/store` recursive `*.json` (per `assemble.go:326-328`) | raw record |
| 7. Fold | `board/fold/algorithm.go:130` | requires record-level `subject`/`kind`; latest-`at` intent wins |
| 8. **Assemble** | `board/assemble/assemble.go:94` → `:286-313` | `manifestPayload` reads **only** `subject`(`:304`), `role`(`:305`), `gate`(`:306`) |
| 9. Wire | `board/assemble/types.go:45` | `DeclaredNotObserved []string \`json:"declaredNotObserved"\`` |
| 10. Client | `/api/snapshot` → `lanes[trains].data.trains[]` | observed live |

### **Where it BREAKS**

**Break 1 (adjudication 1, ticketed as #85):** at hop 8, `assemble.go:307` — a `task_id`-only member is dropped before hop 9 exists for it. `declaredNotObserved` is `[]`, `cars` is `[]`, conditions are `[]`. Confirmed live.

**Break 2 — NOT disclosed anywhere, and it is R1-M2 (Major).** `manifestPayload` (`:304-306`) **never reads `task_id` at all.** The board's only task-id source is `taskIDsBySubject` (`:426-437`) → `taskIDForReturnedSubject` (`:450-462`), which reads `r.Fields["task_id"]` off **returned records exclusively**. Therefore a manifest member's `task_id` — the entire field this commit adds to the contract — **is read by nothing in this repo.**

Measured, an amended member (`task_id: tooling-76-car-r1`, `subject: ad9b6aad6153127f4`) with a dispatched-only record:

```json
"cars":[{"subject":"ad9b6aad6153127f4","role":"car","state":"overdue",
         "at":"2026-07-27T10:05:00Z","recordDir":"ad9b6aad6153127f4"}]
```

**No `taskId` field at all** — in either the trains lane or the dispatches lane. That is precisely the symptom the owner reported on the live board and that triggered the mid-flight redesign. The conductor's amendment asserted "*Naming an in-flight dispatch from its MANIFEST MEMBERSHIP is the non-brittle route, and it is why the both-identities shape matters*". **That sentence is false against the code at HEAD** (`assemble.go:414` even says so: "*dispatched records carry no task_id yet, pending #76's producer work*"). The car verified one clause of that comment by running the board and left the adjacent clause — the one carrying the train's headline goal — unchecked and undisclosed. #85 covers only the `declaredNotObserved` drop.

### The seal — Law 6 CLEAN

`New-TrainManifest.ps1:217-218` is byte-for-byte the idiom at `Produce-Artifact.ps1:364-365`, both calling the single owner `Get-Sha256Hex` (`Artifact.psm1:124-141`, whose own docstring names itself "THE ONE OWNER"). **No second hashing rule introduced.** Independently verified: F2 proves the test detects a changed rule; store-checks 269/269 proves minted records verify under the repo's own independent recompute.

---

# FINDINGS

### Major (any Major = REJECT)

**R1-M1 — the `required` relaxation is not demonstrably necessary; a measured alternative serves the design without it.**
`schema/starcar-manifest.schema.json:45-49`. Evidence above: the seed-subject-with-task-id shape validates against the pre-#76 `required: ["subject","role"]` (`True`), needs no board change, and renders `declaredNotObserved:["tooling-76-car-r1","tooling-76-review-r1"]` — the readable plan-vs-actual surface the corrected design asked for — where the shipped shape renders the member invisible. The brief asked exactly this question; the answer is that the contract was widened to enable the worse of two shapes.

**R1-M2 — undisclosed: a manifest member's `task_id` is read by nothing, and in-flight naming remains unfixed and untracked.**
`board/assemble/assemble.go:304-306` (task_id absent from `manifestPayload`), `:426-462` (task-id sourced only from returned records). Measured: an in-flight member renders with no `taskId`. The mid-flight comment's manifest-membership-naming premise is unmet; #85 does not cover it; no ticket exists. The car's disclosure names one consequence of "assemble keys strictly on subject" and omits the second, which is the one the owner actually asked to see.

**R1-M3 — stale document: the spec that OWNS this contract was left contradicting it.**
`docs/specs/2026-07-23-yard-board-spec.md:46` still reads `members[]{subject, role, gate?}` — `subject` presented as unconditional (the file's own `?` convention marks optional), and `task_id` absent entirely — under the heading **"## 2. The manifest contract"**, "per `schema/starcar-manifest.schema.json`". The schema's own description simultaneously cites "*Spec 2026-07-23-yard-board S2*" as this contract's origin, so the two documents now cite each other and disagree.
Not a fossil: `docs/doc-map.md:53` classifies `docs/specs/*` as **"LIVING while binding; amendment blocks, never rewrites"**; the spec's header reads `Status: Open`, `rev 2 APPROVED`; and the amendment mechanism is already in the file at **§7b "Plan-rung amendments (binding; supersede the named text above)"** with two amendments recorded, one explicitly reversing prior text. `StoreIntegrity.Tests.ps1:109` cites "spec S7b amendment 2" by name, proving the mechanism is live and consumed. The same-commit rule (NORTH STAR: "the commit that invalidates a document updates that document, in the same commit") applies and was not honored.

### Minor

**R1-m1 — the commit failure path reports a confidently wrong cause.** `scripts/New-TrainManifest.ps1:285-291` pipes git's stdout+stderr to `Out-Null`, then throws `"could not commit … after 3 attempts (index contention?)"` for *any* failure. Observed: an author-identity failure (`fatal: unable to auto-detect email address`) surfaced as the index-contention message. Aggravating: the record is already written (`:273`) and `git add` already succeeded, so the operator is left with an uncommitted-but-staged record and a misleading diagnosis. Law 1 / "error messages must be human-readable and actionable".

**R1-m2 — the "mirrors" claim is inaccurate; PowerShell `ValidatePattern` is case-insensitive.** `:70-74` says the literal "mirrors" the schema pattern; `New-TrainManifest.Tests.ps1:102` attributes enforcement to "an early ValidatePattern gate" and `:108` names the uppercase test accordingly. **Measured: `ValidatePattern('^train:[a-z0-9][a-z0-9-]*$')` ACCEPTS `train:Tooling-76`.** The uppercase rejection comes solely from the schema (proven by F5: with schema validation removed, `train-Tooling-76/` was written to disk). The behaviour is correct; the attribution is wrong in two files.

**R1-m3 — doc claims an invariant the code does not enforce.** `schema/starcar-manifest.schema.json` `members.task_id` description ("*present on every member declared by New-TrainManifest.ps1 going forward*") and `scripts/New-TrainManifest.ps1:83-85` ("*task_id (mandatory)*"). Measured: a subject-only member mints successfully.

**R1-m4 — a new silent-loss path is created and recorded on no gated-surface document.** Before this commit the schema rejected subject-less members, making `assemble.go:307` unreachable for schema-valid records. After it, the schema *admits* a shape the board discards with **zero board conditions**. `docs/contracts/gating-matrix.md` — whose doc-map row reads "*Reviewers reject diffs that leave it stale*" — has no row naming this. Law 4 (silent loss). Partly mitigated by #85.

**R1-m5 — the in-code disclosure is stale on arrival.** `scripts/New-TrainManifest.ps1:40-42` says the gap is "*reported for the owner/conductor to judge and, if warranted, file as a follow-on board-side ticket*"; #85 **was** filed. Neither the script header, the test header (`:17-23`), nor the schema description cites #85, so a reader chasing the disclosure has no pointer to the tracked owner.

### Notes (no action required)

- **N1** — Adjudication 3's compensation was weaker than the report implies (existence-failure ≠ behaviour-sensitivity), but my six injections establish the suite is genuinely non-vacuous. Substance holds.
- **N2** — `board/web` attribution **confirmed**: the diff touches zero `board/web` files (`git diff --name-only` = 3 files, all `schema/` + `scripts/`); `board/web/node_modules` is absent in my worktree too; all 5 failures are `ERR_MODULE_NOT_FOUND: Cannot find package 'playwright'` across the five `browser-*.test.js` files. Pre-existing environment gap, unrelated to this diff.
- **N3** — #79's `ManifestBoardJoin` probe is **not** a guard against the #85 class: its fixture members all carry `subject` (`ManifestBoardJoin.Probes.Tests.ps1:194-197,225`) and none carries `task_id`. Issue #85's cross-ref stating this is accurate. Probe unaffected by the schema change (29/29).
- **N4** — Citation check (#42) **PASSES**: `CodeCitationPolicy.Tests.ps1` 13/13; both new files carry the standalone bare marker (`New-TrainManifest.ps1:64`, `New-TrainManifest.Tests.ps1:25`) plus 6 and 10 in-context `#76` citations; the schema edit carries symbol-form `#76` on the changed description and both new/changed property descriptions. Right ticket number throughout.
- **N5** — `docs/contracts/state-ledger.md` is **not** made stale: this adds no mutable service state. Design docs carry no member-contract statement, so R1-M3 is confined to the spec.

---

## THE WORKED EXAMPLE — both commands RUN AS WRITTEN

Executed against the out-of-repo clone's real `artifacts/` store, default (committing) path:

```
New-TrainManifest: wrote artifacts\train-tooling-76\intent-20260727T110000Z.json
git log -1: dc55c2d harness: train manifest intent record for train:tooling-76 (#76)
dirty files after mint: 0

New-TrainManifest: wrote artifacts\train-tooling-76\intent-20260727T110500Z.json
git log -1: 9aa1b94 harness: train manifest intent record for train:tooling-76 (#76)
dirty files after amend: 0
```

The amended record is correct: `task_id` preserved, `subject` added, canonical member order restored (`task_id, subject, role`), the untargeted gate member left with no `subject`, and the prior record untouched. **The worked example executes; the conductor can paste it.** One caveat for the conductor: the commit path requires a configured `user.email`/`user.name` — absent one, you get R1-m1's misleading "index contention?" message.

## UNREPRODUCIBLE-EVIDENCE CALLOUT (#13)

All evidence above is re-derivable from the repo alone; nothing rests on session-local state. Recipe: `go build -o &lt;exe&gt; ./server` from `board/`; mint into a scratch dir with `scripts/New-TrainManifest.ps1 -StoreRoot &lt;dir&gt; -NoCommit`; run the exe with `STARCAR_STORE_PATH=&lt;dir&gt;`, `STARCAR_PORT=0`, `STARCAR_POLL_MS=150`; read `/api/snapshot`. Hand-sealed dispatched/returned fixtures use `Get-Sha256Hex` over the compact body, never a second rule. My probe scripts live in the session scratchpad (`rev76-e2e.ps1`, `rev76-schema.ps1`, `rev76-faults.ps1`, `rev76-faults2.ps1`, `rev76-worked.ps1`, `rev76-alt.ps1`, `rev76-inflight.ps1`) and are session-local — the recipe above, not those files, is the durable form.

## CONSTITUTION CHECK

- **Law 1 (no confident falsehood)** — *Honored* in the board-gap pin (`Tests:308-314` asserts today's truth, not the hope) and in the schema's `subject` description, which I verified clause-by-clause against `assemble.go:304-307`. *Violated* at R1-M3 (spec:46 states a contract the code no longer honors), R1-m1 (wrong cause asserted), R1-m2 (wrong mechanism attributed), R1-m3 (unenforced invariant asserted).
- **Law 4 (never silently drop)** — *Violated in effect* at R1-m4: this diff converts a schema-impossible state into a schema-legal, silently-discarded one, with zero board conditions. Disclosed and ticketed (#85), but recorded on no gated-surface document.
- **Law 6 (one owner per rule)** — *Honored, and this is the diff's strongest leg.* `Get-Sha256Hex` (`Artifact.psm1:124-141`) is the sole hashing owner; `New-TrainManifest.ps1:217-218` matches `Produce-Artifact.ps1:364-365` exactly; `Get-CurrentManifest` (`:231-261`) reads latest-`at` to locate a record and explicitly does not re-implement `board/fold`'s selection. F2 proves a second rule would be caught. Minted records verify under the independent recompute at `StoreIntegrity.Tests.ps1:64-70` (269/269).
- **Law 7 (the stranger / kinds and roles as data)** — *Honored*: no role or kind taxonomy is hardcoded in the new script; `role` and `gate` pass through as opaque caller data, and `schema/vocab/roles.json` remains the vocabulary owner. *Weakened* at R1-m5: a stranger reading the disclosure has no pointer to #85.
- **NO-BACKFILL doctrine** — **Honored, and verified rather than assumed.** All six pre-#76 records validate unchanged against both schemas; no member was retrofitted; the car explicitly declined to make `task_id` unconditionally required *because* it would have invalidated them. This was the right call and the reasoning is on the page.
- **Living contracts / same-commit doc rule** — *Violated* at R1-M3.
- **Honest stop as a success outcome** — *Honored* on adjudication 1: the car ran the thing, disproved a binding premise, refused to fix out of scope, pinned the truth, and filed a board-tracked ticket. That behaviour is exemplary and I want it on the record separately from the REJECT.

## What a fix cycle needs (same car, delta re-review)

1. **R1-M1** — either adopt the seed-subject shape and revert the `required` relaxation, or escalate to the owner with both measured board outputs (I have supplied them) and let the owner rule. Do not resolve this one unilaterally in either direction.
2. **R1-M2** — file the in-flight-naming ticket and state plainly in the report and the in-code disclosure that manifest `task_id` is currently read by nothing.
3. **R1-M3** — add a §7b amendment block to the spec restating the member contract, same commit.
4. **R1-m1..m5** — mechanical.

Items 1 and 2 are the ones that matter; 3 is cheap and non-negotiable under this repo's own rules.

```starcar-artifact
task-id: tooling-76-review-r1
outcome: REJECT
findings: 3 Major, 5 Minor, 5 Notes at round 1. R1-M1 (Major) the schema `required` relaxation is not demonstrably necessary - I built board/server and measured an alternative member shape (task_id AND subject both seeded with the shop-minted handle, amendment overwrites subject while task_id survives) that validates against the PRE-76 required subject-plus-role schema (True), needs zero board changes, and renders declaredNotObserved as the readable names tooling-76-car-r1 / tooling-76-review-r1 - exactly the plan-vs-actual surface the corrected design demanded - where the shipped shape renders the member invisible; the contract was widened to enable the worse of two shapes. R1-M2 (Major) undisclosed - board/assemble/assemble.go:304-306 manifestPayload never reads task_id at all and :426-462 taskIDsBySubject sources task-id exclusively from RETURNED records, so a manifest member's task_id is read by nothing in the repo; measured, an in-flight dispatched-only member renders with no taskId field in either lane, meaning the mid-flight comment's naming-an-in-flight-dispatch-from-manifest-membership premise is false against HEAD, the owner's headline goal is undelivered, and issue 85 does not cover it. R1-M3 (Major) stale document - docs/specs/2026-07-23-yard-board-spec.md:46 under the heading The manifest contract still states members subject-role-gate-optional with task_id absent, while doc-map.md:53 classifies specs LIVING while binding, the spec header reads Status Open rev 2 APPROVED, and its own section 7b binding-amendment mechanism already carries two amendments (one cited by name at StoreIntegrity.Tests.ps1:109); the schema's description cites Spec S2 as this contract's origin, so the two documents now cite each other and disagree. Minors - R1-m1 the commit path pipes git stderr to Out-Null and throws a confidently wrong index-contention cause for any failure (observed with an author-identity failure) leaving a written-and-staged record; R1-m2 PowerShell ValidatePattern is case-insensitive and ACCEPTS train:Tooling-76, so the mirrors claim at :70-74 and the test Context at :102/:108 both attribute the uppercase catch to the wrong mechanism (it is the schema); R1-m3 the schema task_id description and the -Members param doc both assert task_id is mandatory but a subject-only member mints successfully (measured); R1-m4 the widened schema admits a shape the board discards with zero board conditions, a new silent-loss path recorded on no gated-surface document; R1-m5 the in-code disclosure says a ticket may be filed if warranted, but 85 was filed and is cited nowhere in the script, tests, or schema. VERIFIED AND CONFIRMED - adjudication 1 the car is RIGHT end to end - I built board/server from source and ran a four-shape member matrix through real HTTP /api/snapshot - a task_id-only member appears in neither cars nor declaredNotObserved with zero conditions, root cause correct and complete, the pin is an honest pin of today's behaviour not the hope, and staying out of board/ was the right call. Adjudication 2 the anyOf enforces exactly what it claims across all seven measured directions (neither task_id nor subject REJECTED, each alone VALID, role unconditionally required) and NO-BACKFILL is genuinely honored - all six landed train-board-v0 records validate against BOTH schemas, and StoreIntegrity.Tests.ps1:121-136 does layer the manifest schema so the 267/267 citation is real evidence. Adjudication 3 the car's CommandNotFoundException compensation proves only invocation not behaviour-sensitivity, so I ran six behavioural fault injections on an out-of-repo clone (baseline 21/21): placeholder-subject 3 red, non-compact seal 1 red, task_id-replacing amend 1 red, in-place write 8 red, validation-skipped 3 red, silent-no-op amend 1 red - every one red for its own stated reason, suite genuinely non-vacuous, restore byte-identical (0 dirty, sha256 9BFC4097E2363D8B3796B7C3B1471CA806F840579893EE607DD2C1D9E21D5417). Adjudication 4 both fixes sound and the placeholder leaves NO practical gap - minted records into a real artifacts store scored store-checks 269/269 including the independent integrity recompute, Get-Sha256Hex structurally cannot emit a non-conforming value, and five hostile-title round-trips all verified True. Law 6 clean - one hashing owner, New-TrainManifest.ps1:217-218 matches Produce-Artifact.ps1:364-365 exactly, no second rule. Worked example RUNS AS WRITTEN - both mint and amend committed cleanly with correct field order and preserved task_id. Suites observed at 8f9916c - scripts/tests 243 passed 0 failed 5 skipped (car's 244/0/4 reconciles exactly via my detached-HEAD skip of SessionStartGuardRunner's branch-conditional test), baseline 222+5skip confirming the 223 claim, store-checks 267/267, probes 29/29 with 79's ManifestBoardJoin NOT reded, Go board 98 top-level PASS (118 including subtests) vet clean, board/web 209 pass 5 fail all ERR_MODULE_NOT_FOUND playwright with the diff touching zero board/web files. Citation check 42 passes 13/13 with bare #76 markers in both new files.
abstract: Adversarial sentence-check review of #76 producer-side train-manifest minting tooling at 8f9916c over base 3458499. REJECT on 3 Major. The car's central empirical claim is CONFIRMED independently - I built board/server and observed a task_id-only manifest member vanish from both cars and declaredNotObserved with zero board conditions, exactly as reported; its root-cause analysis is correct and complete, its red test is an honest pin of today's behaviour, and staying out of board/ was the right call. That is the process working and it is the best part of this diff. The REJECT rests elsewhere. First, the schema required-relaxation is not demonstrably necessary - I measured an alternative member shape that satisfies the corrected design's both-identities requirement, validates against the unmodified pre-76 contract, needs no board change, and renders READABLE names in declaredNotObserved where the shipped shape renders the member invisible. Second, an undisclosed gap - the board reads manifest task_id nowhere, so in-flight rows still render the runtime hash and the train's headline goal is undelivered and untracked, the conductor's own premise about naming from manifest membership being false against HEAD. Third, the spec that owns this contract was left stating the superseded member shape despite a live in-file amendment mechanism and a LIVING classification. Five Minors cover a misleading commit-failure diagnosis, a case-insensitive ValidatePattern mis-attributed in two files, an unenforced mandatory-task_id claim, an unrecorded new silent-loss path, and a disclosure that never cites the ticket it produced. Six behavioural fault injections on an out-of-repo clone prove the new suite non-vacuous despite a weaker-than-claimed TDD compensation, the placeholder-integrity design leaves no real validation gap (269/269 store-checks on freshly minted records), Law 6 sealing is single-owner and clean, NO-BACKFILL is genuinely honored, and both worked-example commands execute as written.
```</result>
<usage><subagent_tokens>188949</subagent_tokens><tool_uses>69</tool_uses><duration_ms>1343576</duration_ms></usage>
</task-notification>

---

ok roll 84 and we'll do the redesign now

---

<task-notification>
<task-id>a84d35af573e0a7d1</task-id>
<tool-use-id>toolu_01LtUqqWfj5H5vM1GPonHk7e</tool-use-id>
<output-file>C:\Users\Chris\AppData\Local\Temp\claude\C--Users-Chris-git-starcar\8a347c36-20b4-427a-ae0b-46855aa7bb72\tasks\a84d35af573e0a7d1.output</output-file>
<status>completed</status>
<summary>Agent "Design #87 member identity" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Design landed. One file, 564 lines, nothing else touched, nothing pushed.

## §0 answer, first, with its reasoning

**BOTH — and the format half is NOT written here.** The behavioural half (who writes the plan-to-actual binding, when it can exist at all, which component derives it, what each surface renders when it does not exist) is prose in this document. The format half (identity key, `required` set, join rule, precedence, ambiguity, dedup) is specified as an executable deliverable — a schema delta plus 11 named conformance vectors plus red-first tests — and deliberately not held in prose.

The split is a **constraint**, not my preference. A landed verdict in this repo already ruled on this exact class: *"identity, join keys, dedup and ambiguity are format/protocol — prose cannot hold them"* (`artifacts/reviews/2026-07-24-dual-runtime-design-review-round3-REJECT.md:77`). I invite the reviewer to attack my own §0 as §10 Q6, with the test stated: any join/precedence/ordering/dedup rule appearing as prose outside §5.6 is a §0 violation by me.

## Probes run — every behavioural claim was RUN, not read

All six land in the document at §2b with a re-derivation recipe, because an unlanded probe is vigilance-tier.

- **PR-1 (load-bearing, new).** Shape B against a **real `runtime-id` dispatch** rather than an empty store. Observed: `"cars":[]`, `"declaredNotObserved":["d87-car-r1","d87-review-r1"]` while the same wire carried `{"assigned":false,"outcome":"done","state":"returned","subject":"aaaaaaaaaaaaaaaaa","taskId":"d87-car-r1"}` — **zero conditions**. The board asserts a member has not departed while holding its returned record. The round-1 reviewer measured shape B only in the epoch where it cannot be wrong.
- **PR-2.** Same store, same run: a `minted-id`-family train renders a full consist, filled gates lane, `assigned:true`, zero conditions — **with no change to anything.**
- **PR-3.** Two subjects returning with one `task_id`: both render, **zero conditions**. No ambiguity guard exists.
- **PR-4.** Legacy corpus: 6 manifests, 10 member subjects, all runtime ids, **0 of their 16 returned records carry `task_id`**; a legacy-shaped manifest binds and renders today.
- **PR-5.** 97 live launch payloads: `tool_input` key set is exactly `description, model, prompt, subagent_type`, `description` populated **97/97**, carrying prose not the handle.
- **PR-6.** Real dispatched record carries no `task_id`; its returned sibling does.

## Premises attacked, and what survived

| Premise | Verdict |
|---|---|
| P1 one field, two masters (the conductor's) | **FALSE, measured.** The single-field pressure comes from the *consumer* — `assemble.go:304-307` reads one key — promoted to a law of the format. |
| P2 join must be string equality on a single field | **Half survives.** Equality yes; on the identity field, no. |
| P3 identity must be a string | **Survives, deliberately.** The structured alternative re-imports mutation. |
| P4 amendment must mutate identity | **FALSE, stronger:** no identity amendment exists to perform. |
| P5 the manifest must name the runtime id | **FALSE. Dissolves the most.** |
| **P6 (mine, nobody had written it)** the binding must be *written* at launch | **False for returned, true for in-flight.** Shape A vs shape B is a disagreement about one missing wire, and neither said so. |
| **P7 (mine)** the conductor will reliably amend | Both rejected shapes are correct only if this holds; neither states it. Vigilance-tier. |
| **P8 (mine)** one problem, one shape | **FALSE, measured (PR-2).** Solved non-problem for the `minted-id` family. |

## Two disclosures against myself

1. **I touched the shared checkout.** A PowerShell block used `[System.IO.File]` with a relative path; `Set-Location` does not change .NET's `CurrentDirectory`, so it resolved against `C:\Users\Chris\git\starcar` and created a **0-byte untracked file** at `docs/design/2026-07-27-manifest-member-identity-design.md` there. That fault-injection run was therefore also **vacuous** — it reported green having injected nothing. Removed; shared checkout verified clean (`git status --porcelain` empty). No tracked state was touched. Redone correctly with absolute paths and `[Environment]::CurrentDirectory` set.
2. **§2b is an addition to the template's section order**, disclosed in the document itself. The template has no home for probes that *succeeded*, and the probe doctrine requires them to land with coordinates.

## Verification

- `CitationResolverPolicy` + `DocPolicy` + `CodeCitationPolicy`: **43 passed / 0 failed / 4 skipped** at the committed tree.
- **The citation gate scans `git ls-files` only** — my first green run never saw the untracked file and proved nothing. Staged, re-ran, then fault-injected `docs/constitution.md:99999` and watched it red by name: `docs/design/2026-07-27-manifest-member-identity-design.md:55 cites 'docs/constitution.md:99999' - target file has fewer lines`. Restored byte-identical, sha256 `4B3AC3AD…52D5`.
- `scripts/tests`: **223 passed / 0 failed / 4 skipped**.
- **One dead citation caught and fixed before commit:** the round-1 verdict cites `StoreIntegrity.Tests.ps1` bare; it lives at `scripts/store-checks/`, not `scripts/tests/`.

Document: `C:\Users\Chris\git\starcar-wt\design-87\docs\design\2026-07-27-manifest-member-identity-design.md`
Commit: `9e18799ace9332d6d4b472bb2d46a77f1666bf5c` on `design/member-identity-87`, base `51fb403`. Not pushed. `board/` untouched (no #84 collision).

**Deviations:** the §2b insertion (disclosed in-document); the shared-checkout write above. No honest stops — the brief's framing survived contact, though its premise did not.

```starcar-artifact
task-id: design-87-r1
outcome: done
findings: Section 0 answers BOTH - behavioural half in prose here, format half (identity key, required set, join rule, precedence, ambiguity, dedup) specified as an executable deliverable of 11 named conformance vectors and NOT held in prose, binding on the landed ruling at artifacts/reviews/2026-07-24-dual-runtime-design-review-round3-REJECT.md line 77 that identity, join keys, dedup and ambiguity are format and prose cannot hold them. THE CONDUCTOR PREMISE IS FALSE AND MEASURED SO. PR-1, the load-bearing new probe - shape B placed in front of a REAL runtime-id family dispatch rather than the empty store the round-1 reviewer used, renders cars empty and declaredNotObserved carrying d87-car-r1 and d87-review-r1 while the same wire simultaneously carries a returned record subject aaaaaaaaaaaaaaaaa outcome done taskId d87-car-r1, with ZERO board conditions. The board asserts a member has not departed while holding its returned record. That is Law 1's named worst case and a worse class than shape A's silent drop, so the reviewer's R1-M1 alternative was measured only in the epoch where it cannot be wrong. PR-2, same store same run, a minted-id family train renders full consist, filled gates lane, assigned true, zero conditions, with no change to anything - member identity is a solved non-problem for that family, so the crisis is one family's launch event not carrying the shop handle, which is adapter territory. PR-3, two distinct runtime subjects both returning task_id dup-handle-r1 both render with zero conditions - no ambiguity guard exists and any handle-keyed join inherits the hole. PR-4, the landed legacy corpus is 6 manifests, 10 distinct member subjects all runtime ids, owning 16 returned records of which ZERO carry task_id, and a legacy-shaped manifest binds and renders fully today - so the fallback clause protects RENDERING and not merely validation. PR-5, 97 captured live launch payloads, every tool_input key set exactly description-model-prompt-subagent_type with description populated 97 of 97, carrying prose not the handle - a dedicated conductor-authored key exists and the C3R-1e ruling forbids parsing the free-text prompt, not reading a field that exists. PR-6, a real dispatched record carries no task_id while its returned sibling does. PREMISES ATTACKED - P1 one field two masters FALSE, the single-field pressure comes from the consumer at assemble.go 304-307 and was promoted to a law of the format; P2 half survives, equality yes but not on the identity field; P3 string survives deliberately; P4 FALSE and stronger, no identity amendment exists to perform; P5 FALSE and dissolves the most; and three premises nobody had written down - P6 the binding must be written at launch, false for returned and true for in-flight, which reduces the entire shape A versus shape B argument to one missing wire; P7 the conductor will reliably amend, on which both rejected shapes silently depend; P8 one problem one shape, false by PR-2. DECISIONS - a member is identified by its shop-minted task-id and never carries a runtime id, the binding is DERIVED by the assembler and never declared or amended, the anyOf is kept but justified by NO-BACKFILL rather than by shape A's reason, a declared member always renders, an unattributed dispatch is disclosed, ambiguity is shown never resolved by precedence, and the in-flight wire is separable so the design stands whether or not it lands. TWO SELF-DISCLOSURES - I wrote a 0-byte untracked file into the SHARED CHECKOUT because Set-Location does not change .NET CurrentDirectory, which also made that fault-injection run vacuous; removed, shared checkout verified clean, redone correctly with absolute paths and watched to red by name. And section 2b is a disclosed addition to the template's section order because the template has no home for probes that succeeded. VERIFICATION - CitationResolverPolicy plus DocPolicy plus CodeCitationPolicy 43 passed 0 failed 4 skipped at the committed tree; the citation gate scans git ls-files only so my first green never saw the untracked file and proved nothing, corrected by staging then fault-injecting docs/constitution.md:99999 and observing the named red at design file line 55, restored byte-identical sha256 4B3AC3ADB16B6E9D20B9B32B217476C2C3BB9E10BF4E5B496B8E2EF0BBC552D5; scripts/tests 223 passed 0 failed 4 skipped. One dead citation caught and fixed before commit - the round-1 verdict cites StoreIntegrity.Tests.ps1 bare and it lives under scripts/store-checks not scripts/tests. Six real open questions carry costs, and Q6 invites attack on my own section 0 split.
abstract: Design rung 1 for issue 87, manifest member identity, authored in worktree design-87 on branch design/member-identity-87 over base 51fb403 and committed at 9e18799ace9332d6d4b472bb2d46a77f1666bf5c as docs/design/2026-07-27-manifest-member-identity-design.md, 564 lines, single file, board untouched so no collision with the in-flight freight work, nothing pushed. Section 0 rules BOTH and keeps the format half out of prose on the strength of a landed prior verdict. The conductor's central premise - that a member's identity lives in one field serving two masters - was attacked and measured false: the pressure comes from a 10-line consumer function that reads exactly one member key, and that accident of code was promoted into a property of the format. Six probes ran against a board server built from source over sealed scratch stores, all landed in a new section 2b with a re-derivation recipe. The decisive one reframes the rejected shapes: shape B, measured against a real runtime-id dispatch instead of an empty store, renders a returned member as declared-not-observed with zero conditions, a Law 1 confident falsehood rather than the cosmetic placeholder cost the owner's ruling recorded, while shape A degrades to Law 4 silent loss - both correct only while a human remembers to amend. The design removes the amendment entirely: a member is named by the shop-minted task-id it already has, never by a runtime fact, and the plan-to-actual binding is derived by the assembler from data both sides already carry, with an explicit legacy clause proven necessary by measurement over the landed corpus and an ambiguity disclosure proven absent today. Three previously unstated premises were surfaced and attacked, one of which localises the whole shape A versus shape B disagreement to a single missing launch-time wire that 97 captured payloads show a dedicated field already available for. Two self-disclosures are on the record, including a write into the shared checkout that also rendered one fault injection vacuous, caught, cleaned, and redone under observation.
```</result>
<usage><subagent_tokens>212619</subagent_tokens><tool_uses>71</tool_uses><duration_ms>1475988</duration_ms></usage>
</task-notification>