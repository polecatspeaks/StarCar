.claude/probe-logs/
# #50: the SessionStart delivery report - gitignored, freshly truncated per session,
# never committed (it is regenerated by every SessionStart batch).
.claude/session-start-report.txt
testResults.xml
__pycache__/
.ci-checks/
# board/web/package.json (issue #33): the Playwright TEST dependency,
# never shipped - the board/web/ runtime itself still has no npm
# dependency (docs/setup.md's Node.js row).
node_modules/
